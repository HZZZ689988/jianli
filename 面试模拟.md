# 面试模拟

> 目标岗位定位：嵌入式软件工程师（MCU / RTOS / 嵌入式 Linux）。

最新版简历对应的逐项目一问一答版本见：docs/最新版简历逐项目一问一答面试模拟.md。

## 面试表达策略

这份简历的核心优势不建议只讲成“MCU 开发”，更适合包装成：

> 我做过 MCU 底层控制，也参与过 RK3562 Android/Linux 用户态通信链路联调。我的优势是能理解底层硬件状态，也能把 UART、CAN、UDP、MAVLink 等链路接到 Linux 用户态服务和 APK 业务上。

面试中可以突出三条主线：

1. MCU 侧：电源状态机、电量/充电显示、看门狗、UART 协议、Bootloader/IAP。
2. Linux/Android 侧：`/dev/ttyS3`、SocketCAN、UDP socket、用户态 daemon、`poll`、日志和服务联调。
3. 系统联调：RK3562/RK3588、STM32、CAN、网络链路、系统镜像和板端交付问题定位。

---

## 第 1 轮：项目总述

### 面试官问题

请你用 2 分钟介绍一下你简历里的这个项目：

**“手持发射器 RK3562 + STM32F103 控制链路开发”**

回答时尽量包含：

1. 这个项目是做什么的？
2. 系统整体架构是什么？RK3562 和 STM32 分别负责什么？
3. 你主要负责了哪些部分？
4. 你觉得项目里最有技术含量或最难的点是什么？

### 你的原始回答要点

- 项目是无人机手持端，既是发射器，也是控制遥控器交互终端。
- 硬件由 STM32F103RCT6 和 RK3562 组成。
- RK3562 搭载 Android，负责交互、实时画面传输、飞行控制命令。
- STM32 负责开关机、GPS、加速度计、电量显示、充电显示、电量采集估计、RK 端开关机和睡眠。
- 你负责 MCU 端开发，以及 RK 端通信链路开发。
- 包括 RK3562 和飞机端 RK3588/MCU 的 CAN 链路、CAN 交换图传 MAC、RK3562 和 MCU 的通信、RK 对 MCU 固件升级、RK 用户态通信架构、MCU 消息获取、飞机状态检测、转发到 APK。
- 难点在于地面端和飞机端通信、MCU 和 RK3562 通信、多端联调、配合硬件排查 bug。

### 点评

这个回答方向正确，但需要优化三点：

1. “无人机发射”容易让人误解，建议说“无人机手持地面控制终端”或“手持遥控图传终端”。
2. RK、MCU、你负责的内容、难点混在一起，面试官不容易抓住主线。
3. 要注意职责边界：MCU 侧可以说“主要负责”，RK 侧建议说“参与用户态通信服务开发/联调”，避免被追问成完整 Android BSP 或 APK 全部由你独立完成。

### 优化后的回答

这个项目是一个无人机手持地面控制终端，也可以理解为遥控器加图传交互终端。硬件上主要由 RK3562 和 STM32F103RCT6 组成。RK3562 运行 Android，主要负责 APK 交互、图传画面显示、网络通信、MAVLink 消息转发等；STM32F103 主要负责底层控制，比如按键、电源状态机、电量采集、充电检测、低电保护、看门狗，以及和 RK3562 的开关机/睡眠协同。

我主要负责 MCU 侧开发，包括电源按键状态机、电量和充电显示、低电保护、RK3562 `PWRON` 控制、RK-MCU UART 通信协议，以及 UART-IAP Bootloader。RK 侧我参与了用户态通信链路的开发和联调，包括 RK 和 MCU 之间通过 `/dev/ttyS3` 交互状态和命令，RK 侧服务把 MCU 消息、按键事件、飞控相关消息转发给 APK 或图传链路。同时我也参与了 P401 图传无感配对相关功能，通过 CAN/DroneCAN 自定义报文交换天地端 `tun` 网卡 MAC，实现 MAC report、ACK 和去重验证。

这个项目比较难的地方不是单个外设，而是多端联调。MCU、RK3562、APK、P401 图传、飞机端 RK3588 和 CAN/UART/UDP/MAVLink 多条链路都有关联。问题可能出在协议、设备节点、权限、服务启动、硬件接触、CAN 收发器或者时序上，所以调试时需要分层定位，比如先确认 MCU 串口帧是否正确，再看 RK 用户态服务是否收到，再看是否转发到 APK 或 CAN 链路。

### 更偏 Linux 岗位的说法

如果面试岗位更靠近嵌入式 Linux，可以把重点稍微前移到 RK3562 侧：

> 这个项目里我不是只做单片机逻辑，我还参与了 RK3562 Android 用户态通信链路。比如 RK 侧通过 `/dev/ttyS3` 和 MCU 通信，通过 SocketCAN 访问 `can1`，通过 UDP socket 对接 APK 和 P401 图传链路。用户态服务需要同时处理 UART、CAN、UDP、UDS 等多个 fd，并根据消息来源做路由和转发。所以这个项目对我来说比较有价值的一点是，我把 MCU 底层状态、Linux 设备节点、用户态服务和上层 APK 业务链路串起来做过联调。

---

## 第 2 轮：RK-MCU UART 协议

### 面试官问题

你刚才提到 RK3562 和 STM32F103 之间通过 UART 通信。

请你回答：

**你们为什么要自己设计一套 UART 协议？这个协议大概长什么样？里面的帧头、type、seq、len、TLV、CRC 分别有什么作用？**

### 你的原始回答要点

- UART 本身是字节流，没有天然的消息边界。
- 需要自己定义类型、长度、消息边界和校验。
- MCU 和 RK 之间要相互传输数据。
- RK 端还要发送新固件给 MCU 做升级。
- 帧格式由帧头 `0xA5`、消息类型、发送方递增序号、消息长度、payload、CRC 校验组成。
- payload 里使用 TLV，也就是 Type-Length-Value。
- 多字节字段使用小端组合。

### 点评

这次回答抓住了关键点：**UART 是字节流，所以必须自己解决分包、粘包、类型识别和错误校验**。下一步要把每个字段的作用讲清楚，并且结合项目说明它不仅用于状态上报，也用于 RK 触发 MCU 固件升级。

### 优化后的回答

我们自己设计 UART 协议，主要是因为 UART 本身只是字节流。它不像 UDP 那样天然有一包一包的边界，也不像高级协议那样自带消息类型和校验。如果 RK 和 MCU 直接发送裸数据，接收方不知道一条消息从哪里开始、到哪里结束，也不知道这条消息是电量状态、开关机命令，还是固件升级数据。所以我们需要在 UART 字节流上定义一层应用协议。

我们协议的大致格式是：

```text
0xA5 + type + seq + len + payload + CRC16
```

其中 `0xA5` 是帧头，用来帮助接收方在连续字节流里找到一帧数据的开始。如果串口中间丢字节或者解析错位，接收方可以继续扫描下一个 `0xA5` 尝试重新同步。

`type` 是消息类型，用来区分这帧数据的业务含义，比如 HELLO、状态上报、命令下发、ACK、ERROR，或者 IAP 升级相关数据。这样 RK 端和 MCU 端收到帧以后，可以根据 `type` 分发到不同处理函数。

`seq` 是递增序号，主要用于请求和响应匹配，也方便做 ACK、超时重试和日志定位。例如 RK 发出一条命令，MCU 回 ACK 时可以带回对应序号，RK 就知道是哪条命令成功了。

`len` 是 payload 长度，用来告诉接收方后面还有多少字节属于本帧数据。因为 UART 没有消息边界，所以长度字段非常关键，接收方可以根据 `len` 判断一帧是否收完整，也能避免 payload 解析越界。

`payload` 是真正的业务数据。我们在 payload 内部使用 TLV，也就是 Type-Length-Value。这样做的好处是可扩展，比如现在有电量、电源状态、充电状态，后续想增加 GPS 状态、版本号、错误码时，只需要新增 TLV 类型。老版本解析器遇到不认识的 TLV，也可以根据 length 跳过，不至于整个协议不兼容。

`CRC16` 用于校验整帧数据是否被破坏。串口通信可能会受到干扰，也可能出现丢字节或错字节，CRC 可以帮助接收方发现这帧不可信，然后丢弃它，避免错误命令被执行。

多字节字段我们约定使用小端格式解析，避免 RK 和 MCU 两边因为大小端或结构体对齐问题出现不一致。因此我们没有直接传 C 结构体，而是按字节明确组帧和解析。

在项目里，这套协议既用于普通状态交互，比如 MCU 上报电量、充电状态、看门狗状态，RK 下发开关机/睡眠/进入 Bootloader 命令；也用于 RK 侧通过 `/dev/ttyS3` 给 MCU 发送新固件，配合 MCU Bootloader 完成 UART-IAP 升级。

### 更偏 Linux 岗位的说法

如果面试官关注 Linux 方向，可以这样补充：

> RK3562 侧看到的是一个串口设备节点，比如 `/dev/ttyS3`。用户态服务通过 `open/read/write/ioctl/poll` 操作这个 fd。因为 `read` 读到的可能不是完整一帧，可能半帧，也可能多帧粘在一起，所以用户态需要有接收 buffer 和协议解析状态机。解析状态机会先找帧头，再读固定头部，再根据 `len` 等待完整 payload 和 CRC，最后校验通过才分发给业务层。这个过程本质上就是在 Linux 用户态把一个字节流设备封装成可靠的消息通道。

### 可能追问

1. 如果 UART 接收时丢了一个字节，你的协议怎么恢复同步？
2. 为什么不直接发送 C 结构体？
3. TLV 相比固定结构体有什么优点和缺点？
4. CRC16 和 checksum 有什么区别？
5. Linux 用户态服务里，`read` 一次一定能读到完整一帧吗？如果不能，怎么处理？
6. RK 端发送固件升级数据时，如何避免升级失败后 MCU 变砖？

---

## 第 3 轮：Linux 用户态串口读取与协议解析

### 面试官问题

假设 RK3562 用户态服务从 `/dev/ttyS3` 读取 UART 数据。

**`read()` 一次一定能读到一整帧吗？如果不能，可能出现哪些情况？你会怎么设计接收 buffer 和解析流程？**

回答时尽量说清楚：

1. 半帧
2. 粘包
3. buffer
4. 状态机
5. CRC 校验
6. 丢字节后重新同步

### 你的原始回答要点

- `read()` 一次不一定能读到完整一帧。
- 可能出现半帧、粘包。
- buffer 可以设计成 512B、1KB 或 4KB。
- 用状态机分析每一帧。
- 状态机可以分成六段：等待帧头、读取 type、读取 seq、读取 len、读取 payload、验证 CRC。
- 如果期间有错误，则返回等待帧头。

### 点评

这个回答的主方向是正确的，说明你知道串口是字节流，不能把一次 `read()` 当成一条完整消息。需要补强的点有三个：

1. `buffer` 大小不能只说“512B、1K、4K 均可”，更严谨的说法是根据协议最大帧长、串口速率和服务调度延迟设计。RK3562 用户态内存比较宽裕，可以选 1KB 或 4KB；MCU 侧则要考虑 RAM 限制。
2. 状态机不是跟着一次 `read()` 走，而是 `read()` 负责把字节追加到接收 buffer，解析器从 buffer 中尽可能解析完整帧。一次 `read()` 可能只读到半帧，也可能读到多帧。
3. 出错后不能简单清空所有 buffer。更合理的是丢弃当前候选帧，然后从剩余数据中继续扫描下一个 `0xA5`，尽量恢复同步，避免把后面已经收到的有效帧也丢掉。

### 优化后的回答

`read()` 一次不一定能读到完整一帧。因为 UART 本质是字节流，Linux 用户态从 `/dev/ttyS3` 读到的只是当前内核缓冲区里已有的一段字节。它可能只包含半帧，也可能一次读到多帧粘在一起，还可能前面夹杂噪声字节，或者中间因为干扰出现丢字节、错字节。

所以我不会把一次 `read()` 当成一条消息，而是把 `read()` 读到的数据追加到接收 buffer，然后由协议解析状态机从 buffer 里解析。buffer 大小要根据协议最大帧长、串口速率和服务调度延迟设计。在 RK3562 用户态服务里内存相对宽裕，可以用 1KB 或 4KB；如果在 MCU 侧实现，就要更关注 RAM 占用和最大突发数据量。

解析流程一般是：

```text
WAIT_SOF -> READ_HEADER -> READ_PAYLOAD -> READ_CRC -> CHECK_FRAME -> DISPATCH
```

也就是先在 buffer 中扫描帧头 `0xA5`；找到后读取固定头部，比如 `type、seq、len`；拿到 `len` 后先判断长度是否合法，不能超过协议允许的最大 payload；如果当前 buffer 中数据还不够，就继续等待下一次 `read()`；如果数据足够，就取出 payload 和 CRC，计算 CRC 并校验。

CRC 通过后，根据 `type` 分发到业务处理函数，比如状态上报、命令 ACK 或 IAP 数据。CRC 失败、长度异常或类型不合法时，说明当前候选帧不可信，应该丢弃当前候选帧，并继续从后续字节里扫描下一个 `0xA5`，尝试重新同步。这样可以同时处理半帧、粘包、噪声字节和丢字节后的恢复。

更偏 Linux 岗位时可以补充：

> 在 RK3562 侧，`/dev/ttyS3` 是一个 fd。用户态服务可以用 `poll` 等待串口可读，避免阻塞在单个 fd 上。有数据后调用 `read()`，读到多少就放进 buffer，然后解析器循环处理 buffer 中已经完整的帧。这样这套串口协议就被封装成了 Linux 用户态服务里的一个可靠消息通道。

---

## 第 4 轮：CRC 与 checksum

### 面试官问题

你刚才提到 `CRC`。

**CRC 和普通 checksum 有什么区别？为什么你的 UART 协议里更适合用 CRC16？CRC 能不能纠错？**

### 你的原始回答要点

- CRC16 更可靠。
- CRC 的错误检测机制更好，更容易检测出帧是否坏了。
- CRC 无法纠错。
- 普通 checksum 较为简单，只能发现简单错误。

### 点评

这次回答方向正确，但略抽象。面试官可能会继续追问“为什么 CRC 更可靠”。你不一定要把多项式推导讲很深，但至少要说清楚：

1. checksum 通常是简单加和或取反，对某些错误不敏感。
2. CRC 是把数据看成二进制多项式，用约定的生成多项式做除法取余。
3. CRC 对连续突发错误、多个 bit 翻转的检测能力通常比简单 checksum 更强。
4. CRC 是检错码，不是纠错码；它只能告诉你“这帧大概率坏了”，不能告诉你“哪一位错了并修回来”。

### 优化后的回答

普通 checksum 一般是把所有字节做加和、取反或者求补码，计算简单，开销小，但是检错能力比较弱。比如有些字节同时发生变化，最后加和结果可能刚好不变，这种错误 checksum 就可能发现不了。

CRC 的机制更强一些。它不是简单相加，而是把整段数据看成一个二进制多项式，再用约定好的生成多项式做模 2 除法，最后得到一个余数作为校验值。接收方用同样的算法重新计算，如果算出来的 CRC 和帧尾携带的 CRC 不一致，就说明这帧数据在传输过程中大概率发生了错误。

在 RK-MCU UART 协议里，我更倾向于用 CRC16，是因为串口通信可能受到干扰，尤其是控制命令和 IAP 固件升级数据不希望误解析。CRC16 的开销不大，两个字节就能提供比普通 checksum 更强的错误检测能力，适合 MCU 和 RK 之间这种中低速控制链路。

CRC 只能检错，不能纠错。也就是说，它能告诉我们“这帧不可信，应该丢弃或重传”，但不能知道具体哪一位错了，也不能直接把错误修回来。如果要纠错，需要额外的纠错码或者更复杂的重传机制。在项目里更实际的处理方式是：CRC 错误就丢弃当前帧，命令类消息靠 ACK、超时和重试保证最终可靠性；IAP 固件升级则还会做整包 CRC32 校验，避免错误固件写入 APP 区。

### 更简洁的面试版

> checksum 更像简单加和，计算快但检错能力弱；CRC 是基于生成多项式的余数校验，对突发错误和多 bit 错误更敏感。UART 链路上可能有干扰，而且我们还要传控制命令和 IAP 数据，所以用 CRC16 更合适。CRC 只能检错，不能纠错；发现 CRC 错误后一般丢弃这一帧，命令类消息通过 ACK 和超时重试处理。

### 可能追问

1. CRC16 和 CRC32 通常分别适合什么场景？
2. UART 单帧用了 CRC16，为什么 IAP 整包还要做 CRC32？
3. CRC 错误后直接丢帧会不会造成命令丢失？如何保证命令可靠？
4. CRC 能不能防止恶意篡改？

---

## 第 5 轮：ACK、超时重试与命令去重

### 面试官问题

你刚才提到：CRC 错误后丢帧，命令类消息靠 ACK 和重试。

**RK 给 MCU 下发一个关键命令，比如“进入 Bootloader”或“关机”，你会怎么设计 ACK、超时、重试机制？如何避免重复执行同一条命令？**

回答时尽量包含：

1. `seq` 或 `request_id`
2. ACK / ERROR
3. timeout
4. retry
5. 幂等性
6. 去重缓存

### 你的原始回答要点

- ACK 和发送命令采用相同的 `request_id`。
- RK 发送命令后，如果没有收到 ACK，则等待 2 秒后重发。
- 最多重试 3 次。
- MCU 如果重复收到相同 `request_id`，重新回复 ACK。
- MCU 不直接重复进入 Bootloader。

### 点评

这个回答已经抓住了关键点，可以给 80 分。你说出了 `request_id`、超时重发、最多重试、重复请求只回 ACK 不重复执行，这些都是可靠命令设计里的核心。

需要补强的是两个边界：

1. 为什么会重复收到同一条命令：可能是 MCU 已经执行并回复 ACK，但 ACK 在 UART 上丢了，RK 没收到，于是 RK 超时重发。
2. 幂等性：像“查询状态”天然可以重复执行；但“进入 Bootloader”“关机”这类命令有副作用，必须避免重复触发，所以 MCU 要记录最近处理过的 `request_id` 和执行结果。

### 优化后的回答

对于 RK 下发给 MCU 的关键命令，我会把它设计成“请求-响应”模型。RK 发送命令时带一个 `request_id` 或 `seq`，MCU 处理完成后回复 ACK 或 ERROR，并且 ACK/ERROR 里带回相同的 `request_id`。这样 RK 可以知道这条响应对应的是哪一条命令。

RK 侧发送命令后启动超时定时器，比如 2 秒。如果 2 秒内没有收到匹配 `request_id` 的 ACK，就重发同一条命令，最多重试 3 次。如果 3 次都失败，就认为链路或 MCU 状态异常，上报错误给上层服务或 APK。

MCU 侧需要做去重处理。因为可能出现一种情况：MCU 已经收到了命令，也执行成功并回复了 ACK，但 ACK 在串口链路上丢了，RK 没收到，于是 RK 会重发同一个 `request_id`。这时 MCU 如果重复执行“进入 Bootloader”或“关机”，就可能造成异常。所以 MCU 应该保存最近处理过的 `request_id`、命令类型和执行结果。

如果 MCU 再次收到相同 `request_id` 的同一条命令，就不重复执行，只重新回复上一次的 ACK 或 ERROR。这样即使 RK 重试，也不会导致关键命令被重复执行。

这里还要区分命令是否有副作用。像“查询状态”这种命令重复执行问题不大；但“进入 Bootloader”“关机”“擦写 Flash”这类命令不是天然幂等的，就必须通过 `request_id` 去重、状态检查和执行结果缓存来保证重复请求不会重复触发危险动作。

### 更简洁的面试版

> RK 下发关键命令时带 `request_id`，MCU 回复 ACK/ERROR 时带回同一个 `request_id`。RK 侧发出后等待超时，比如 2 秒，没有收到就重发，最多重试 3 次。MCU 侧要缓存最近处理过的 `request_id` 和结果，如果重复收到同一个请求，说明可能是 ACK 丢了，就只重新回复 ACK，不重复执行关机或进入 Bootloader。这样可以解决串口丢包下的可靠性问题，也能避免关键命令重复执行。

### 可能追问

1. MCU 去重缓存需要保存多久？保存多少条？
2. `seq` 和 `request_id` 有什么区别？
3. 如果 RK 重启后 `request_id` 从 0 开始，会不会和旧请求冲突？
4. 对于“进入 Bootloader”这种命令，ACK 应该在执行前回，还是执行后回？
5. 如果 MCU 已经跳进 Bootloader，RK 没收到 ACK，应该怎么办？

---

## 第 6 轮：进入 Bootloader 的 ACK 时机

### 面试官问题

如果 RK 给 MCU 发送 **进入 Bootloader** 命令，MCU 收到后应该：

1. 先回复 ACK，再复位进入 Bootloader？
2. 还是先进入 Bootloader，再让 Bootloader 回复 ACK？

你会怎么设计？为什么？

### 你的原始回答要点

- 应该先回复 ACK。
- 延迟 100ms 后再进入 Bootloader。
- 因为进入 Bootloader 意味着 MCU 要重启，重启后当前 APP 就无法回复 ACK。

### 点评

这个回答方向正确，可以给 85 分。关键判断是对的：**APP 收到命令后应该先回复 ACK，再复位进入 Bootloader**。否则 RK 发出命令后收不到 ACK，会认为命令失败并反复重试。

需要把“延迟 100ms”说得更工程化。固定延迟可以作为兜底，但更好的表达是：

1. APP 先发送 ACK。
2. 确认 UART 数据已经发送完成，比如等待 TX complete 或发送队列清空。
3. 写入升级标志，比如 backup register、RTC backup 或固定 RAM magic。
4. 关闭不必要中断和外设，触发系统复位。
5. Bootloader 启动后检查升级标志，进入 IAP 等待窗口。
6. RK 侧再和 Bootloader 做 `IAP1` 之类的握手，确认确实进入升级模式。

### 优化后的回答

我会设计成 APP 先回复 ACK，再复位进入 Bootloader。

原因是“进入 Bootloader”这个动作本身通常意味着 MCU 要复位，APP 一旦复位就不能再继续回复当前协议里的 ACK。如果 MCU 收到命令后直接复位，RK 侧可能收不到响应，就会认为命令失败，然后超时重试，状态会比较混乱。

更稳妥的流程是：MCU APP 收到 `ENTER_BOOTLOADER` 命令后，先校验命令合法性，然后回复带相同 `request_id` 的 ACK。ACK 发出后，不能马上复位，至少要确认 UART 发送已经完成，比如等待 TX complete 或确认发送队列清空。之后 APP 写入一个进入升级模式的标志，比如 backup register 或 magic value，然后触发系统复位。

MCU 复位后先进入 Bootloader。Bootloader 读取升级标志，如果发现是 RK 请求升级，就停留在 IAP 等待窗口。RK 侧收到 APP 的 ACK 后，也不能直接认为升级链路已经可用，还要继续和 Bootloader 做一次握手，比如发送 `IAP1`，Bootloader 回复 `OK` 或版本信息。这样 RK 才能确认 MCU 已经真正进入 Bootloader。

简单说，APP 层 ACK 代表“我收到了进入 Bootloader 的请求，并准备复位”；Bootloader 握手代表“我已经进入升级模式，可以开始传固件”。这两个确认不要混在一起。

### 更简洁的面试版

> 我会让 APP 先回 ACK，再进入 Bootloader。因为进入 Bootloader 通常要复位，直接复位会导致 APP 没机会回复 ACK，RK 侧可能误判命令失败。更稳妥的做法是 APP 收到命令后先回复带相同 `request_id` 的 ACK，确认 UART 发送完成后写升级标志并复位。Bootloader 启动后读取标志进入 IAP 窗口，RK 再发送 `IAP1` 做二次握手，确认 MCU 已经进入升级模式。

### 可能追问

1. 为什么 ACK 发送完成后还要再做 Bootloader 握手？
2. 如果 APP 回复 ACK 后复位失败怎么办？
3. 升级标志放在 RAM、backup register、Flash 各有什么优缺点？
4. Bootloader 等待升级多久合适？超时后应该跳 APP 还是继续等待？
5. 进入 Bootloader 前为什么要关闭中断？

---

## 第 7 轮：Bootloader 升级标志放在哪里

### 面试官问题

MCU APP 收到进入 Bootloader 命令后，需要写一个“升级标志”，让复位后的 Bootloader 知道这次不是普通启动，而是要进入 IAP。

**这个升级标志可以放在哪里？比如 RAM、backup register、Flash 分别有什么优缺点？你会选哪种？**

### 你的原始回答要点

- RAM 使用快，但是关机掉电会丢失。
- backup register 是 STM32 备用域的小寄存器，专门用于复位时保留信息，使用方便，也不用擦写 Flash。
- Flash 容量大，但是擦写慢，有寿命损耗，而且如果处理不好可能写坏 APP。

### 点评

这个回答可以给 85 分，基本覆盖了工程取舍。需要补充两个细节：

1. 普通 RAM 在某些软件复位后可能还保留，但不可靠。启动代码可能清零 `.bss`，掉电会丢失，不同复位源行为也不完全一样。所以普通 RAM 只适合作为临时 magic，不适合作为非常稳妥的升级标志。
2. backup register 也不是“任何情况下都不丢”。它属于备份域，通常依赖 VBAT 或备用域供电配置。它适合跨软件复位保存少量状态，但如果完全断电且没有备份供电，也可能丢失。

### 优化后的回答

这个升级标志可以放在 RAM、backup register 或 Flash，不同方案取舍不一样。

普通 RAM 的优点是读写最快，也没有擦写寿命问题。但它不够可靠：掉电后一定会丢，软件复位后虽然有些 RAM 内容可能还在，但启动代码可能会清 `.bss`，而且不同复位类型下行为不一定一致。所以如果只是做临时 magic 可以用 RAM，但我不会把它作为最稳妥的升级入口判断依据。

backup register 更适合这个场景。它是 STM32 备份域里的小寄存器，容量不大，但足够放一个 magic value 或升级原因。它的优点是跨软件复位能保留，不需要擦写 Flash，读写也很快，非常适合 APP 收到进入 Bootloader 命令后写一个升级标志，复位后 Bootloader 再读取这个标志。Bootloader 读取到标志后应该及时清掉，避免下次复位又误进入升级模式。

Flash 的优点是掉电后也能保留，容量也更大，可以记录升级状态、固件版本、失败原因等长期信息。但缺点是擦写慢，有擦写寿命限制，而且如果升级标志和 APP 区、参数区规划不好，可能带来额外风险。对于“APP 请求下一次复位进入 Bootloader”这种短生命周期标志，没有必要频繁写 Flash。

所以我会优先选择 backup register 存升级标志。如果项目还要求断电后继续恢复升级状态，比如需要记录“升级中、已下载、待校验”等长期状态，那可以另外设计 Flash 参数区保存升级状态，但不要把短期进入 Bootloader 的标志直接频繁写在 APP 区附近。

### 更简洁的面试版

> RAM 读写最快，但掉电丢失，软件复位后的保留也不够可靠；Flash 掉电不丢，但擦写慢、有寿命限制，还要避免影响 APP 区；backup register 容量小但跨软件复位能保留，不需要擦写 Flash，正适合放一个进入 Bootloader 的 magic。所以我会用 backup register 存短期升级标志，Bootloader 读到后立即清除。如果要做断电保护或升级状态记录，再单独用 Flash 参数区保存长期状态。

### 可能追问

1. Bootloader 读到升级标志后，为什么要立刻清除？
2. 如果进入 Bootloader 后 RK 没有发送固件，Bootloader 应该一直等吗？
3. Flash 参数区怎么设计才能避免写坏 APP？
4. 如何区分普通复位、看门狗复位和升级请求复位？
5. 如果升级过程中断电，下次上电应该怎么处理？

---

## 第 8 轮：Bootloader 等待超时策略

### 面试官问题

Bootloader 读到升级标志后进入 IAP 等待窗口。

**如果 RK 一直没有发送固件，Bootloader 应该一直等吗？如果不一直等，超时后应该跳 APP，还是继续留在 Bootloader？判断依据是什么？**

### 你的原始回答要点

- Bootloader 可以等待 120 秒。
- 这个时间足够 RK 启动并发送固件升级包。
- 如果有有效 APP，超时后进入 APP，防止一直升级失败影响后续使用。
- 如果没有有效 APP，则一直停留在 Bootloader 等待升级。

### 点评

这个回答可以给 90 分，工程策略非常正确。Bootloader 不能无脑一直等，也不能无脑超时跳转，核心判断依据就是：**当前是否存在合法可运行的 APP**。

还可以补充两个面试加分点：

1. 120 秒是一个经验参数，具体要根据 RK3562 启动时间、Android 服务启动时间、串口升级脚本启动时间和固件大小来定。
2. Bootloader 等待期间最好周期性回包或打印状态，让 RK 侧知道 MCU 仍在 IAP 窗口里，而不是设备无响应。

### 优化后的回答

我不会让 Bootloader 无条件一直等待，而是根据 APP 合法性和升级触发原因来决定。

如果 Bootloader 是因为 APP 写了升级标志才进入 IAP，我会设置一个等待窗口，比如 120 秒。这个时间要覆盖 RK3562 启动、Android 用户态服务启动、升级脚本打开 `/dev/ttyS3`、发送 `IAP1` 握手和开始传固件的时间。具体值可以根据实测启动时间调整。

如果 120 秒内 RK 没有发送有效升级请求，Bootloader 要先检查 APP 是否有效。如果 APP 有效，比如栈顶地址和 Reset_Handler 地址合法，必要时固件 CRC 也通过，那么超时后就跳转到 APP。这样可以避免因为一次升级触发失败，设备永远停留在 Bootloader，影响正常使用。

如果没有有效 APP，比如 APP 区为空、栈顶地址不在 SRAM 范围、Reset_Handler 不在 APP Flash 范围，或者整包 CRC 校验失败，那就不能跳 APP，只能继续停留在 Bootloader 等待升级。否则跳到坏 APP 可能直接 HardFault 或反复重启。

所以总结就是：有合法 APP，等待超时后回到 APP，保证设备可用；没有合法 APP，就留在 Bootloader 等待 RK 重新升级，保证设备可恢复。

### 更简洁的面试版

> 我会设置一个 IAP 等待窗口，比如 120 秒，具体根据 RK 启动和升级服务启动时间来定。如果超时后 APP 合法，就跳转 APP，避免设备因为升级触发失败一直停在 Bootloader；如果 APP 不合法，就不能跳，只能继续留在 Bootloader 等待重新升级。判断依据主要是 APP 栈顶地址、Reset_Handler 地址是否合法，必要时再结合固件 CRC 或升级状态标志。

### 可能追问

1. APP 合法性具体检查哪些内容？
2. 为什么只检查栈顶和 Reset_Handler 还不够绝对可靠？
3. Bootloader 跳 APP 前为什么要设置 MSP 和 VTOR？
4. 如果 APP 有效但版本太旧，超时后要不要跳？
5. IAP 等待期间如何避免看门狗复位？

---

## 第 9 轮：APP 合法性检查与 Bootloader 跳转

### 面试官问题

你刚才提到“APP 合法性”。

**Bootloader 跳转 APP 前，为什么要检查栈顶地址和 Reset_Handler 地址？它们分别应该落在哪个地址范围？检查通过后，跳转 APP 前还要做哪些动作？**

### 你的原始回答要点

- 检查这两个地址是为了确定 APP 是合法、有效的。
- 栈顶地址对应 APP 的 RAM 范围。
- Reset_Handler 对应 APP 的 Flash 范围。
- STM32F103RCT6 的 SRAM 范围可按 `0x20000000-0x2000BFFF` 判断。
- APP Flash 范围可按 `0x08004000-0x0803FFFF` 判断。
- 跳转前要关闭 Bootloader 外设和中断。
- 把中断向量表切到 APP。
- 把栈切到 APP。
- 入口函数切到 APP。

### 点评

这个回答可以给 90 分。你把两个核心地址范围说出来了，而且知道跳转前要关中断、切 VTOR、设置 MSP，再跳 Reset_Handler。

可以补强三个细节：

1. Cortex-M 向量表第 0 个 word 是初始 MSP，第 1 个 word 是 Reset_Handler。Bootloader 检查的本质是读取 APP 向量表前 8 个字节。
2. Reset_Handler 地址通常最低 bit 是 Thumb 状态位，Cortex-M 只支持 Thumb 指令，所以函数指针跳转时要注意地址有效性判断一般看去掉 bit0 后是否落在 Flash 范围，或者判断原始地址是否带 Thumb bit。
3. 跳转前除了关中断，还应清除 SysTick、NVIC pending，反初始化 Bootloader 用过的外设，避免 Bootloader 的中断或外设状态影响 APP。

### 优化后的回答

Bootloader 跳 APP 前检查栈顶地址和 Reset_Handler，是为了避免跳到空 Flash 或坏 APP。Cortex-M 的 APP 起始地址处是一张中断向量表，第 0 个 word 是初始 MSP，也就是 APP 启动后要使用的主栈指针；第 1 个 word 是 Reset_Handler，也就是 APP 的入口函数。

如果 APP 区没有有效固件，这两个位置可能是 `0xFFFFFFFF`，或者是乱值。Bootloader 如果不检查就直接跳转，轻则 HardFault，重则反复重启，所以跳转前要先做基本合法性检查。

以 STM32F103RCT6 为例，SRAM 是 48KB，范围大致是：

```text
0x20000000 - 0x2000BFFF
```

所以 APP 向量表第 0 个 word，也就是初始 MSP，应该落在这个 SRAM 范围内。

如果 Bootloader 占用 `0x08000000 - 0x08003FFF`，APP 从 `0x08004000` 开始，Flash 总大小 256KB，那么 APP 的 Reset_Handler 应该落在 APP Flash 范围内：

```text
0x08004000 - 0x0803FFFF
```

实际判断 Reset_Handler 时，还要注意 Cortex-M 的 Thumb 位。函数地址最低 bit 通常为 1，表示 Thumb 状态。做范围判断时可以对地址做掩码，比如 `reset_handler & ~1U` 后再判断是否在 APP Flash 范围内。

检查通过后，跳转 APP 前一般要做这些动作：

1. 关闭全局中断，避免跳转过程中被 Bootloader 的中断打断。
2. 停止 SysTick，清除 SysTick pending。
3. 关闭或反初始化 Bootloader 用过的外设，比如 UART、定时器、DMA 等。
4. 清除 NVIC 中已使能和 pending 的中断。
5. 设置 `SCB->VTOR = APP_BASE`，把中断向量表重定位到 APP。
6. 设置 MSP 为 APP 向量表第 0 个 word。
7. 读取 APP 向量表第 1 个 word，转换成函数指针，跳转到 Reset_Handler。

这样 APP 启动后使用的是自己的栈、自己的中断向量表和自己的入口函数，不会继续沿用 Bootloader 的运行环境。

### 更简洁的面试版

> Cortex-M 向量表第 0 个 word 是初始 MSP，第 1 个 word 是 Reset_Handler。Bootloader 跳 APP 前要检查 MSP 是否落在 SRAM 范围，比如 STM32F103RCT6 是 `0x20000000-0x2000BFFF`；Reset_Handler 是否落在 APP Flash 范围，比如 APP 从 `0x08004000` 开始，则应在 `0x08004000-0x0803FFFF`。检查通过后，要关闭中断和 Bootloader 外设，停 SysTick，清 NVIC pending，设置 `VTOR` 到 APP 起始地址，设置 MSP 为 APP 栈顶，最后跳转到 APP 的 Reset_Handler。

### 可能追问

1. 为什么只检查 MSP 和 Reset_Handler 不能保证 APP 一定正确？
2. 为什么要设置 `SCB->VTOR`？
3. 如果不关闭 SysTick 或 NVIC pending，会发生什么？
4. Reset_Handler 的最低 bit 为什么可能是 1？
5. APP 从 `0x08004000` 启动，链接脚本或 Keil 工程需要改哪些地方？

---

## 第 10 轮：为什么要重定位 VTOR

### 面试官问题

**如果 Bootloader 跳 APP 前没有重定位 `SCB->VTOR`，会发生什么？为什么 APP 从 `0x08004000` 启动时必须修改向量表地址？**

### 你的原始回答要点

- APP 中使用中断时可能无法正常使用中断。
- 程序可能崩溃。
- 原因是系统之前使用的是 Bootloader 的中断向量表。

### 点评

这个回答可以给 75 分，方向是对的，但需要把问题说得更具体。

不是简单地“中断无法使用”，而是中断发生时，CPU 会根据 `SCB->VTOR` 指向的向量表去取对应中断服务函数地址。如果 `VTOR` 还指向 Bootloader 的向量表，那么 APP 运行过程中触发 UART、SysTick、DMA、EXTI 等中断时，CPU 会去 Bootloader 的向量表里找 handler。

结果可能有几种：

1. 跳到 Bootloader 的中断处理函数，逻辑完全不对。
2. 跳到默认 `Default_Handler`，程序卡死。
3. 跳到无效地址，引发 HardFault。
4. SysTick 还沿用 Bootloader 配置，影响 APP 的时基或 RTOS 调度。

### 优化后的回答

`SCB->VTOR` 是 Cortex-M 用来指定中断向量表基地址的寄存器。CPU 响应中断时，会从 `VTOR + 中断号偏移` 的位置读取中断处理函数地址，然后跳过去执行。

如果系统只有一个 APP，向量表默认在 `0x08000000`，问题不大。但有 Bootloader 后，Bootloader 通常放在 `0x08000000`，APP 被偏移到 `0x08004000`。这时 APP 自己的向量表也在 `0x08004000` 开始。

如果 Bootloader 跳 APP 前没有设置：

```c
SCB->VTOR = APP_BASE;
```

那么 `VTOR` 仍然指向 Bootloader 的向量表。APP 跑起来后，一旦发生中断，比如 UART 接收中断、定时器中断、SysTick 中断，CPU 还是会去 Bootloader 的向量表里取 handler，而不是 APP 的 handler。这样就可能跳到错误函数、默认死循环或者无效地址，导致功能异常或 HardFault。

所以 APP 从 `0x08004000` 启动时，除了链接地址要改到 `0x08004000`，Bootloader 跳转前也必须把 `VTOR` 重定位到 APP 起始地址。这样 APP 的中断才能按照自己的向量表正常分发。

### 更简洁的面试版

> `VTOR` 决定 CPU 响应中断时去哪里找向量表。Bootloader 在 `0x08000000`，APP 在 `0x08004000`，如果不把 `VTOR` 改成 APP 起始地址，APP 运行时触发中断还会去 Bootloader 的向量表找 handler，可能跳到 Bootloader 的默认中断、错误函数或无效地址，导致卡死或 HardFault。所以跳 APP 前必须设置 `SCB->VTOR = APP_BASE`。

### 可能追问

1. APP 工程里除了 Bootloader 设置 VTOR，自己还要不要设置 `VECT_TAB_OFFSET`？
2. 如果 APP 一启动就重新设置 VTOR，还需要 Bootloader 设置吗？
3. `VTOR` 地址对齐有什么要求？
4. 为什么 Cortex-M0 有些芯片没有 VTOR，怎么办？
5. SysTick 没关就跳 APP 会有什么现象？

---

## 第 11 轮：APP 偏移到 0x08004000 后工程要改什么

### 面试官问题

APP 从 `0x08004000` 启动后，除了 Bootloader 跳转前设置 `VTOR`，**APP 工程本身还需要改哪些地方？比如 Keil/链接脚本、Flash 起始地址、向量表偏移，这些分别是干什么的？**

### 你的原始回答要点

- 你没有使用 Keil，而是用 J-Link 下载，一般使用 hex 文件。
- 一般有两种烧录方案：
  - 分别烧录两个 hex，一个 Bootloader，一个 APP。
  - 设置好 `0x08000000` 地址后烧录一个完整混合 bin。
- Flash 起始地址就是 APP 的起始地址。
- 向量表偏移是告诉 CPU 从哪里找新的中断地址。

### 点评

这个回答有一些正确点，但概念需要区分清楚。Bootloader + APP 偏移启动时，至少涉及三层东西：

1. **APP 的链接地址**：APP 编译链接时必须知道自己从 `0x08004000` 开始运行，否则全局变量初始化、向量表、函数绝对地址等都可能不对。
2. **向量表地址**：运行时要让 CPU 知道中断向量表在 `0x08004000`，这可以由 Bootloader 设置 `SCB->VTOR = APP_BASE`，APP 自己启动后也可以再次设置。
3. **烧录方式**：HEX 文件通常自带地址信息，BIN 文件通常只是裸数据，需要烧录工具指定写入地址。

需要修正的是：如果 APP 起始地址是 `0x08004000`，那么 APP 不能按 `0x08000000` 链接。`0x08000000` 是 Bootloader 的起始地址。完整混合固件可以从 `0x08000000` 烧录，但里面 Bootloader 和 APP 各自仍然要按自己的链接地址编译。

### 优化后的回答

APP 从 `0x08004000` 启动后，工程上主要要改三类东西：链接地址、向量表设置和烧录地址。

第一是 APP 的链接地址要改。因为 Bootloader 占用了 `0x08000000 - 0x08003FFF`，APP 放在 `0x08004000`，所以 APP 工程的 Flash 起始地址应该改成 `0x08004000`，Flash 长度也要相应减去 Bootloader 占用的 16KB。这样编译出来的 APP 向量表、代码地址、只读数据地址才都是按 APP 实际运行地址生成的。

第二是向量表偏移要处理。APP 的向量表在 `0x08004000`，CPU 响应中断时必须从 APP 的向量表取 handler。这个可以由 Bootloader 在跳转前设置：

```c
SCB->VTOR = 0x08004000;
```

同时 APP 自己启动后也可以再次设置 VTOR，或者在系统初始化里配置向量表偏移，比如某些 STM32 工程里的 `VECT_TAB_OFFSET`。这样即使 APP 不是从复位默认地址启动，中断也能正常进 APP 的 handler。

第三是烧录产物要区分 HEX 和 BIN。HEX 文件一般带地址信息，所以可以分别烧录 Bootloader hex 和 APP hex，只要它们各自链接地址正确，烧录工具会按地址写到对应 Flash 区域。BIN 文件一般不带地址信息，只是一段裸数据，所以烧录 APP bin 时必须指定写入地址 `0x08004000`；如果烧录完整混合 bin，则这个混合 bin 要从 `0x08000000` 开始，里面前面是 Bootloader，后面在偏移 `0x4000` 的位置放 APP。

所以我会这样总结：Bootloader 链接到 `0x08000000`，APP 链接到 `0x08004000`；HEX 可以分开烧录，BIN 要指定地址；运行时 Bootloader 或 APP 要设置 `VTOR` 到 APP 起始地址，保证中断向量正确。

### 更简洁的面试版

> APP 偏移到 `0x08004000` 后，首先 APP 工程的 Flash 起始地址要改成 `0x08004000`，长度减去 Bootloader 占用空间；其次向量表也要对应偏移，Bootloader 跳转前设置 `SCB->VTOR = 0x08004000`，APP 初始化里也可以再设置一次；最后烧录时要注意 HEX 和 BIN 的区别，HEX 自带地址信息，BIN 不带地址信息，烧录 APP bin 时要指定写入 `0x08004000`，混合 bin 才是从 `0x08000000` 开始烧。

### 可能追问

1. 如果 APP 没有按 `0x08004000` 链接，但被烧到 `0x08004000`，会发生什么？
2. HEX 和 BIN 的本质区别是什么？
3. Bootloader 和 APP 合并成一个固件时，如何处理空洞区域？
4. APP 的 Flash 长度为什么也要改？
5. IAP 时 RK 发送的是 bin 还是 hex 更合适？为什么？

---

## 第 12 轮：HEX 和 BIN 的区别

### 面试官问题

**HEX 和 BIN 有什么区别？为什么 IAP 升级时通常更适合让 RK 发送 bin，而不是 hex？**

### 你的原始回答要点

- HEX 是文本格式。
- HEX 里面带地址、长度、类型、校验。
- BIN 是纯粹的固件包。
- BIN 烧录更加方便。
- IAP 时发送 BIN 不需要 MCU 端二次解析 HEX。

### 点评

这个回答可以给 80 分，关键点基本正确。需要补强两个边界：

1. BIN 本身不带地址信息，所以 IAP 时必须通过额外 header 告诉 Bootloader：写到哪里、大小是多少、整包 CRC 是多少。
2. HEX 每行有自己的校验，但那主要是记录行级别的文本校验，不等价于整个固件的完整性校验。IAP 仍然建议做整包 CRC32。

### 优化后的回答

HEX 和 BIN 的最大区别是：HEX 是带格式的文本文件，BIN 是裸二进制数据。

HEX 一般是 Intel HEX 格式，每一行都是文本记录，里面包含长度、地址、记录类型、数据和行校验。它的优点是自带地址信息，适合用烧录器或调试器下载，因为工具可以根据 HEX 里的地址把不同段写到对应 Flash 位置。缺点是体积更大，传输时需要解析 ASCII 文本，还要处理扩展地址记录、数据记录、结束记录等格式。

BIN 是纯二进制固件内容，没有文本格式，也通常不自带写入地址。它的优点是体积小、传输和写入简单，Bootloader 收到后可以按固定大小分包，直接写入 Flash。缺点是地址、大小、版本、CRC 等元信息需要协议额外提供。

在 RK 通过 UART 给 MCU 做 IAP 时，我更倾向于让 RK 发送 BIN。因为 MCU Bootloader 资源有限，如果让 MCU 解析 HEX，就要解析 ASCII、处理每行地址和类型，复杂度更高，而且传输体积也更大。发送 BIN 时，RK 侧可以先解析或生成好固件包，Bootloader 只需要根据升级 header 里的 `base、size、crc32` 接收二进制数据、写 Flash、最后做整包 CRC 校验。

所以 IAP 里比较合理的格式是：

```text
IAP header: base + size + crc32 + version/reserved
firmware:   raw bin data
```

这样 MCU 端逻辑简单，传输效率也更高。

### 更简洁的面试版

> HEX 是文本格式，里面每行带地址、长度、记录类型、数据和校验，适合烧录工具使用；BIN 是裸二进制数据，体积小、写入简单，但本身不带地址和大小。IAP 时 MCU Bootloader 资源有限，不适合在 MCU 上解析 HEX，所以更适合让 RK 发送 BIN，同时在升级 header 里带 `base、size、crc32`，MCU 只负责按地址写 Flash 并做整包 CRC 校验。

### 可能追问

1. BIN 不带地址，Bootloader 怎么知道写到哪里？
2. HEX 每行有校验，为什么还要做整包 CRC？
3. 如果固件 bin 中间有空洞怎么办？
4. RK 侧如何从 ELF/HEX 生成 APP bin？
5. IAP 时为什么不能让 APP 首页一开始就被擦掉或写入？

---

## 第 13 轮：IAP 防变砖与 APP 首页延迟写入

### 面试官问题

**IAP 升级时，为什么不能一开始就擦掉或写入 APP 的第一页？你会怎么设计写 Flash 的顺序，来降低升级失败后变砖的风险？**

### 你的原始回答要点

- APP 第一页开头包含栈顶地址、Reset_Handler 和各种中断入口。
- 如果升级失败时 APP 首页已经被破坏，板子可能无法再判断或启动 APP。
- 先接收 header，检查 `size、base、crc、版本、目标硬件` 等字段。
- 先擦除后面的页。
- 将第一页暂存在 RAM 中。
- 整包校验通过之后，最后再擦除并写入第一页。
- 最后检查中断向量表合法性。

### 点评

这个回答可以给 90 分，已经说到了防变砖的关键：**APP 首页是 APP 合法性的入口，不能一开始就破坏**。

需要修正和补强两个点：

1. “先擦除后面的页，然后将第一页存到 RAM”建议调整为：接收固件时先把新固件第一页暂存在 RAM，不立即写入 Flash；先擦写 APP 首页之后的页，等整包接收和 CRC 校验通过后，最后再写 APP 首页。
2. 如果 MCU RAM 不够暂存整个首页或固件页，需要按 Flash 页大小设计缓存，至少保证 APP 向量表所在第一页最后写入。

### 优化后的回答

IAP 升级时不能一开始就擦掉或写入 APP 的第一页，因为 APP 第一页最前面就是中断向量表。向量表第 0 个 word 是初始 MSP，第 1 个 word 是 Reset_Handler，后面还有各种中断入口。Bootloader 判断 APP 是否有效时，最基本也是先检查这些位置。

如果升级刚开始就把 APP 首页擦掉，而后续传输过程中断电、串口中断、CRC 校验失败或 RK 侧异常退出，那么旧 APP 的入口也被破坏了。下次上电时 Bootloader 会发现 APP 无效，只能停在 Bootloader 等待升级。虽然 Bootloader 还在，不一定彻底变砖，但设备正常功能已经不可用了，所以要尽量避免过早破坏 APP 首页。

我的设计是先接收升级 header，检查 `base、size、crc32、版本、目标硬件` 等字段是否合法，确认不会越界写到 Bootloader 区或非法 Flash 区。然后开始接收固件数据。对于新固件的第一页，先暂存在 RAM buffer 中，不立即写入 Flash；后续页可以按页擦除并写入 APP 区的后续 Flash。

当所有固件数据接收完成后，Bootloader 对写入区域和暂存的第一页一起计算整包 CRC32。如果 CRC 通过，再最后擦除并写入 APP 第一页。这样只有在新固件完整且校验通过时，APP 的向量表才会被替换成新版本。

写完第一页后，再做 APP 合法性检查，比如检查初始 MSP 是否在 SRAM 范围、Reset_Handler 是否在 APP Flash 范围，必要时再检查版本和整包 CRC。通过后再复位或跳转 APP。

这种方式的核心是：让 APP 首页成为最后提交点。升级失败时，尽量保留旧 APP 的入口；升级成功时，最后写首页，相当于正式提交新 APP。

### 更简洁的面试版

> APP 第一页开头是向量表，包含初始 MSP、Reset_Handler 和中断入口。Bootloader 判断 APP 是否有效也依赖这些内容。如果一开始就擦 APP 首页，升级中途断电或 CRC 失败，旧 APP 入口就没了，设备只能停在 Bootloader。我的做法是先校验 header，确认 `base、size、crc32、版本、目标硬件` 合法；接收时把新固件第一页先放 RAM，不立即写 Flash，先擦写后续页；等整包接收完成并 CRC32 通过后，最后再擦除并写入第一页。这样 APP 首页相当于最后提交点，可以降低升级失败后不可用的风险。

### 可能追问

1. 这种方式能不能保证断电后一定不变砖？
2. 如果升级失败时后续页已经被新固件覆盖，旧 APP 还能不能正常跑？
3. 真正更可靠的 A/B 分区回滚怎么做？
4. 如果 RAM 不够缓存 APP 第一页怎么办？
5. 升级状态是否需要写入 Flash 参数区？

---

## 第 14 轮：A/B 分区回滚

### 面试官问题

你刚才说“APP 首页最后写”可以降低变砖风险。

**它能不能保证升级中途断电后旧 APP 一定还能运行？如果不能，真正更可靠的 A/B 分区回滚应该怎么设计？**

### 你的原始回答要点

- 不能保证旧 APP 一定还能运行。
- 真正的 A/B 回滚应该有两份 APP。
- 设立两份 metadata，分为 A/B。
- 升级时升级其中一个非当前分区。
- 验证通过后再切换到 pending。
- 如果启动失败，则通过看门狗复位回到 A 分区。

### 点评

这个回答可以给 80 分，核心方向正确：**单分区延迟写首页只能降低风险，不能保证旧 APP 一定可运行；真正可靠要靠 A/B 分区和回滚状态机**。

需要补强三个点：

1. metadata 不一定要两份 A/B，而是建议做两份冗余 metadata，用序号和 CRC 判断哪份最新有效。metadata 里面记录当前活动槽、待启动槽、版本、CRC、启动次数、确认状态等。
2. `pending` 不是“验证通过后再切换”的最终状态。更准确是：新固件写入并离线校验通过后，标记为 `pending`，Bootloader 下次尝试启动它；APP 启动后完成自检，再把自己标记为 `confirmed`。
3. 回滚不只依赖看门狗。看门狗复位是一种失败信号，但 Bootloader 还要结合 metadata 里的启动次数、确认标志和复位原因来判断是否回滚。

### 优化后的回答

APP 首页最后写只能降低单分区升级失败的风险，不能保证升级中途断电后旧 APP 一定还能运行。因为即使旧 APP 的第一页还在，后面的代码页可能已经被新固件擦写了。Bootloader 如果只看 MSP 和 Reset_Handler，可能误判 APP 有效，实际跳进去仍然会 HardFault。

更可靠的做法是 A/B 分区。Flash 里保留两个完整 APP 槽位，比如 slot A 和 slot B。当前系统从 A 运行时，升级就写入 B；当前从 B 运行时，升级就写入 A。这样升级过程不会破坏当前正在运行的旧版本。

同时要设计 metadata 区，最好做两份冗余 metadata，避免 metadata 自己写坏。metadata 里可以记录：

```text
active_slot       当前确认可用的分区
pending_slot      下一次准备试启动的分区
slot_version      A/B 版本号
slot_crc          A/B 固件 CRC
boot_count        新分区已尝试启动次数
state             empty / valid / pending / confirmed / invalid
metadata_seq      metadata 版本序号
metadata_crc      metadata 自身校验
```

升级流程可以这样设计：

1. 当前从 A 运行，RK 下载新固件到 B。
2. Bootloader 或升级程序校验 B 分区 CRC、版本、目标硬件等信息。
3. 校验通过后，把 B 标记为 `pending`，表示下次尝试启动 B。
4. Bootloader 下次启动时看到 B 是 `pending`，就跳转 B，并记录启动尝试次数。
5. B 分区 APP 启动后，如果完成自检、能正常喂狗、能和 RK 通信，就主动把自己标记为 `confirmed`。
6. 如果 B 启动失败、看门狗复位、超过最大启动次数仍未 confirmed，Bootloader 就把 B 标记为 invalid，回滚到原来的 A。

这样 A/B 的关键不是“写完就切换”，而是“先试启动，成功后确认，失败后回滚”。它比单分区延迟写首页更可靠，因为升级过程中不会破坏当前可用 APP。

### 更简洁的面试版

> APP 首页最后写只能降低风险，不能保证旧 APP 一定还能运行，因为后续页可能已经被新固件覆盖。更可靠的是 A/B 分区：当前从 A 运行时，新固件写入 B，校验 B 的 CRC、版本和硬件型号后，把 B 标记为 pending。Bootloader 下次尝试启动 B，B 启动成功并完成自检后主动把自己标记为 confirmed。如果 B 启动失败、看门狗复位或多次未确认，Bootloader 就把 B 标记为 invalid，回滚到 A。metadata 要做冗余和 CRC，防止状态区本身写坏。

### 可能追问

1. A/B 分区的缺点是什么？
2. metadata 写坏了怎么办？
3. APP 如何告诉 Bootloader 自己启动成功？
4. 看门狗复位后 Bootloader 怎么判断是新 APP 启动失败？
5. STM32F103 Flash 空间够不够做 A/B？

---

## 第 15 轮：STM32F103RCT6 上做 A/B 的限制

### 面试官问题

A/B 方案听起来更可靠，但它不是免费的。

**你觉得在 STM32F103RCT6 上做 A/B 分区有什么缺点或限制？如果 Flash 空间不够，你会怎么取舍？**

### 你的原始回答要点

- STM32F103RCT6 的 Flash 和 RAM 大约为 256KB 和 20KB。
- 做 A/B 分区意味着需要两份 APP，以及相应的 Bootloader 和 metadata。
- Flash 空间不足时，应该先舍弃 A/B 分区。
- 但会保留 Bootloader。

### 点评

这个回答可以给 75 分，取舍方向正确：资源不足时优先保留 Bootloader 和基础 IAP，而不是强行做 A/B。

需要纠正一个硬件参数：STM32F103RCT6 通常是 **256KB Flash、48KB SRAM**。20KB SRAM 更像 STM32F103C8T6 这一类小容量型号。面试里如果芯片型号写的是 RCT6，建议说 48KB SRAM，避免被面试官抓住。

另外要说清楚 A/B 的成本：

1. 需要两份完整 APP 空间。
2. 需要 Bootloader 空间。
3. 需要 metadata/参数区。
4. APP 单槽空间会变小。
5. Flash 擦写、版本管理和回滚状态机会更复杂。

### 优化后的回答

STM32F103RCT6 的资源比较有限，通常是 256KB Flash、48KB SRAM。做 A/B 分区最大的限制是 Flash 空间，因为 A/B 需要保留两份完整 APP，再加上 Bootloader 和 metadata 参数区。

假设 Bootloader 占 16KB，metadata 和参数区再占几 KB，剩下的 Flash 要分成 A/B 两个槽位。这样每个 APP 槽位可能只有一百 KB 左右。如果 APP 本身体积比较大，比如包含 FreeRTOS、通信协议、电源状态机、Bootloader 通信、日志和传感器逻辑，A/B 可能很快就不够用。

所以在 STM32F103RCT6 这种 Flash 不大的 MCU 上，我会先看实际 APP 大小。如果 APP 很小，A/B 可以考虑；但如果空间紧张，我会优先舍弃 A/B，保留一个可靠的 Bootloader 和单 APP 分区 IAP。单分区方案里再通过 header 校验、CRC32、APP 首页最后写、升级状态标志和超时回退来降低失败风险。

如果项目确实要求强断电回滚和高可靠 OTA，那就应该考虑换更大 Flash 的 MCU，或者增加外部 Flash 存放备用固件，再由 Bootloader 从外部 Flash 拷贝到内部 APP 区。不能在空间明显不够的情况下硬做 A/B，否则会挤压 APP 空间，反而增加维护风险。

### 更简洁的面试版

> STM32F103RCT6 通常是 256KB Flash、48KB SRAM，A/B 的主要问题是 Flash 空间。它需要 Bootloader、metadata 和两份 APP，每个 APP 槽位会明显变小。如果 APP 体积较大，我会优先保留 Bootloader 和单分区 IAP，不强行做 A/B；单分区里用 header 校验、CRC32、APP 首页最后写、升级状态标志来降低风险。如果业务强要求断电回滚，就考虑更大 Flash 的 MCU 或外部 Flash 存备用固件。

### 可能追问

1. 如何估算 Bootloader、metadata、APP A、APP B 的 Flash 分区大小？
2. 如果不用 A/B，单分区 IAP 最少要做哪些保护？
3. 外部 Flash 存备用固件时，Bootloader 怎么校验和拷贝？
4. metadata 参数区应该放在 Flash 哪个位置？
5. Bootloader 自身能不能升级？如果能，风险怎么控制？

---

## 第 16 轮：单分区 IAP 的最小保护措施

### 面试官问题

**如果不用 A/B，只做单分区 IAP，你认为最少要做哪些保护措施，才能让它在工程上相对可靠？**

### 你的原始回答要点

- Bootloader 要有单独分区，使其不会被常规 APP 升级擦除。
- 不能只通过 APP 首页判断有效。
- 还要通过 metadata 判断升级是否成功。
- 要完成完整的镜像验证。

### 点评

这个回答可以给 80 分，抓住了三个核心：Bootloader 独立、metadata 状态、整包镜像校验。

需要补全成一套完整保护清单。单分区 IAP 虽然没有 A/B 那么可靠，但至少要保证：

1. Bootloader 不会被 APP 升级覆盖。
2. 不会写越界到 Bootloader 或参数区。
3. 传输错误能被发现。
4. 未完成升级不会被误判为可启动 APP。
5. 升级失败后设备还能停在 Bootloader 等待再次升级。

### 优化后的回答

如果不用 A/B，只做单分区 IAP，我认为最少要做这些保护措施。

第一，Bootloader 必须是独立分区，并且常规 APP 升级不能擦写 Bootloader 区。比如 Bootloader 固定在 `0x08000000 - 0x08003FFF`，APP 从 `0x08004000` 开始。Bootloader 在擦写前必须检查 `base` 和 `size`，确保写入范围只落在 APP 区，不能越界覆盖 Bootloader 或 metadata 参数区。

第二，升级开始前要校验 header。header 至少包含 `base、size、crc32、版本、目标硬件、reserved` 等字段。Bootloader 要检查 base 是否等于 APP 起始地址，size 是否小于 APP 分区大小，目标硬件是否匹配，版本策略是否允许，reserved 是否为预期值。

第三，传输过程中每一帧要有 CRC16 或类似校验，防止单帧数据错误被写入；整包接收完成后，还要对整个 APP 镜像做 CRC32，确认完整固件没有损坏。不能只靠单帧 CRC，因为单帧都对，不代表整包顺序、长度和最终镜像一定正确。

第四，要有 metadata 或升级状态标志。比如状态可以包括 `idle、updating、verified、valid、failed`。开始升级时标记 `updating`，整包 CRC 通过后再标记 `verified/valid`。Bootloader 启动时不能只看 APP 首页，还要结合 metadata 和整包 CRC 判断 APP 是否可启动。

第五，APP 首页最后写。新固件第一页先暂存在 RAM，不立即写入 Flash；先写后续页，等整包 CRC32 通过后再写 APP 首页。这样可以避免一开始就破坏 MSP、Reset_Handler 和中断向量表。

第六，失败处理要明确。如果传输超时、CRC 失败、断电后 metadata 显示还在 `updating`，Bootloader 就不要跳 APP，而是停留在 IAP 等待窗口，等待 RK 重新升级。如果 APP 仍被标记为 valid 且 CRC 通过，可以按策略跳 APP；否则不能贸然跳转。

所以单分区 IAP 的底线是：Bootloader 永远保留，写入范围受控，传输和整包都有校验，升级状态可追踪，APP 首页最后提交，失败后能重新进入升级。

### 更简洁的面试版

> 单分区 IAP 最少要保证 Bootloader 独立且不会被擦，写 Flash 前检查 `base/size` 防止越界；升级 header 里带 `base、size、crc32、版本、目标硬件`；单帧用 CRC16，整包用 CRC32；metadata 记录 `updating/valid/failed` 等状态，不能只靠 APP 首页判断；APP 首页最后写，避免一开始破坏向量表；如果升级超时、CRC 失败或状态还是 updating，Bootloader 就留在 IAP 等待重新升级。

### 可能追问

1. metadata 状态机具体怎么设计？
2. metadata 写入过程中断电怎么办？
3. 为什么单帧 CRC 通过还要整包 CRC？
4. 如何防止 `base/size` 被错误设置导致擦 Bootloader？
5. Bootloader 自身是否需要写保护？

---

## 第 17 轮：metadata 状态机与断电保护

### 面试官问题

**metadata 状态机你会怎么设计？比如 `idle / updating / valid / failed` 分别表示什么？如果写 metadata 的过程中断电，怎么避免状态区自己损坏导致误判？**

### 你的原始回答要点

- 升级时将 `status` 修改为 `updating`。
- 升级完成后修改为 `pending-confirmed`。
- 升级完可以正常启动运行后修改为 `confirmed`。

### 点评

这个回答可以给 65 分。你说出了主线：升级开始是 `updating`，新固件启动成功后是 `confirmed`。但状态名还不够清晰，`pending-confirmed` 建议拆成两个状态：

- `pending`：新固件已经写入并校验通过，下一次准备试启动。
- `confirmed`：新固件已经启动成功，并完成自检或和 RK 通信确认。

另外，问题里问到了“metadata 写入过程中断电怎么办”，你还没有回答。这个点需要用冗余 metadata、序号和 CRC 来解决。

### 优化后的回答

metadata 可以设计成一个简单状态机。比如：

```text
idle        当前没有升级任务
updating    正在接收或写入新固件
pending     新固件已经写入并通过 CRC，准备下次试启动
confirmed   APP 已经启动成功并完成自检，确认可用
failed      升级失败或启动失败
```

单分区 IAP 的流程可以是：

1. RK 发起升级，Bootloader 校验 header 通过后，把 metadata 状态写成 `updating`。
2. Bootloader 接收固件并写入 APP 区，但 APP 首页延迟写入。
3. 整包 CRC32 校验通过后，写 APP 首页，把状态改成 `pending`。
4. Bootloader 下次启动时看到 `pending`，会检查 APP 合法性和 CRC，然后尝试跳转 APP。
5. APP 启动后，如果关键任务运行正常、能喂狗、能和 RK 通信，就调用确认接口，把 metadata 改成 `confirmed`。
6. 如果升级 CRC 失败、超时，或者 pending APP 启动失败，则把状态改成 `failed`，Bootloader 留在 IAP 等待重新升级。

为了避免 metadata 自己写坏，不能只保存一份裸状态。更稳妥的是保存两份 metadata，每份包含：

```text
magic
version
seq
status
app_base
app_size
app_crc32
boot_count
metadata_crc
```

Bootloader 启动时读取两份 metadata，先检查 `magic` 和 `metadata_crc`，过滤掉损坏副本；如果两份都有效，就选择 `seq` 更新的那一份。写 metadata 时使用递增 `seq`，先写新副本内容，最后写 CRC 或 commit 标志。这样即使写 metadata 过程中断电，最多损坏正在写的那一份，另一份旧 metadata 仍然可用，不至于误判状态。

### 更简洁的面试版

> 我会把状态拆成 `idle、updating、pending、confirmed、failed`。开始升级写 `updating`，整包写入并 CRC 通过后写 `pending`，Bootloader 下次试启动 pending APP；APP 启动后完成自检和通信确认，再写 `confirmed`。如果 CRC 失败、超时或 pending APP 启动失败，就写 `failed` 并留在 Bootloader。metadata 自己要做双副本，每份带 `magic、seq、status、app_size、app_crc、metadata_crc`，启动时选择 CRC 有效且 seq 最新的一份，避免写 metadata 中途断电导致状态区损坏。

### 可能追问

1. 单分区 IAP 中，`pending` 状态下如果 APP 启动失败，旧 APP 还能回滚吗？
2. APP 如何确认自己启动成功，什么时候写 `confirmed` 合适？
3. metadata 双副本放在同一页还是不同页？
4. Flash 只能从 1 写成 0，metadata 频繁更新怎么处理？
5. `confirmed` 状态是否还需要每次启动都校验 APP CRC？

---

## 第 18 轮：APP 何时从 pending 确认为 confirmed

### 面试官问题

**APP 启动后，什么时候才适合把状态从 `pending` 改成 `confirmed`？是刚进 `main()` 就确认，还是等一段时间？你会用哪些条件判断 APP 真正启动成功？**

### 你的原始回答要点

- 应该等一段时间。
- 各个任务都可以正常运行。
- 中断、看门狗、各个外设正常。
- 相应通信链路正常。

### 点评

这个回答可以给 80 分，方向正确：不能刚进 `main()` 就 confirmed。因为进入 `main()` 只说明 Reset_Handler 跳转成功，不代表系统真的能正常工作。

需要补强的是“正常运行”要落成可检测条件。比如：

1. FreeRTOS 调度启动，关键任务都有心跳。
2. IWDG 喂狗机制正常，不是某个任务卡死后仍然单点喂狗。
3. 关键外设初始化成功，如 GPIO、ADC、UART、CAN。
4. RK-MCU UART 链路完成 HELLO/状态上报/ACK。
5. 运行一段稳定时间，比如 10 秒或 30 秒，没有 HardFault、看门狗复位、关键错误码。

### 优化后的回答

我不会在 APP 刚进 `main()` 时就把状态改成 `confirmed`。因为刚进 `main()` 只能说明 Bootloader 跳转、MSP 和 Reset_Handler 基本没问题，但不代表 FreeRTOS 调度、外设初始化、通信链路和业务逻辑都正常。

更稳妥的做法是 APP 处于 `pending` 状态启动后，先运行一段观察窗口，比如 10 秒或 30 秒。期间检查几个条件：

1. 系统没有 HardFault 或异常复位。
2. FreeRTOS 调度正常，关键任务都能周期性更新心跳。
3. 看门狗喂狗策略正常，最好是所有关键任务都报活后再统一喂狗。
4. 关键外设初始化成功，比如按键 GPIO、ADC 电量采样、UART、CAN 等。
5. RK-MCU UART 链路能正常通信，比如 APP 能向 RK 上报 HELLO 或状态帧，并收到 RK 的 ACK 或命令。
6. 如果项目要求，还可以检查电源状态机、充电检测、低电保护等关键业务是否进入正常状态。

这些条件满足后，APP 再把 metadata 从 `pending` 改成 `confirmed`。这样 confirmed 的含义才是“新 APP 不只是能跳进去，而是已经具备基本运行能力”。如果观察窗口内发生看门狗复位、关键任务无心跳、通信链路建立失败，就不要 confirmed，Bootloader 下次启动时可以把这次启动视为失败。

### 更简洁的面试版

> 不应该刚进 `main()` 就 confirmed。进入 `main()` 只说明跳转成功，不代表系统可用。我会让 pending APP 运行一个观察窗口，比如 10 到 30 秒，确认 FreeRTOS 关键任务有心跳、看门狗正常、GPIO/ADC/UART/CAN 等关键外设初始化成功、RK-MCU 通信能完成 HELLO/ACK 或状态上报，没有 HardFault 或异常复位。满足这些条件后，APP 才把 metadata 改成 `confirmed`。

### 可能追问

1. 如果 RK 没开机，UART 链路无法确认，APP 是否永远不能 confirmed？
2. 所有任务都报活后再喂狗，怎么设计？
3. confirmed 写 metadata 时断电怎么办？
4. 如果 pending APP 能启动但某个非关键外设失败，要不要 confirmed？
5. Bootloader 怎么记录 pending APP 的启动失败次数？

---

## 第 19 轮：IWDG、WWDG 与任务级看门狗

### 面试官问题

你刚才提到看门狗。

**IWDG 应该怎么喂？为什么不建议在一个定时器中断里无条件喂狗？在 FreeRTOS 项目里怎么设计任务级看门狗？**

### 你的原始回答要点

- IWDG 是独立看门狗，精度较低，一般用于最终兜底。
- 程序跑飞时可能存在看门狗无法正确触发的问题。
- 可以采用精度较高的窗口看门狗。
- 窗口看门狗要求在特定时间窗口内喂狗。

### 点评

这个回答可以给 55 分。你说到了 IWDG 和 WWDG 的区别方向，但没有回答最关键的问题：**为什么不能在定时器中断里无条件喂狗**，以及 FreeRTOS 下任务级看门狗怎么设计。

需要明确：

1. IWDG 用独立低速时钟，适合做系统兜底复位，不依赖主时钟。
2. WWDG 有窗口限制，喂太早或太晚都可能复位，适合检测时序异常，但配置和使用更严格。
3. 定时器中断无条件喂狗的问题是：即使业务任务死锁、通信任务卡死、状态机不跑了，只要中断还在，看门狗就一直被喂，失去发现系统异常的意义。
4. FreeRTOS 项目里应该做任务级 heartbeat，由监控任务统一判断是否喂狗。

### 优化后的回答

IWDG 是独立看门狗，通常由独立低速时钟驱动，不依赖系统主时钟，所以适合作为系统最后兜底。如果程序跑飞、任务死锁或者调度异常，IWDG 超时后可以复位 MCU。它的缺点是时钟精度相对低，超时时间不适合做得特别精确。

WWDG 是窗口看门狗，它要求在指定时间窗口内喂狗，喂太早或太晚都不行，所以它可以检测一些时序异常。但 WWDG 依赖系统时钟，配置也更严格。工程里常见做法是 IWDG 做最终兜底，必要时再结合 WWDG 做更严格的时序检测。

不建议在一个定时器中断里无条件喂狗。因为这样只能证明“定时器中断还在跑”，不能证明系统业务正常。比如 FreeRTOS 的某个关键任务死锁了，UART 通信任务卡住了，电源状态机不再执行了，但定时器中断仍然周期触发并喂狗，那么 IWDG 永远不会复位，反而掩盖了故障。

在 FreeRTOS 项目里，我更倾向于任务级看门狗。每个关键任务维护一个 heartbeat，比如通信任务、电源状态机任务、电量采样任务、日志或状态上报任务。任务每完成一轮正常逻辑，就更新自己的 heartbeat 时间戳或计数。

然后单独有一个 watchdog monitor 任务周期检查这些 heartbeat：

```text
power_task_alive
uart_task_alive
battery_task_alive
can_task_alive
```

只有所有关键任务都在规定时间窗口内更新过 heartbeat，并且系统没有关键错误标志时，monitor 任务才喂 IWDG。否则就停止喂狗，让 IWDG 复位系统。

这样 IWDG 触发条件就不只是“CPU 还在执行中断”，而是“系统关键任务都还活着”。这比定时器中断无条件喂狗更能发现真实故障。

### 更简洁的面试版

> IWDG 是独立看门狗，用独立低速时钟，适合做最终兜底；WWDG 是窗口看门狗，要求在规定窗口内喂，能发现喂太早或太晚这类时序异常。不建议在定时器中断里无条件喂 IWDG，因为业务任务死锁时，只要中断还在跑，看门狗就一直被喂，故障反而暴露不出来。FreeRTOS 里我会让每个关键任务更新 heartbeat，由一个监控任务检查所有关键任务都在超时时间内报活，再统一喂 IWDG；如果某个任务卡死或通信链路长时间异常，就停止喂狗让系统复位。

### 可能追问

1. 如果 watchdog monitor 任务自己卡死怎么办？
2. 低优先级任务被高优先级任务饿死，任务级看门狗能发现吗？
3. IWDG 复位后如何判断上次是看门狗复位？
4. 看门狗复位后是否应该直接进入 Bootloader？
5. 哪些任务应该纳入 heartbeat，哪些不应该？

---

## 第 20 轮：看门狗复位后的启动决策

### 面试官问题

**如果 IWDG 复位了，Bootloader 或 APP 怎么判断上一次复位是看门狗复位？如果发现是看门狗复位，应该直接进入 Bootloader，还是继续启动 APP？**

### 你的原始回答要点

- 可以通过读取 RCC 来判断复位原因。
- 是否进入 Bootloader 要分情况判断。
- 结合 metadata。
- 如果是 `pending` 状态，则进入 Bootloader。
- 如果是 `confirmed` 状态，则进入 APP。

### 点评

这个回答可以给 85 分，核心判断是对的：**看门狗复位不是一定进入 Bootloader，要结合 metadata 状态判断**。

需要补充三个细节：

1. STM32 通常可以通过 `RCC_CSR` 的复位标志判断复位原因，比如 `IWDGRSTF`、`WWDGRSTF`、`PORRSTF`、`PINRSTF` 等。
2. 读取复位原因后要清除复位标志，否则下次启动可能误判上一次的复位原因。
3. 对 confirmed APP，单次 IWDG 复位可以继续启动 APP；但如果连续多次 IWDG 复位，可能要进入 Bootloader 或安全模式，避免无限重启。

### 优化后的回答

STM32 里可以通过 RCC 的复位标志判断上一次复位原因，通常看 `RCC_CSR` 里的标志位，比如独立看门狗复位 `IWDGRSTF`、窗口看门狗复位 `WWDGRSTF`、上电复位 `PORRSTF`、外部 NRST 引脚复位 `PINRSTF` 等。Bootloader 启动早期读取这些标志，记录或参与决策，然后要清除复位标志，避免下次启动继续误判。

发现是 IWDG 复位后，不应该无脑进入 Bootloader，而要结合 metadata 和 APP 状态判断。

如果当前 APP 是 `pending` 状态，说明这是刚升级后的试运行版本。pending APP 还没有确认成功，如果它发生 IWDG 复位，说明新 APP 可能启动失败或关键任务卡死。这时 Bootloader 应该增加启动失败计数。如果超过允许次数，标记为 `failed`，留在 Bootloader 等待重新升级；如果是 A/B 方案，则回滚到上一个 `confirmed` 分区。

如果当前 APP 已经是 `confirmed` 状态，单次 IWDG 复位不一定代表固件不可用，可能只是偶发异常。这种情况下可以继续启动 APP，并记录一次异常复位计数。如果短时间内连续多次 IWDG 复位，比如连续 3 次，就说明 APP 可能存在稳定性问题，可以进入 Bootloader、安全模式，或者等待 RK 重新升级。

所以决策逻辑可以总结为：

```text
pending + IWDG reset      -> 认为新固件试启动失败，增加失败计数
confirmed + 单次 IWDG    -> 记录异常，继续启动 APP
confirmed + 多次 IWDG    -> 进入 Bootloader/安全模式，等待处理
APP 无效                 -> 留在 Bootloader
```

### 更简洁的面试版

> 可以读 STM32 的 RCC 复位标志，比如 `RCC_CSR` 里的 `IWDGRSTF/WWDGRSTF` 判断是否看门狗复位，读取后要清掉标志。看门狗复位后不一定直接进 Bootloader，要结合 metadata。如果 APP 是 pending，说明新固件还没确认，IWDG 复位可以认为试启动失败，增加失败计数，必要时标记 failed 或回滚；如果 APP 是 confirmed，单次 IWDG 可以记录后继续启动 APP，但连续多次 IWDG 就应该进入 Bootloader 或安全模式，避免无限重启。

### 可能追问

1. RCC 复位标志什么时候清除比较合适？
2. 连续复位计数存在哪里？
3. 如果 confirmed APP 连续复位，进入 Bootloader 后如何让 RK 知道原因？
4. 看门狗复位后 Bootloader 自己要不要喂狗？
5. 如果复位原因同时有多个标志，怎么判断优先级？

---

## 第 21 轮：电源/按键状态机

### 面试官问题

**你项目里的电源/按键状态机是怎么设计的？比如短按、长按、开机、关机、低电保护、RK3562 的 `PWRON` 协同，这些状态和事件你会怎么组织？**

### 你的原始回答要点

- 电源主要有两个状态：开机和关机。
- 开机状态下短按，拉低相应管脚，控制 RK3562 的 RK809 进入息屏模式。
- MCU ADC 检测到电量低于阈值时，进入关机模式。
- 开机状态下长按 2 秒以上进入关机流程。
- 确保 RK809 安全关机后，拉低外设并关闭电源，进入关机状态。
- 关机状态下短按进入电量显示模式，点亮对应 LED。
- 关机状态下长按 2 秒进入开机模式。
- 低电量时显示开机失败。

### 点评

这个回答可以给 75 分，业务逻辑基本说到了：短按显示电量/息屏，长按开关机，低电保护，和 RK809 `PWRON` 协同。

主要问题是“只有开机和关机两个状态”太粗。真实项目里可以把主状态仍然抽象为开机/关机，但面试表达最好拆出中间态，否则很多时序说不清楚。比如：

- `OFF`
- `SHOW_BATTERY`
- `POWERING_ON`
- `ON`
- `SCREEN_OFF` 或 `SLEEP_REQUEST`
- `SHUTDOWN_REQUEST`
- `POWERING_OFF`
- `LOW_BAT_LOCK`

这样能体现你不是简单 if/else，而是按事件和状态组织逻辑。

### 优化后的回答

我会把电源按键逻辑设计成事件驱动的状态机，而不是简单判断按键电平。按键层先做去抖和动作识别，把原始 GPIO 电平转换成事件，比如短按、长按 2 秒、按下、释放。电源状态机只处理这些事件。

核心状态可以这样划分：

```text
OFF              关机
SHOW_BATTERY     关机短按显示电量
POWERING_ON      正在开机
ON               正常开机
SCREEN_OFF       RK 息屏/睡眠协同
SHUTDOWN_REQUEST 请求 RK 软关机
POWERING_OFF     等待 RK 安全关机并关闭外设
LOW_BAT_LOCK     低电禁止开机
```

在关机状态 `OFF` 下，短按不真正开机，只显示电量，比如根据 ADC 估算电量点亮 1 到 4 格 LED，显示几秒后回到 OFF。长按超过 2 秒时，先检查电量。如果电量低于开机阈值，就进入低电提示，比如最低电量灯快闪，然后回到 OFF；如果电量足够，就进入 `POWERING_ON`，打开必要电源 rails，拉动 RK3562/RK809 的 `PWRON` 时序，等待 RK 启动。

在开机状态 `ON` 下，短按可以作为息屏或唤醒事件，通过控制 RK809 `PWRON` 或通知 RK 侧服务，让 RK3562 进入息屏/睡眠，而不是直接断电。长按超过 2 秒时，不会马上切电，而是进入 `SHUTDOWN_REQUEST`，先通知 RK 或模拟 RK809 `PWRON` 长按，让 RK3562 有机会正常关机、同步文件系统和停止服务。

进入 `POWERING_OFF` 后，MCU 等待 RK 安全关机条件，比如等待一段固定关机窗口、读取 RK 状态脚、或者等待 RK-MCU 协议里的关机 ACK。确认后再关闭外设电源、拉低相关控制脚，最后进入 `OFF`。

低电保护是一个全局事件。MCU 通过 ADC 采样电池电压，并做滤波和阈值判断。如果开机状态下电量自然下降到低电关机阈值，就触发低电关机流程；如果关机状态下电量低于开机阈值，就拒绝开机并用 LED 提示。

### 更简洁的面试版

> 我会把按键先做去抖和短按/长按识别，再把事件交给电源状态机。状态不只开机和关机，还包括 `OFF、SHOW_BATTERY、POWERING_ON、ON、SCREEN_OFF、SHUTDOWN_REQUEST、POWERING_OFF、LOW_BAT_LOCK`。关机短按只显示电量，长按 2 秒检查电量后开机；低电则拒绝开机并闪灯。开机短按控制 RK809/RK3562 息屏或唤醒，长按 2 秒进入软关机流程，先通知或模拟 RK809 PWRON 让 RK 安全关机，等待 ACK/状态或超时后再关闭外设电源。低电保护作为全局事件，开机状态下触发软关机，关机状态下禁止开机。

### 可能追问

1. 按键去抖怎么做？
2. 长按 2 秒是在哪里计时，定时器还是任务 tick？
3. 为什么关机不能直接断 RK3562 电源？
4. 如何判断 RK3562 已经安全关机？
5. ADC 电量阈值如何避免抖动导致反复开关机？

---

## 第 22 轮：按键去抖、短按长按与 GPIO 中断

### 面试官问题

**按键去抖你会怎么做？短按和长按怎么区分？为什么不建议在 GPIO 中断里直接执行开关机逻辑？**

### 你的原始回答要点

- 按下后做 20-30ms 延时，再检测是否放开。
- 短按定义为 500ms 以下。
- 长按定义为 2000ms 以上。
- 中断里不能放耗时太长的任务。
- 按键抖动可能触发多次中断。

### 点评

这个回答可以给 75 分，关键点基本正确：需要 20-30ms 去抖，需要用按下持续时间区分短按和长按，中断里不能做耗时开关机逻辑。

需要注意一个表达风险：不要说成“在 GPIO 中断里 delay 20-30ms”。中断里延时会阻塞其他中断和系统调度。更好的方式是：

1. GPIO 中断里只记录事件或置标志。
2. 在定时器 tick 或 FreeRTOS 按键任务里做 10ms 周期扫描。
3. 连续多次采样稳定后才认为按键状态变化。
4. 用按下持续时间判断短按、长按。

### 优化后的回答

按键去抖我不会在 GPIO 中断里直接 `delay`，而是用状态机或周期扫描来做。机械按键按下和释放时会有抖动，可能在 10-30ms 内产生多次电平变化。如果直接在中断里处理开关机，很容易被重复触发。

比较稳妥的做法是：GPIO 中断只做很轻的事情，比如记录有按键边沿、唤醒按键任务，或者干脆不用中断，直接让按键任务每 10ms 扫描一次 GPIO。只有连续 2-3 次采样都保持同一状态，才认为按键状态稳定，相当于 20-30ms 去抖。

去抖后，状态机记录按下时间和释放时间。比如：

```text
按下持续时间 < 500ms       -> 短按
按下持续时间 >= 2000ms    -> 长按
500ms-2000ms              -> 可按业务定义为无效或普通按压
```

长按事件通常可以在按住超过 2 秒时触发，不一定非要等释放；短按一般在释放时根据按下时长判断。

不建议在 GPIO 中断里直接执行开关机逻辑，原因有三个：

1. 中断里不能做耗时操作，比如等待 RK 关机、ADC 滤波、LED 动画、串口通信。
2. 按键抖动会造成多次中断，容易重复触发开关机。
3. 开关机涉及状态机、低电判断、RK809 `PWRON` 时序和外设电源控制，应该放到任务上下文里按状态处理。

所以更好的结构是：中断或扫描层只产生 `KEY_SHORT_PRESS`、`KEY_LONG_PRESS` 这类事件，电源状态机任务再根据当前状态决定是显示电量、息屏、开机还是软关机。

### 更简洁的面试版

> 按键去抖我不会在中断里 delay，而是用 10ms 周期扫描或中断置标志后交给任务处理。连续 2-3 次采样稳定才认为状态变化，相当于 20-30ms 去抖。记录按下持续时间，500ms 以下算短按，2s 以上算长按。GPIO 中断里不直接执行开关机，因为按键会抖动触发多次中断，而且开关机涉及 RK 软关机、低电判断、外设电源控制，都是耗时逻辑，应该在任务里的状态机处理。

### 可能追问

1. 长按事件是在按住 2 秒时触发，还是释放时触发？
2. 如果用户一直按住不放，会不会重复触发长按？
3. 多个按键同时按下怎么处理？
4. 关机过程中再次按键要不要响应？
5. FreeRTOS 中按键事件用队列还是事件组传给电源任务？

---

## 第 23 轮：长按事件防重复触发

### 面试官问题

**如果用户一直按住按键不放，长按事件会不会被重复触发？你会怎么设计状态机避免重复触发？**

### 你的原始回答要点

- 不会重复触发。
- 从超过 2000ms 开始进入长按状态。
- 到按键松开才完成长按事件的触发。

### 点评

这个回答可以给 75 分，方向是对的：需要进入长按状态，并且一次按住只产生一次长按事件。

注意口误：应该说 `2000ms` 或 `2s`，不要说 `2000s`。

还可以补充一个关键实现点：需要一个 `long_pressed_reported` 或类似标志位，表示本轮按下周期中长按事件已经上报过。否则按键任务每 10ms 扫描一次，只要持续按住超过 2s，就可能每次扫描都重复发长按事件。

### 优化后的回答

用户一直按住不放时，长按事件不应该重复触发。我的设计是把一次“按下到释放”看作一个按键周期，在这个周期里最多只产生一次短按或长按事件。

状态机可以这样设计：

```text
IDLE -> DEBOUNCE_PRESS -> PRESSED -> LONG_REPORTED -> DEBOUNCE_RELEASE -> IDLE
```

按键稳定按下后进入 `PRESSED`，记录 `press_start_tick`。按键任务周期扫描时，如果发现当前按住时间超过 2000ms，并且 `long_pressed_reported == false`，就产生一次 `KEY_LONG_PRESS` 事件，然后把 `long_pressed_reported` 置为 true，进入 `LONG_REPORTED` 状态。

之后即使用户继续按住，状态机也不会再重复发送长按事件。直到检测到按键释放，并且释放去抖完成，才清除 `long_pressed_reported`，回到 `IDLE`，等待下一次按键周期。

短按一般在释放时判断：如果释放时按住时间小于短按阈值，比如 500ms，并且没有触发过长按，就产生 `KEY_SHORT_PRESS`。

这样可以避免长按重复触发，也能避免长按之后释放时又误判成短按。

### 更简洁的面试版

> 不会重复触发。我会把一次按下到释放作为一个按键周期，增加 `long_pressed_reported` 标志。按住超过 2s 且这个标志为 false 时，只上报一次长按事件，然后置 true；继续按住不再重复上报。等释放并去抖完成后，清掉这个标志，回到空闲状态。短按则在释放时判断，如果按住时间小于 500ms 且没有触发过长按，才上报短按。

### 可能追问

1. 长按触发后，释放时还会不会触发短按？
2. 长按是在超过 2s 立即触发好，还是释放后触发好？
3. 如果按键抖动导致误释放，怎么避免状态机乱跳？
4. 多击，比如双击，要怎么扩展？
5. 低功耗睡眠时按键怎么唤醒系统？

---

## 第 24 轮：RK3562 安全关机与 MCU 断电时机

### 面试官问题

**为什么关机时不能直接切断 RK3562 电源？你会怎么判断 RK3562 已经安全关机，可以由 MCU 关闭外设和主电源？**

### 你的原始回答要点

- 直接切断 RK3562 电源不是安全关机，而是硬掉电。
- 硬掉电可能导致重要事件没保存就丢失。
- 可以通过 RK809 这类 SoC 电源芯片控制关机，理论上长按 6 秒。
- MCU 和 SoC 之间可以通过心跳相互检测。
- 待 RK 完全关机后，MCU 再关闭外设和主电源。

### 点评

这个回答可以给 75 分，方向正确：不能直接硬断电，要先让 RK3562/Android 有机会完成软关机。

需要修正和补强两点：

1. RK-MCU 的主通信链路建议说 UART 心跳、状态 ACK 或关机 ACK。CAN 更适合你项目里的 P401/天地端链路，除非你确实在 RK-MCU 之间也有 CAN。
2. “长按 6 秒”是模拟 PMIC 按键行为的一种方式，但面试里最好说成一种触发手段，而不是唯一判断依据。真正判断是否安全关机，需要结合 ACK、状态脚、心跳消失和超时兜底。

### 优化后的回答

关机时不能直接切断 RK3562 电源，因为这相当于硬掉电。RK3562 上运行 Android/Linux，可能还有文件系统写入、日志落盘、用户态服务退出、网络连接关闭等动作。如果 MCU 直接切主电源，可能导致文件系统损坏、日志丢失、配置没保存，严重时还会影响下次启动。

所以 MCU 侧长按关机时，应该先进入软关机流程。触发方式可以有几种：

1. 通过 RK-MCU UART 协议通知 RK 侧服务准备关机。
2. 模拟 RK809 `PWRON` 长按，让 PMIC/系统进入正常关机流程。
3. 如果系统有状态脚或 power-good 信号，也可以一起作为判断依据。

判断 RK3562 是否已经安全关机，不能只靠固定延时。可以综合几个条件：

1. RK 侧通过 UART 回复关机 ACK 或状态，比如 `SHUTDOWN_READY`。
2. RK-MCU 心跳停止，说明 RK 用户态服务已经退出或系统已经下电。
3. 读取 RK 的 power-good、运行状态脚或 PMIC 状态，确认 SoC 已经进入关机状态。
4. 设置最大等待超时，比如等待若干秒，避免 RK 异常时 MCU 永远卡在关机流程。

实际状态机可以是：用户长按 2 秒后，MCU 进入 `SHUTDOWN_REQUEST`，先通知 RK 或模拟 `PWRON` 长按；然后进入 `POWERING_OFF` 等待 ACK、状态脚或心跳消失；满足安全条件后再关闭外设电源和主电源。如果超时仍没有响应，可以根据产品策略选择强制断电，但要记录这是异常关机。

### 更简洁的面试版

> 不能直接切 RK3562 电源，因为 Android/Linux 可能正在写文件系统、保存配置或退出服务，硬掉电可能造成数据丢失或文件系统损坏。MCU 长按关机时应先触发 RK 软关机，比如通过 UART 通知 RK 服务，或者模拟 RK809 `PWRON` 长按。然后 MCU 等待 RK 的关机 ACK、状态脚/power-good 变化，或者心跳停止，确认 RK 已经退出后再关闭外设和主电源。同时要有超时兜底，避免 RK 异常时 MCU 永远等待。

### 可能追问

1. 如果 RK 一直不回复关机 ACK，MCU 应该怎么办？
2. 心跳停止一定代表 RK 已经安全关机吗？
3. 强制断电前要不要记录异常关机原因？
4. 息屏、睡眠、关机有什么区别？
5. MCU 如何避免关机流程中被重复按键打断？

---

## 第 25 轮：息屏、睡眠和关机的区别

### 面试官问题

**息屏、睡眠、关机有什么区别？在你的项目里，短按为什么适合做息屏/睡眠，而长按才做关机？**

### 你的原始回答要点

- 现阶段的息屏接近深度睡眠。
- 通过控制 RK809 进入深度睡眠，降低功耗。
- 关掉大部分进程。
- 相比关机，睡眠可以快速恢复、迅速启动。
- 短按和长按是业务设计问题，市面上大部分竞品和手机等电子产品都是这样设计，符合用户常识。

### 点评

这个回答可以给 70 分。你说到了低功耗、快速恢复和用户习惯，但有一个概念风险：**息屏、睡眠、关机不能简单等同**。

更准确的区分是：

1. 息屏：主要是显示屏关闭或背光关闭，系统可能仍然运行。
2. 睡眠：系统进入低功耗状态，部分外设或进程挂起，保留内存和唤醒能力。
3. 关机：系统服务退出、文件系统同步、SoC 断电或大部分电源 rail 关闭，恢复需要完整启动流程。

如果你项目里短按实际触发的是 RK809/RK3562 的深度睡眠，那可以说“我们当前业务把短按定义为息屏/睡眠入口”，但不要泛化成“息屏就是深度睡眠”。

### 优化后的回答

息屏、睡眠和关机是三个层级。

息屏一般只是关闭显示屏或背光，系统和大部分服务还在运行，功耗会下降一些，但下降有限，恢复最快。睡眠是更深一层的低功耗状态，系统会暂停部分外设或任务，降低 SoC 和外设功耗，同时保留唤醒能力，所以恢复速度比关机快。关机则是系统服务退出、文件系统同步，最后由 PMIC 或 MCU 关闭主要电源，恢复时需要完整启动 Android/Linux。

在这个项目里，短按适合做息屏或睡眠，是因为短按是高频操作，用户可能只是临时不用屏幕或想省电，但希望很快恢复图传和控制界面。如果短按直接关机，容易误触，而且再次使用时启动时间长，会影响体验。

长按适合做关机，是因为关机是低频、不可立即恢复的动作，需要更明确的用户意图。长按 2 秒可以降低误触风险，也符合手机、遥控器等设备的常见交互习惯。

从工程上，短按只需要通知 RK 进入息屏/睡眠，保留快速唤醒能力；长按则进入完整软关机流程，先让 RK3562 同步文件系统、退出服务，再由 MCU 关闭外设和主电源。

### 更简洁的面试版

> 息屏主要是关显示或背光，系统还在跑；睡眠是更深的低功耗状态，保留唤醒能力；关机则要退出服务、同步文件系统并关闭主要电源，恢复时需要完整启动。短按是高频操作，适合做息屏/睡眠，省电且能快速恢复，也避免误关机；长按代表更明确的用户意图，适合触发软关机流程，先让 RK3562 正常关机，再由 MCU 断外设和主电源。

### 可能追问

1. 如果短按进入睡眠后，哪些模块应该保持唤醒能力？
2. MCU 如何唤醒 RK3562？
3. 睡眠和关机状态下 MCU 自己是否也要低功耗？
4. 睡眠状态下图传链路是否保留？
5. 用户短按唤醒时，如何避免重复触发息屏/唤醒抖动？

---

## 第 26 轮：睡眠/关机状态下 MCU 是否低功耗

### 面试官问题

**睡眠或关机状态下，MCU 自己要不要进入低功耗？如果要，哪些外设/中断需要保留，哪些可以关闭？**

### 你的原始回答要点

- 在这个项目中 MCU 没必要、也不适合进入低功耗。
- MCU 属于常供电的电源管理核心。
- MCU 要保持存活，用于快速响应 RK 端和电源/按键事件。
- MCU 控制的外设启动时间较长，如果频繁进入低功耗，收益不明显，反而影响响应和控制。

### 点评

这个回答可以给 75 分。它符合你当前项目事实：MCU 在系统里承担电源管理、按键检测、RK3562 唤醒/关机协同、电量/充电显示等角色，不是一个可以随时深睡的普通业务 MCU。

但面试里要避免只说“没必要”，最好补上这是**产品取舍**：

1. MCU 常供电，用于保证电源管理链路随时在线。
2. 如果 MCU 进入深度低功耗，按键、RK 唤醒、充电/低电检测、外设电源控制的响应会变复杂。
3. 该项目主要耗电大头在 RK3562、屏幕、图传和外设，MCU 自身功耗不是主要矛盾。
4. 目前优先保证响应速度和电源控制可靠性，而不是把 MCU 功耗压到最低。

### 优化后的回答

在这个项目里，我不会把 MCU 设计成深度低功耗模式下长期休眠。原因是 MCU 是整个手持端的电源管理核心，它是常供电的，需要随时响应按键、电量显示、充电检测、低电保护，以及 RK3562 的开关机、睡眠和唤醒协同。

如果 MCU 进入比较深的低功耗模式，虽然能省一点 MCU 自身功耗，但会带来几个问题：按键唤醒和长按计时逻辑会更复杂；ADC 电量检测、充电状态检测和 LED 提示响应会变慢；RK3562 相关电源控制脚和外设电源时序也更难保证；而且项目里一些外设启动时间较长，频繁关停再恢复可能影响用户体验。

从功耗占比看，手持端主要耗电大头通常在 RK3562、屏幕、图传链路和其他外设，STM32F103 自身功耗相对不是主要矛盾。所以当前项目更优先保证 MCU 常在线、电源状态机可靠、用户按键响应快，而不是强行进入深度低功耗。

当然，这不代表完全不做功耗优化。可以做一些轻量优化，比如非必要时减少 LED 常亮、降低 ADC 采样频率、减少无意义日志和周期任务，未使用的外设不初始化或关闭时钟。但 MCU 作为电源管理核心仍然保持运行，不进入会影响响应的深睡模式。

### 更简洁的面试版

> 在这个项目里 MCU 是常供电的电源管理核心，负责按键、电量/充电检测、低电保护，以及 RK3562 的开关机和唤醒协同，所以我不会让它进入影响响应的深度低功耗。项目主要功耗在 RK、屏幕、图传和外设，MCU 自身不是主要矛盾。我们更看重电源控制可靠性和快速响应。可以做轻量优化，比如降低 ADC 采样频率、减少 LED 常亮、关闭不用的外设时钟，但 MCU 仍保持在线。

### 可能追问

1. 如果客户要求极低待机功耗，你会怎么重新设计？
2. MCU 常供电时，如何估算它对整机续航的影响？
3. 不进低功耗时，如何避免 LED、ADC、日志任务造成额外耗电？
4. 如果未来要做低功耗，哪些唤醒源必须保留？
5. 为什么说电源管理可靠性比 MCU 自身省电更重要？

---

## 第 27 轮：客户要求极低待机功耗时怎么改

### 面试官问题

**如果客户后来要求极低待机功耗，你会怎么改这个方案？哪些唤醒源必须保留？哪些模块可以关闭？**

### 你的原始回答要点

- 该产品使用场景不太会存在极低待机功耗要求。
- 产品本身电池续航足够高。
- 如果有相应需求，可以进入 Standby。
- 但 Standby 和彻底关机差别不大。

### 点评

这个回答可以给 70 分。你有产品场景判断，这是对的：手持图传遥控终端的主要功耗在 RK、屏幕、图传和外设，极低待机不一定是第一目标。

但面试官问“如果客户要求”，希望听到你能提出改造方案。你可以说当前项目没必要，但如果需求变化，要从系统层重新设计，而不是只说 Standby。

Standby 的判断也基本对：STM32 进入 Standby 后功耗很低，但 RAM 和大多数外设状态会丢失，恢复更接近冷启动，所以体验上接近关机再开机。

### 优化后的回答

从当前产品场景看，这个手持端主要用于无人机控制和图传交互，整机功耗大头在 RK3562、屏幕、图传链路和其他外设，MCU 自身功耗占比不高。而且用户更关注按键响应、快速唤醒和电源控制可靠性，所以当前方案没有把 MCU 极低待机作为第一目标。

如果客户后来明确要求极低待机功耗，我会把它当成系统级改造，而不是只改一行低功耗模式。可以分几层做：

1. RK3562、屏幕、图传、外设电源 rail 全部关闭或进入最低功耗。
2. MCU 关闭不必要外设，比如 UART、CAN、I2C、ADC、LED、日志任务。
3. MCU 进入 Stop 或 Standby，具体取决于要求的功耗和唤醒速度。
4. 保留必要唤醒源，比如电源按键 EXTI、充电插入检测、RTC 定时唤醒或低电检测。
5. 唤醒后重新初始化外设、电源 rails 和 RK3562。

如果用 Stop 模式，RAM 和部分寄存器状态可以保留，唤醒比 Standby 快，但功耗比 Standby 高。如果用 Standby，功耗更低，但 RAM 和大部分外设状态会丢，唤醒后基本类似复位重启，体验接近关机再开机。

所以如果客户只是要求比当前待机更省电，我会优先考虑轻量降频和 Stop；如果要求极低静态功耗，那就可以考虑 Standby，但要接受恢复速度变慢、状态丢失、软件重新初始化复杂度增加这些代价。

### 更简洁的面试版

> 当前产品主要功耗在 RK、屏幕和图传，MCU 是常供电电源管理核心，所以现阶段不把 MCU 极低待机作为第一目标。如果客户强要求，我会做系统级低功耗：关 RK、屏幕、图传和外设电源，MCU 关闭 UART/CAN/ADC/LED 等非必要外设，只保留电源按键、充电插入、RTC 等唤醒源。低功耗模式上，Stop 恢复快但功耗稍高，Standby 功耗最低但 RAM 和外设状态丢失，恢复接近冷启动，体验上接近关机。

### 可能追问

1. Stop 和 Standby 的最大区别是什么？
2. 进入 Standby 后 metadata、运行状态和按键状态会不会丢？
3. 如果保留充电插入唤醒，硬件上要怎么支持？
4. 极低待机下 IWDG 是否还应该开启？
5. 如何测量整机待机电流？

---

## 第 28 轮：STM32 Sleep、Stop、Standby 区别

### 面试官问题

**STM32 的 Sleep、Stop、Standby 模式有什么区别？从功耗、唤醒速度、RAM/寄存器保留、唤醒源这几个角度讲。**

### 你的原始回答要点

- Sleep 只是停 CPU，时钟和外设还在。
- Stop 停了时钟，但是 RAM 还在。
- Standby 下 RAM 清空，基本等于复位重启。
- 功耗依次降低。
- 唤醒时间依次增长。
- Standby 对于当前硬件链路自带开关机的项目没有必要。

### 点评

这个回答可以给 80 分，主线是正确的：Sleep、Stop、Standby 功耗逐级降低，唤醒代价逐级增加。

需要补充几个准确点：

1. Sleep：CPU 内核停止，外设时钟通常还可以继续，任意使能中断都可以唤醒，恢复最快。
2. Stop：大部分高速时钟停止，SRAM 和寄存器内容保持，唤醒后通常要重新配置系统时钟。
3. Standby：核心电源域基本关闭，SRAM 和大部分寄存器不保持，唤醒后类似复位启动，只保留备份域/RTC/备份寄存器等少量内容。
4. 唤醒源从 Sleep 到 Standby 会越来越受限。

### 优化后的回答

STM32 的 Sleep、Stop、Standby 是不同深度的低功耗模式。

Sleep 最浅，主要是 CPU 内核停止执行，外设和时钟通常还在运行。比如定时器、UART、外部中断等仍然可以工作。它功耗下降有限，但唤醒最快，基本上中断来了就能继续执行，RAM 和寄存器都保持。

Stop 更深，会停止大部分高速时钟，比如 HSE/PLL，SRAM 和寄存器内容仍然保持。它功耗比 Sleep 低很多，但唤醒后通常需要重新配置系统时钟。Stop 适合需要保留运行状态，同时又希望明显降低功耗的场景。

Standby 最深，核心电源域基本关闭，SRAM 和大部分寄存器不保持，唤醒后更像一次复位启动。通常只保留备份域、RTC、backup register 这类少量内容。它功耗最低，但唤醒最慢，唤醒源也最受限，比如 WKUP 引脚、RTC 闹钟等。

所以从功耗看：

```text
Sleep > Stop > Standby
```

从唤醒速度和状态保留看：

```text
Sleep 最快，状态保留最多
Stop 居中，RAM 保留但时钟要恢复
Standby 最慢，接近复位重启
```

结合当前项目，手持端已经有 MCU 控制 RK3562 和外设电源的开关机链路，MCU 又是常供电的电源管理核心，所以没有必要为了 MCU 自身省电而进入 Standby。Standby 的恢复体验接近重新开机，和项目里已有的关机流程差别不大。

### 更简洁的面试版

> Sleep 最浅，主要停 CPU，外设和时钟通常还在，RAM/寄存器保持，任意中断可快速唤醒；Stop 更深，停大部分高速时钟，SRAM 和寄存器保持，唤醒后要恢复系统时钟；Standby 最深，核心电源域关闭，SRAM 和大部分寄存器丢失，只保留备份域/RTC，唤醒后类似复位启动。功耗依次降低，唤醒时间依次增加。当前项目 MCU 是常供电电源管理核心，且已有硬件开关机链路，所以 Standby 对项目意义不大。

### 可能追问

1. Stop 唤醒后为什么要重新配置系统时钟？
2. Standby 唤醒后如何判断是从 Standby 唤醒还是普通上电？
3. backup register 在 Standby 后还能不能保留？
4. FreeRTOS 里进入 Sleep/Stop 要注意什么？
5. 低功耗模式下 SysTick 和 IWDG 会怎样？

---

## 第 29 轮：ADC 电量检测、滤波和低电保护

### 面试官问题

**ADC 电量检测为什么不能直接用一次采样值判断电量？你会怎么做滤波、分档和低电保护，避免 LED 电量显示来回跳或者误触发关机？**

### 你的原始回答要点

- 电源电压不够稳定。
- 一次采样不够精准。
- 负载变化或插入充电器会导致电压跳变。
- 如果电压在阈值边界附近波动，LED 电量显示会频繁变化。
- 设计上去除远超 16 次采样平均值的异常电压。
- 对剩余电压取平均。
- 电源显示灯延迟变化。
- 确认档位变化稳定 5 分钟后再改变档位。
- 低电关机也做类似延迟确认。

### 点评

这个回答可以给 85 分，已经有工程味道。你说到了电池电压受负载、充电插入和采样噪声影响，也提到了多次采样、剔除异常值、平均和延迟确认。

需要补强两个点：

1. LED 显示分档可以慢一点，比如稳定几分钟再降档，避免来回跳；但低电保护是安全功能，不能简单也等 5 分钟。低电保护可以有独立阈值和较短确认时间，比如连续多次低于阈值或持续若干秒。
2. 分档最好加滞回。比如从 3 格降到 2 格用一个阈值，从 2 格升回 3 格用更高阈值，避免边界抖动。

### 优化后的回答

ADC 电量检测不能只用一次采样值，因为电池电压本身会波动。比如 RK3562、屏幕或图传负载变化时，电池端电压会瞬间下跌；插入充电器时电压会抬升；ADC 采样本身也有噪声。如果直接用一次采样判断电量，就会导致 LED 档位在边界附近来回跳，甚至误触发低电关机。

我的处理会分几层。

第一层是采样滤波。比如周期采样 16 次，先剔除明显异常的最大/最小值，或者剔除偏离平均值太多的点，再对剩余值求平均。这样可以减小 ADC 噪声和瞬时毛刺影响。

第二层是分档滞回。电量 LED 分成 1-4 档时，每个档位不要只用一个阈值，而是用上升阈值和下降阈值。比如从 3 格降到 2 格用较低阈值，从 2 格升回 3 格要超过更高阈值。这样电压在边界附近轻微波动时，LED 不会反复跳。

第三层是时间确认。LED 显示可以允许慢变化，比如检测到档位变化后，要求稳定一段时间再更新显示。你项目里用类似 5 分钟稳定确认来避免电量灯频繁跳变，这是合理的，因为电量显示不是强实时功能。

第四层是低电保护。低电关机不能完全和 LED 档位显示用同一套慢延时。低电保护是安全功能，应该有独立的低电阈值和确认时间，比如连续 N 次采样低于阈值，或者持续若干秒低于阈值，再触发低电关机。这样既避免瞬时电压跌落误关机，也不会在真正低电时拖太久。

另外，充电状态下可以使用不同策略。插入充电器时，电压会被充电电路抬高，不能简单按开路电压估算真实电量，可以结合 CH224/充电状态和当前系统状态做显示修正。

### 更简洁的面试版

> 不能用一次 ADC 值判断电量，因为电池电压会受负载、充电器插入和 ADC 噪声影响。我的做法是多次采样，比如 16 次，剔除异常值后取平均；LED 分档用滞回阈值，升档和降档阈值分开；档位变化再做时间确认，比如稳定几分钟后才更新，避免 LED 来回跳。低电关机单独做保护阈值，要求连续多次或持续一段时间低于阈值才触发，不能完全用 LED 分档的慢延时，避免真正低电时保护太慢。

### 可能追问

1. ADC 原始值怎么换算成电池电压？
2. 为什么要加滞回，滞回阈值怎么选？
3. 充电状态下电量显示为什么可能不准？
4. 低电保护阈值和 LED 1 格阈值是否应该相同？
5. 如果电池老化，电压-电量映射会有什么问题？

---

## 第 30 轮：ADC 原始值换算电池电压

### 面试官问题

**ADC 原始值怎么换算成电池电压？如果电池电压高于 MCU ADC 量程，硬件上一般怎么接？软件里怎么计算？**

### 你的原始回答要点

- 通过电阻分压。
- 把电池电压分成小于 3.3V 的电压。
- 软件里按电阻比例乘回去。

### 点评

这个回答可以给 75 分，核心正确：电池电压高于 ADC 量程时，用电阻分压把电压降到 ADC 可测范围，再按比例还原。

需要补上公式和几个工程注意点：

1. STM32F103 ADC 通常是 12 位，原始值范围 `0-4095`。
2. `Vadc = raw / 4095 * Vref`。
3. 如果上拉电阻是 `R1`，下拉电阻是 `R2`，ADC 采样下拉电阻上的电压，则 `Vbat = Vadc * (R1 + R2) / R2`。
4. 分压电阻不能太小，否则一直耗电；也不能太大，否则 ADC 采样源阻抗过高，采样不准，需要更长采样时间或并联电容。
5. 还要考虑 Vref 精度、电阻误差和 ADC 校准。

### 优化后的回答

如果电池电压可能高于 MCU ADC 的量程，就不能直接接 ADC。一般用两个电阻做分压，把电池电压分到 0-3.3V 范围内，再接到 ADC 引脚。

假设上面的电阻是 `R1`，下面接地的电阻是 `R2`，ADC 采样的是 `R2` 上的电压，那么：

```text
Vadc = Vbat * R2 / (R1 + R2)
```

所以软件里反推电池电压：

```text
Vbat = Vadc * (R1 + R2) / R2
```

STM32F103 的 ADC 通常是 12 位，如果参考电压是 `Vref = 3.3V`，ADC 原始值是 `raw`，则：

```text
Vadc = raw / 4095.0 * Vref
Vbat = raw / 4095.0 * Vref * (R1 + R2) / R2
```

工程上还要注意分压电阻的选择。电阻太小会一直消耗电池电流，影响待机功耗；电阻太大又会导致 ADC 输入源阻抗高，采样电容充电不足，采样值不准。可以适当增加 ADC 采样时间，或者在 ADC 输入处加一个小电容做滤波。

另外，软件里不能只用理论值，还要考虑电阻精度、Vref 波动和 ADC 误差。更严谨的做法是用实测电压做校准系数，或者读取内部参考电压来修正 Vref。

### 更简洁的面试版

> 电池电压高于 ADC 量程时，用电阻分压接到 ADC。假设上拉是 `R1`，下拉是 `R2`，ADC 采样 `R2` 上电压，则 `Vadc = Vbat * R2 / (R1 + R2)`，所以 `Vbat = Vadc * (R1 + R2) / R2`。STM32 12 位 ADC 下，`Vadc = raw / 4095 * Vref`。工程上要注意分压电阻不能太小浪费电，也不能太大导致采样不准，必要时加长采样时间或加滤波电容，并考虑 Vref 和电阻误差校准。

### 可能追问

1. 为什么分压电阻太大会影响 ADC 精度？
2. ADC 采样时间怎么影响测量结果？
3. 如果 Vref 不是精确 3.3V，怎么修正？
4. 为什么要在 ADC 输入加电容？
5. 电池电压到电量百分比能不能简单线性映射？

---

## 第 31 轮：电池电压到电量百分比映射

### 面试官问题

**电池电压到电量百分比能不能简单线性映射？比如 4.2V 是 100%，3.3V 是 0%，中间直接按比例算，这样有什么问题？**

### 你的原始回答要点

- 不能简单线性映射。
- 电池电压和电量的关系是非线性的。
- 应该按照相应表格进行计算。
- 或者部署相应的电池算法。

### 点评

这个回答可以给 80 分，方向正确。需要补充“为什么不能线性”的原因，以及项目里如何取舍。

主要原因：

1. 锂电池放电曲线不是直线，中间有较长平台区。
2. 电压会受负载影响，同一 SOC 在大负载下电压更低。
3. 温度、电池老化、内阻都会影响端电压。
4. 充电状态下电压会被充电电路抬高，不等于真实剩余电量。
5. 只靠电压估算 SOC 通常只能做粗略档位，不适合做高精度百分比。

### 优化后的回答

电池电压到电量百分比不能简单线性映射。比如不能直接认为 4.2V 是 100%，3.3V 是 0%，中间按比例算。因为锂电池的放电曲线是非线性的，中间有比较明显的平台区，电压变化不大但电量可能已经消耗了很多；到了低电段，电压又可能下降很快。

另外，电池端电压还会受负载影响。比如 RK3562、屏幕、图传链路负载变大时，电池内阻会造成瞬时压降，同样的剩余电量下测到的电压会更低。温度、电池老化、内阻变化、充电器插入也都会影响电压。所以单纯用电压线性换算百分比会不准，可能出现电量突然掉、突然涨、低电误判等问题。

工程上可以用几种方式：

1. 简单方案：根据电池厂家曲线或实测数据做电压-SOC 查表，再插值。
2. 项目里只显示 1-4 格 LED 时，可以用经验阈值和滞回，不一定显示精确百分比。
3. 更高精度方案：使用电量计芯片，或者做库仑计积分，结合电压、温度和负载模型估算 SOC。

在我们这个项目里，如果只是四档 LED 显示和低电保护，用查表/阈值/滞回已经够用；如果要在 APK 上显示精确百分比，就需要更完整的电池模型或电量计方案。

### 更简洁的面试版

> 不能简单线性映射。锂电池放电曲线不是直线，中间有平台区，低电段又下降很快；同一电量下，负载、温度、内阻、老化和充电状态都会影响端电压。所以只靠电压线性算百分比会不准。简单项目可以按电池曲线查表加插值，四档 LED 可以用经验阈值和滞回；如果要高精度百分比，就要用电量计芯片或库仑计积分算法。

### 可能追问

1. 什么是 SOC？
2. 什么是库仑计？
3. 只用电压估算电量有什么场景限制？
4. 电池老化后为什么电压-电量映射会变差？
5. 低电保护应该看电压、SOC，还是两者结合？

---

## 第 32 轮：RK3562 用户态服务为什么用 poll

### 面试官问题

**你项目里 RK3562 用户态服务为什么要用 `poll`？如果同时要处理 `/dev/ttyS3`、UDP socket、UDS、SocketCAN，这些东西为什么都可以放进同一个 `poll` 循环里？**

### 你的原始回答要点

- `poll` 可以让一个线程同时等待多个 fd。
- 哪个 fd 先有数据，就先处理哪个。
- `/dev/ttyS3`、UDP socket、UDS、SocketCAN 本质上都可以被当成 fd。

### 点评

这个回答可以给 80 分，核心正确。你已经抓住了 Linux 用户态 IO 的关键抽象：很多资源都可以用文件描述符 fd 表示，并且可以用 `poll` 统一等待事件。

需要补强三点：

1. `poll` 等待的是“就绪事件”，比如可读 `POLLIN`、可写 `POLLOUT`、错误 `POLLERR`。
2. 如果不用 `poll`，直接对多个 fd 做阻塞 `read`，线程可能卡在某一个 fd 上，导致其他链路的数据不能及时处理。
3. 对 CommRouter 这类路由服务，单线程 `poll` 事件循环可以减少多线程锁和共享状态复杂度。

### 优化后的回答

RK3562 用户态服务要同时处理多条链路，比如 MCU 串口 `/dev/ttyS3`、APK 或 P401 的 UDP socket、本地 UDS、以及 SocketCAN。它们数据到达的时间不固定，如果对某一个 fd 直接阻塞 `read`，线程可能一直卡在那里，导致其他链路的数据不能及时处理。

`poll` 的作用就是让一个线程同时等待多个 fd 的事件。比如把这些 fd 都放进 `pollfd` 数组里，设置关心的事件为 `POLLIN`。当任意一个 fd 有数据可读时，`poll` 返回，程序再检查每个 fd 的 `revents`，看是串口可读、UDP 可读、UDS 可读，还是 CAN socket 可读，然后分别调用对应的处理函数。

这些东西能放到同一个 `poll` 里，是因为 Linux 把很多内核资源都抽象成文件描述符：

```text
/dev/ttyS3     字符设备 fd
UDP socket     网络 socket fd
UDS            Unix domain socket fd
SocketCAN      CAN raw socket fd
```

它们虽然底层驱动不同，但在用户态都可以通过 fd 访问，并且支持 `read/recvfrom/write/sendto/ioctl/poll` 这类统一接口。

对 CommRouter 这类消息路由服务来说，`poll` 事件循环比较合适。它可以在一个线程里处理 UART、UDP、CAN、UDS 的输入，再根据消息来源做 MAVLink 路由和转发。这样比每个链路开一个线程更简单，也减少了多线程共享路由表、队列和状态时的锁竞争。

### 更简洁的面试版

> `poll` 是为了让一个线程同时等多个 fd。如果直接阻塞 read 某一个 fd，线程可能卡住，其他 UART/UDP/CAN 数据就处理不到。Linux 里 `/dev/ttyS3` 是字符设备 fd，UDP/UDS/SocketCAN 都是 socket fd，虽然底层不同，但用户态都可以用 fd 表示，并通过 `poll` 等待 `POLLIN` 等就绪事件。`poll` 返回后检查 `revents`，哪个 fd 可读就处理哪个，适合 CommRouter 这种多链路消息路由服务。

### 可能追问

1. `pollfd` 结构体里 `fd/events/revents` 分别是什么？
2. `poll` 和阻塞 `read` 有什么区别？
3. `poll` 和 `select/epoll` 有什么区别？
4. `poll` 返回后为什么还要检查 `revents`？
5. 如果某个 fd 读到半包，事件循环怎么处理？

---

## 第 33 轮：pollfd 的 fd、events、revents

### 面试官问题

**`pollfd` 结构体里的 `fd`、`events`、`revents` 分别是什么？`poll` 返回后你会怎么判断哪个链路有数据？**

### 你的原始回答要点

- `fd` 是监听的对象。
- `events` 是等待发生的事件。
- `revents` 是实际发生的事件。
- `poll()` 返回后，逐个检查 `fds[i].revents`。
- 哪个 fd 带 `POLLIN`，哪个链路就有数据可读。

### 点评

这个回答可以给 90 分，已经很接近标准答案。

可以补充两个工程细节：

1. `events` 是应用告诉内核“我关心什么事件”，常见是 `POLLIN`、`POLLOUT`。
2. `revents` 是内核返回“实际发生了什么事件”，除了 `POLLIN`，还要处理 `POLLERR`、`POLLHUP`、`POLLNVAL` 这些异常。

### 优化后的回答

`pollfd` 结构体里：

```c
struct pollfd {
    int   fd;
    short events;
    short revents;
};
```

`fd` 是要监听的文件描述符，比如 `/dev/ttyS3`、UDP socket、UDS socket、SocketCAN socket。

`events` 是应用层告诉内核自己关心哪些事件。比如 `POLLIN` 表示关心可读事件，`POLLOUT` 表示关心可写事件。

`revents` 是 `poll()` 返回后由内核填写的实际事件。比如某个 fd 的 `revents & POLLIN` 为真，就说明这个 fd 当前有数据可读。

在 RK3562 用户态服务里，可以把多条链路放进同一个数组：

```c
fds[0].fd = uart_fd;    fds[0].events = POLLIN;
fds[1].fd = udp_fd;     fds[1].events = POLLIN;
fds[2].fd = uds_fd;     fds[2].events = POLLIN;
fds[3].fd = can_fd;     fds[3].events = POLLIN;
```

`poll()` 返回后逐个检查：

```c
if (fds[0].revents & POLLIN) handle_uart();
if (fds[1].revents & POLLIN) handle_udp();
if (fds[2].revents & POLLIN) handle_uds();
if (fds[3].revents & POLLIN) handle_can();
```

同时还要检查异常事件，比如：

```c
POLLERR   fd 出错
POLLHUP   对端关闭或挂起
POLLNVAL  fd 无效
```

这样路由服务才能在某条链路异常时重连、关闭 fd 或上报错误，而不是一直在异常 fd 上空转。

### 更简洁的面试版

> `fd` 是要监听的文件描述符，`events` 是我希望等待的事件，比如 `POLLIN` 可读，`revents` 是 `poll` 返回后内核告诉我实际发生的事件。`poll` 返回后遍历 `pollfd` 数组，哪个 `revents & POLLIN` 为真，就处理哪个链路的数据。工程上还要处理 `POLLERR/POLLHUP/POLLNVAL`，避免 fd 出错或断开后事件循环异常。

### 可能追问

1. `poll` 返回值大于 0、等于 0、小于 0 分别表示什么？
2. `poll` 超时时间传 `-1`、`0`、正数分别是什么意思？
3. 如果多个 fd 同时可读，处理顺序怎么决定？
4. `POLLHUP` 出现后应该继续读还是关闭重连？
5. `poll` 适合非常大量 fd 吗？什么时候考虑 epoll？

---

## 第 34 轮：P401 CAN-MAC 配对项目总述

### 面试官问题

请你用 1-2 分钟介绍一下 P401 CAN-MAC 配对这个功能。

回答时尽量包含：

1. 这个功能要解决什么问题？
2. 天空端和地面端分别是什么角色？
3. 为什么要交换 `tun` 网卡 MAC？
4. 你具体做了哪一部分？
5. 当前验证到什么程度，哪些地方不能夸大？

### 你的原始回答要点

- 解决 P401 图传天空端和地面端有时无法互联、不稳定、连接慢的问题。
- 天空端是 RK3588，从摄像头获取画面，并向地面发送。
- 地面端是 RK3562，通过图传接收画面，传给上层 APK 并在屏幕显示。
- 交换 `tun` 网卡 MAC 可以让图传快速连接配对。
- 你做了通过 CAN 相互交换 MAC。
- 你主要做了 CAN 链路通信、节点自主拉起、发送心跳、传输 MAC。

### 点评

这个回答可以给 70 分，方向对，但需要更稳妥地表达项目边界。

需要优化三点：

1. “解决无法互联、不稳定、连接慢”可以作为背景，但不要完全归因于 MAC 交换。更稳妥是说：目标是减少人工配置，提高天地端接触后的自动化配对效率。
2. “交换 MAC 可以直接让图传快速连接配对”说得偏满。当前更安全的说法是：交换 `tun` MAC 为后续自动配对或 vendor pairing API 调用提供 peer 标识和触发条件。
3. 职责边界要清楚：重点在 RK3562 地面端和 CAN/DroneCAN 自定义报文链路；天空端自动服务、真实 P401 pairing API 和长期稳定性不要夸大。

### 优化后的回答

这个功能是为了解决 P401 图传天地端配对配置比较繁琐、接触后自动化程度不够的问题。天空端是 RK3588，主要负责摄像头采集、编码和图传发送；地面端是 RK3562，主要负责接收图传数据，并转给上层 APK 在屏幕上显示。

我们的思路是利用天地端已有的 CAN 接触链路，在两端建立 CAN 通信后，自动交换 P401 相关 `tun` 网卡的 MAC 地址。MAC 地址相当于后续配对或识别 peer 的关键标识，这样可以减少人工读取和配置 MAC 的过程，为后续自动调用配对接口提供准备。

我主要参与的是 RK3562 地面端和 CAN 报文链路，包括读取本地 `tun` MAC，通过 SocketCAN `can1` 发送 MAC report，接收天空端 report 后解析 peer MAC，回复 ACK，并做重复报文去重和 dry-run 验证。

当前验证边界上，地面端的 report/ACK 闭环和模拟天空端报文注入已经验证过；手动恢复到稳定 CAN 状态时，也观察到过天空端 report、地面端 ACK、地面端 report 的有效交换帧。但真实 P401 pairing API、天空端 MCP2515 接收稳定性、自动天空端服务和反复接触场景下的长期稳定性还不能夸大，需要继续联调。

### 更简洁的面试版

> P401 CAN-MAC 配对的目标是减少天地端图传配对的人工配置。天空端 RK3588 负责摄像头采集和图传发送，地面端 RK3562 负责接收图传并显示到 APK。我们通过已有 CAN 链路交换两端 `tun` 网卡 MAC，让双方拿到 peer 标识，为后续自动配对触发提供条件。我主要做 RK3562 地面端的 CAN/SocketCAN 链路，包括读取本地 tun MAC、发送 MAC report、解析天空端 report、回复 ACK 和去重验证。当前地面端 report/ACK 和模拟天空端注入已验证，但真实 P401 pairing API、天空端 MCP2515 稳定接收和长期自动配对还不能夸大。

### 可能追问

1. 为什么要通过 CAN/DroneCAN 报文交换 MAC，而不是 UDP、UART 或手动配置？
2. `tun` 网卡是什么？为什么 P401 相关 MAC 要看 `tun`？
3. MAC report 和 ACK 分别解决什么问题？
4. 为什么要做去重？
5. 当前 `dry_run=true` 代表什么边界？

---

## 第 35 轮：为什么用 CAN/DroneCAN 交换 MAC

### 面试官问题

**为什么要通过 CAN/DroneCAN 报文交换 MAC，而不是 UDP、UART 或手动配置？CAN 在这个场景里的优势是什么？**

### 你的原始回答要点

- CAN 是差分信号，更稳定，不容易受外界干扰。
- CAN 交换 MAC 面临物理链路断开和连接的场景。
- CAN 的容错率更高。

### 点评

这个回答可以给 70 分，方向是对的：CAN 差分传输、抗干扰、适合物理接触链路。

但还需要补充这个项目里的关键理由：

1. MAC 交换发生在图传网络完全建立之前，而 UDP 依赖 IP 网络已经可用，所以 UDP 不适合作为配对前置链路。
2. 手动配置 MAC 容易出错，也不符合“无感快速配对”的目标。
3. UART 更适合板内点对点，比如 RK-MCU；天地端之间已经有 CAN/DroneCAN 接触链路，复用它更自然。
4. CAN 有仲裁、错误检测、ACK、错误状态机，适合反复接触、断开、重连的链路验证。

### 优化后的回答

选择 CAN/DroneCAN 交换 MAC，主要是因为这个阶段的目标是“在图传网络真正建立之前，先让天地端获得对方的 peer 标识”。如果用 UDP，前提是两端 IP 网络和 P401 图传链路已经建立，但 MAC 交换本身就是为了辅助后续配对，所以 UDP 不适合作为前置发现链路。

手动配置虽然简单，但不符合无感快速配对的目标。用户需要读取、输入或维护 MAC，容易出错，也不适合现场快速接触配对。

UART 更适合板内点对点通信，比如 RK3562 和 MCU。天地端之间已经存在 CAN/DroneCAN 接触链路，所以复用 CAN 更合适，不需要额外增加一条串口链路。

CAN 本身也适合这种场景。它是差分信号，抗干扰能力比普通单端信号更好；协议层有仲裁、ACK、CRC、错误计数和 error-active/error-passive/bus-off 等状态，适合在物理接触、断开、重连的场景下做可靠的小数据交换。MAC 地址只有 6 字节，非常适合放在单帧 CAN/DroneCAN 自定义报文里传输。

所以这个方案的本质是：复用已有 CAN 接触链路，用很小的报文在配对前交换 peer MAC，减少人工配置，并为后续 P401 自动配对触发提供依据。

### 更简洁的面试版

> MAC 交换发生在图传网络完全建立之前，如果用 UDP，就要求 IP/图传链路已经通了，这和配对前置目标矛盾。手动配置效率低且容易出错，UART 更适合板内点对点。天地端已有 CAN/DroneCAN 接触链路，CAN 又是差分信号，抗干扰强，有 ACK、CRC、错误状态机，适合接触、断开、重连场景。MAC 只有 6 字节，很适合通过 CAN 单帧 report/ACK 交换。

### 可能追问

1. CAN 的 ACK 和你协议里的 ACK 是一回事吗？
2. CAN 错误状态机有哪些状态？
3. 为什么 MAC 6 字节适合放进 CAN 单帧？
4. DroneCAN 在这里起到什么作用？
5. 如果 CAN 链路不稳定，MAC report 怎么重发？

---

## 第 36 轮：CAN ACK 和应用层 ACK 的区别

### 面试官问题

**CAN 协议本身也有 ACK，你们 MAC report 里又设计了 ACK 报文。这两个 ACK 是一回事吗？为什么应用层还需要 ACK？**

### 你的原始回答要点

- 不一样。
- CAN 协议本身的 ACK 是为了确定这一帧已经送达。
- 收到节点会做响应，验证链路正常。
- MAC report 的 ACK 是为了确定 MAC 成功发送、成功被接收并且验证通过。
- MAC ACK 也是一种信号，让另一端发送相应 MAC。

### 点评

这个回答可以给 80 分，方向正确。你已经区分了 CAN 协议层 ACK 和 MAC report 应用层 ACK。

需要把 CAN ACK 说得更精确一点：

1. CAN ACK 只能说明总线上至少有一个节点正确接收了这一帧，并在 ACK 槽响应。
2. 它不能说明“目标节点应用程序已经收到并解析了 MAC”。
3. 它也不能说明“收到的是我们期望的天空端或地面端节点”。
4. 应用层 ACK 才能表达业务语义：我收到了 MAC、格式合法、类型正确、已经记录 peer MAC。

### 优化后的回答

这两个 ACK 不是一回事。

CAN 协议本身的 ACK 是链路层机制。发送节点发出一帧后，如果总线上至少有一个接收节点正确接收并通过 CRC 校验，它会在 ACK 槽给出响应。这个 ACK 只能说明“这帧在 CAN 总线上被某个节点正确接收了”，可以帮助发送端判断物理链路和 CAN 帧传输是否基本正常。

但 CAN ACK 不能说明目标应用已经处理成功。比如总线上可能有多个节点，某个节点 ACK 了这一帧，但不一定是我们期望的天空端；即使目标节点收到了，应用程序也可能因为 DTID 不匹配、payload 长度错误、MAC 类型不对、服务没启动等原因没有处理。

所以 MAC report 还需要应用层 ACK。应用层 ACK 的含义是：对端的 MAC exchange 逻辑已经收到 report，解析出 MAC，确认 MAC 类型和报文格式有效，并且已经记录 peer MAC。这样发送方才知道这次 MAC 交换在业务层真正成功。

另外，应用层 ACK 也可以作为状态推进信号。例如地面端收到天空端 MAC report 后，回复 ACK；天空端收到 ACK 后知道地面端已经拿到自己的 MAC，同时也可以继续发送或确认地面端 MAC。重复 report 时，应用层可以重复 ACK，但不重复触发真实配对逻辑。

### 更简洁的面试版

> CAN ACK 是链路层 ACK，只能说明总线上至少有节点正确收到这一帧，不能说明目标应用已经解析并接受 MAC。我们设计的 MAC ACK 是应用层 ACK，表示对端服务已经收到 MAC report，校验了 DTID、payload、MAC 类型，并记录了 peer MAC。它还可以作为状态推进信号，用来确认双方 MAC 交换完成。重复 report 可以继续 ACK，但不能重复触发真实配对。

### 可能追问

1. 如果 CAN ACK 成功但应用层 ACK 没回来，可能是什么原因？
2. MAC ACK 的 payload 为什么要带回对方 MAC？
3. 重复 report 为什么还要继续 ACK？
4. ACK 丢了怎么办？
5. 应用层 ACK 和去重怎么配合？

---

## 第 37 轮：CAN ACK 成功但应用层 ACK 没回来怎么排查

### 面试官问题

**如果 CAN ACK 成功，但应用层 MAC ACK 一直没回来，可能有哪些原因？你会怎么排查？**

### 你的原始回答要点

- CAN ACK 成功说明物理链路基本是通的。
- 可能是对端应用层脚本出错，没有正常返回 ACK。
- 可能是发送的 MAC 出错。
- 也可能是对端 P401 图传连接失败。
- 可以查询相应日志。

### 点评

这个回答可以给 70 分，有分层排查意识，但还需要更系统。

需要注意：CAN ACK 成功只能说明总线上至少有节点 ACK 了这一帧，不能证明目标节点应用层收到了，也不能证明目标节点就是我们期望的天空端或地面端。

排查时要按层次走：

1. CAN 物理/驱动层：接口是否 up、bitrate 是否一致、错误计数、是否 bus-off。
2. CAN 帧层：`candump` 是否能看到 report，CAN ID/扩展帧/DTID/source node 是否正确。
3. 应用层过滤：对端脚本是否监听正确 can 口，是否过滤错 DTID/node id。
4. payload 解析：MAC 长度、mac_type、tail byte、字节序是否正确。
5. ACK 发送路径：对端是否发 ACK，ACK CAN ID 是否正确，本端是否能收到。
6. 日志：脚本、服务、SocketCAN 错误、P401 dry-run 日志。

### 优化后的回答

CAN ACK 成功只能说明这帧在 CAN 总线上至少被某个节点正确接收，物理层和链路层大概率不是完全断的。但应用层 MAC ACK 没回来，说明 MAC exchange 的业务闭环没有完成。

我会按层排查。

第一层看 CAN 接口状态。用 `ip -details link show can0/can1` 看接口是否 up、bitrate 是否一致、是否 error-active、error-passive 或 bus-off，错误计数是否持续增加。

第二层用 `candump` 抓包，确认对端是否真的看到了我发出的 MAC report。重点看 CAN ID 是否符合预期，是否是扩展帧，DTID 是否是 MAC report，source node id 是否正确。

第三层看应用层脚本或服务。确认对端 MAC exchange 脚本是否启动，监听的是不是正确的 can 口，比如天空端是 `can0`，地面端是 `can1`。还要看过滤条件是否写错，比如只接收某个 DTID 或 node id，导致帧虽然到了内核，但应用层丢掉了。

第四层看 payload 解析。MAC 应该是 6 字节，`mac_type` 要正确，tail byte 要符合当前单帧格式。如果 MAC 格式错误或类型不对，对端可能不会回复 ACK。

第五层看 ACK 回包路径。对端可能已经发了 ACK，但本端没收到。这时要在两端同时 `candump`，看 ACK 是否从对端发出、CAN ID 是否是 ACK DTID、source node id 是否正确、本端过滤是否把 ACK 丢了。

最后看日志。比如脚本解析日志、SocketCAN 错误日志、服务启动日志、dry-run 日志。P401 图传连接失败可能影响后续真实配对，但如果只是 MAC report/ACK 阶段，首先还是要确认 CAN 报文和应用层解析是否闭环。

### 更简洁的面试版

> CAN ACK 成功只能说明总线上至少有节点 ACK 了这一帧，不能说明目标应用层已经处理。排查时我会先看 `ip -details link show can0/can1` 的状态、bitrate 和错误计数；再用 `candump` 确认对端能否看到 report，CAN ID、扩展帧、DTID、node id 是否正确；然后看对端脚本是否启动、监听的 can 口是否正确、过滤条件是否写错；再检查 payload 里的 6 字节 MAC、mac_type、tail byte；最后看 ACK 是否真的从对端发出、本端是否收到，并结合服务日志和 dry-run 日志定位。

### 可能追问

1. `ip -details link show can0` 里 ERROR-ACTIVE 是否一定代表链路正常？
2. 为什么两端都要同时 `candump`？
3. 如果对端能看到 report，但应用脚本没日志，可能是什么问题？
4. 如果对端发出了 ACK，本端收不到，怎么排查？
5. P401 图传连接失败和 MAC ACK 没回来是同一层问题吗？

---

## 第 38 轮：ERROR-ACTIVE 是否代表 CAN 链路正常

### 面试官问题

**`ip -details link show can0` 里显示 `ERROR-ACTIVE`，能不能说明 CAN 链路一定正常？为什么？**

### 你的原始回答要点

- 不一定。
- 还要看 RX 和 TX。
- 如果节点完全断开，自身也没有在发帧，也可能处于 `ERROR-ACTIVE`。

### 点评

这个回答可以给 85 分，核心正确。`ERROR-ACTIVE` 是 CAN 控制器的错误状态，说明当前错误计数还没有进入 error-passive 或 bus-off，但它不能证明链路已经正常通信。

关键点是：

1. 没有发送、没有接收时，错误计数可能不增长，也可能显示 `ERROR-ACTIVE`。
2. `ERROR-ACTIVE` 不代表对端在线，也不代表应用层能收到。
3. 真正判断链路正常，要看是否能收发对端已知帧。

### 优化后的回答

`ERROR-ACTIVE` 不能说明 CAN 链路一定正常。它只是 CAN 控制器错误状态机里的一个状态，表示当前发送/接收错误计数还在正常范围，没有进入 `ERROR-PASSIVE` 或 `BUS-OFF`。

如果节点完全断开，或者当前没有发送帧、也没有接收帧，错误计数可能不会变化，接口仍然可能显示 `ERROR-ACTIVE`。所以它只能说明控制器当前没有严重错误，不能证明物理链路、收发器、终端电阻、对端节点和应用层都正常。

更可靠的判断方式是：

1. 看 `RX/TX` 计数是否随着收发变化。
2. 看 `berr-counter tx/rx` 是否持续增长。
3. 用 `candump can0` 确认能收到对端已知帧。
4. 用 `cansend` 或应用报文确认对端能收到并回 ACK。
5. 两端同时抓包，看帧是否真正从一端发出并到达另一端。

在 P401 这个问题里，不能只因为 `ip -details link show can0` 显示 `ERROR-ACTIVE` 就判断天空端 CAN 链路恢复。更可靠的成功判据是天空端 `candump can0` 能正确看到地面端的已知帧，比如 MAC report、心跳或其他测试帧，并且应用层能解析和回复 ACK。

### 更简洁的面试版

> 不能。`ERROR-ACTIVE` 只是说明 CAN 控制器错误计数还没进入 error-passive 或 bus-off，不代表链路一定正常。如果节点断开但没有收发，错误计数也可能不变，仍然显示 ERROR-ACTIVE。判断链路要看 RX/TX 计数、错误计数是否增长，更重要的是用 `candump` 确认能收到对端已知帧，并用 ACK 或回包确认应用层闭环。

### 可能追问

1. CAN 的 error-active、error-passive、bus-off 分别是什么意思？
2. `berr-counter tx/rx` 怎么看？
3. 为什么 CAN 节点断开但不发送时不一定立刻报错？
4. 终端电阻错误会出现什么现象？
5. MCP2515 收不到帧时，除了 CAN 状态还要查什么？

---

## 第 39 轮：CAN error-active、error-passive、bus-off

### 面试官问题

**CAN 的 `error-active`、`error-passive`、`bus-off` 分别是什么意思？如果进入 `bus-off`，工程上一般怎么恢复？**

### 你的原始回答要点

- `error-active` 是正常状态，没有错误。
- `error-passive` 是已经有一定错误累计，降了一档。
- `bus-off` 说明错误过多，严重影响总线，节点下线。
- 工程上可以用 `ip link set can0 down`，或者 reboot、硬断电重启恢复。

### 点评

这个回答可以给 70 分，状态大方向对，但需要更准确。

关键修正：

1. `error-active` 不是“没有错误”，而是发送/接收错误计数还低于阈值，节点仍可以主动发送错误帧并正常参与总线。
2. `error-passive` 表示错误计数超过一定阈值，节点仍能通信，但错误处理更保守，对总线影响降低。
3. `bus-off` 表示发送错误计数过高，节点被强制从总线上脱离，不能继续收发，防止持续干扰总线。
4. 恢复时不建议优先硬断电，应先软件恢复和查根因。

### 优化后的回答

CAN 控制器内部有发送错误计数和接收错误计数，会根据错误计数进入不同状态。

`error-active` 是正常参与总线的状态，但不代表完全没有错误，只是错误计数还没有超过阈值。这个状态下节点可以正常收发，也可以发送主动错误帧。

`error-passive` 表示错误累计比较多，节点仍然可以收发，但它对总线的影响会被限制，错误帧行为更保守。这个状态说明链路已经不太健康，需要关注错误计数、接线、终端电阻、波特率等问题。

`bus-off` 是最严重的状态，通常是发送错误计数过高，说明节点持续发送失败或严重干扰总线。进入 bus-off 后，节点会被 CAN 控制器从总线通信中隔离，不能正常收发，避免继续影响其他节点。

工程上恢复 bus-off 可以有几种方式：

1. Linux SocketCAN 可以配置自动恢复，比如设置 `restart-ms`，让内核在指定时间后自动重启 CAN 控制器。
2. 手动恢复可以执行 `ip link set can0 down`，再重新配置 bitrate 并 `ip link set can0 up`。
3. 如果是外置 MCP2515/收发器异常，可能需要复位 MCP2515、重新初始化 SPI 驱动，严重时才考虑整机 reboot 或断电重启。

但恢复只是结果处理，真正要查根因。常见原因包括 bitrate 不一致、终端电阻不对、CAN_H/CAN_L 接反、共地问题、收发器供电异常、MCP2515 晶振/时钟配置错误、SPI 通信异常、采样点不合适等。

### 更简洁的面试版

> `error-active` 表示错误计数还在正常范围，节点能正常参与总线，不等于完全没错误；`error-passive` 表示错误累计较多，节点还能通信但错误处理更保守；`bus-off` 是发送错误过多后节点被隔离，不能正常收发，防止继续干扰总线。恢复上可以配置 SocketCAN 的 `restart-ms` 自动恢复，或者 `ip link set can0 down/up` 重新拉起，外置 MCP2515 还可能需要复位芯片。更重要的是查根因，比如 bitrate、终端电阻、接线、收发器供电、MCP2515 时钟和 SPI。

### 可能追问

1. `restart-ms` 是什么？
2. 进入 bus-off 后为什么不能只重启服务？
3. bitrate 不一致会看到什么现象？
4. 终端电阻应该是多少，为什么？
5. MCP2515 的时钟配置错了会影响什么？

---

## 第 40 轮：SocketCAN 命令配置、发送和抓包

### 面试官问题

**如果你要在 Linux 上把 `can0` 配成 500kbps 并启动，然后用命令发送和抓取一帧 CAN 报文，你会怎么操作？**

### 你的原始回答要点

```bash
ip link set can0 down
ip link set can0 type can bitrate 500000
ip link set can0 up
cansend can0 123#112233
candump can0
```

### 点评

这个回答可以给 90 分，命令主流程是正确的。

可以补充三个工程细节：

1. 配置完后用 `ip -details link show can0` 查看状态、bitrate、错误计数。
2. 可以加 `restart-ms`，让 bus-off 后自动恢复。
3. `123#112233` 是标准帧；P401 项目里的 `07200302#...` 这类 8 位 CAN ID 是扩展帧。

### 优化后的回答

配置 `can0` 为 500kbps，可以这样操作：

```bash
ip link set can0 down
ip link set can0 type can bitrate 500000 restart-ms 100
ip link set can0 up
ip -details link show can0
```

其中 `restart-ms 100` 表示如果进入 bus-off，内核可以在 100ms 后尝试自动恢复。`ip -details link show can0` 用来确认接口是否 up、bitrate 是否正确、错误状态和错误计数是否正常。

抓包：

```bash
candump can0
```

发送一帧标准 CAN 帧：

```bash
cansend can0 123#112233
```

这里 `123` 是标准帧 ID，`112233` 是 3 字节 payload。

如果发送扩展帧，使用 8 位十六进制 ID，例如 P401 MAC report 类似：

```bash
cansend can0 07200302#AABBCCDDEEFF01C0
```

其中 `07200302` 是扩展 CAN ID，payload 里可以放 6 字节 MAC、MAC 类型和 tail byte。

### 更简洁的面试版

> 我会先 `ip link set can0 down`，再 `ip link set can0 type can bitrate 500000 restart-ms 100`，然后 `ip link set can0 up`，用 `ip -details link show can0` 看状态。抓包用 `candump can0`，发标准帧可以用 `cansend can0 123#112233`。如果是 P401 这种扩展帧，就用 8 位 ID，比如 `cansend can0 07200302#AABBCCDDEEFF01C0`。

### 可能追问

1. `cansend can0 123#112233` 里 `123` 和 `112233` 分别是什么？
2. 标准帧和扩展帧有什么区别？
3. CAN payload 最多几个字节？
4. `restart-ms` 是否能解决所有 bus-off 问题？
5. 如果 `candump` 看不到自己发的帧，可能是什么原因？

---

## 第 41 轮：CAN 标准帧、扩展帧和 payload 长度

### 面试官问题

**CAN 标准帧和扩展帧有什么区别？CAN 经典帧 payload 最多几个字节？为什么你的 MAC report 可以刚好放进一帧？**

### 你的原始回答要点

- 标准帧是 11 位 ID，范围 `0x000-0x7FF`。
- 扩展帧是 29 位 ID，范围 `0x00000000-0x1FFFFFFF`。
- CAN 经典帧 payload 最多 8 字节。
- MAC report 刚好设计成 8 字节。
- 6 字节 MAC + 1 字节类型 + 1 字节尾字节。

### 点评

这个回答可以给 90 分，基本准确。

补充一个容易被追问的点：

> 扩展帧只是仲裁 ID 从 11 位扩展到 29 位，不代表 payload 变长。经典 CAN 不管标准帧还是扩展帧，payload 最多都是 8 字节。CAN FD 才支持更长 payload。

### 优化后的回答

CAN 标准帧和扩展帧主要区别在仲裁 ID 长度。标准帧是 11 位 ID，范围是：

```text
0x000 - 0x7FF
```

扩展帧是 29 位 ID，范围是：

```text
0x00000000 - 0x1FFFFFFF
```

但扩展帧只是 ID 更长，不代表数据区更长。经典 CAN 中，不管标准帧还是扩展帧，payload 最多都是 8 字节。如果要超过 8 字节，需要 CAN FD 或者应用层分包。

P401 MAC report 之所以可以单帧发送，是因为 MAC 地址本身是 6 字节，我们把 payload 设计成刚好 8 字节：

```text
Data[0..5] = tun MAC，6 字节
Data[6]    = mac_type，比如 0x01 表示 TUN MAC
Data[7]    = DroneCAN single-frame tail byte，比如 0xC0 | transfer_id
```

所以一个 MAC report 或 ACK 可以放在一帧经典 CAN 扩展帧里，不需要应用层分包，解析和抓包都比较简单。

### 更简洁的面试版

> 标准帧是 11 位 ID，范围 `0x000-0x7FF`；扩展帧是 29 位 ID，范围 `0x00000000-0x1FFFFFFF`。扩展帧只是 ID 更长，经典 CAN 的 payload 仍然最多 8 字节。我们的 MAC report 是 6 字节 MAC + 1 字节 mac_type + 1 字节 tail byte，刚好 8 字节，所以能放进一帧，不需要分包。

### 可能追问

1. CAN FD 和经典 CAN 有什么区别？
2. 如果 payload 超过 8 字节，应用层怎么分包？
3. 扩展帧 ID 里为什么可以编码 DTID 和 source node id？
4. CAN 仲裁 ID 越小优先级越高，这对你的报文有什么影响？
5. MAC report 为什么不用标准帧？

---

## 第 42 轮：CommRouter 整体架构和消息路由

### 面试官问题

**请介绍 RK3562 用户态 `CommRouter` 的整体架构：它连接哪些通信端点，为什么需要统一路由，一条 MAVLink 消息从收到到转发大致经过哪些步骤？**

### 本轮学习情况

- 本轮没有独立作答，先进入讲解。
- 当前能确认的项目边界是：参与 `CommRouter`、`PeripheralManager`、`LinkController` 的用户态通信链路开发和联调，不能表述为完整 Android BSP 或 APK 独立开发。

### 讲解后的回答框架

`CommRouter` 可以理解为 RK3562 上的用户态通信路由服务。它统一管理 APK UDP、P401 TUN UDP、UART MAVLink 和本地 UDS 外设消息等通信入口，避免每个模块各自维护一套互相耦合的转发逻辑。

主循环通过 `poll` 同时等待多个 fd。某个 fd 可读后，服务读取数据并根据入口采用对应的处理方式：UART 是字节流，需要接收缓冲和 MAVLink 解析状态；UDP 保留 datagram 边界；UDS 用于本机进程间消息。解析成功后，再依据消息来源、类型和路由策略转发到 APK 或 P401，并记录收发、解析失败和路由丢弃等日志。

`PeripheralManager` 更偏外设输入管理，例如读取按键、识别短按/长按并构造 `COMMAND_LONG`；`CommRouter` 负责把这类消息接入统一路由，不能把所有外设逻辑都说成由 `CommRouter` 完成。

### 更简洁的面试版

> `CommRouter` 是 RK3562 上的用户态通信路由服务，通过 `poll` 同时处理 APK UDP、P401 TUN UDP、UART MAVLink 和本地 UDS 等多个 fd。收到数据后，它先按入口完成读取和协议解析，再根据来源、消息类型和目标端执行路由，把消息转发到 APK 或 P401。UART 侧还要处理半帧和粘包，UDP 侧利用 datagram 边界。我主要参与这条用户态通信链路的开发和联调，并通过串口日志、logcat 和 tcpdump 验证各层收发。

### 可能追问

1. 为什么 `CommRouter` 使用 `poll`，而不是每个通道一个线程？
2. UART 和 UDP 的消息边界有什么区别？
3. 如何防止消息在 APK、P401 和 UART 之间形成转发回环？
4. `PeripheralManager` 和 `CommRouter` 的职责边界是什么？

---

## 第 43 轮：为什么使用 poll，而不是每个通道一个线程

### 面试官问题

**`CommRouter` 同时处理 UART、UDP 和 UDS，为什么使用 `poll`？每个通信通道创建一个线程是否也可以？**

### 本轮学习情况

- 本轮没有独立作答，先进入讲解。
- 已建立核心认识：`poll` 解决的是多个 fd 的统一等待，不负责协议解析和消息路由。

### 讲解后的回答框架

如果按顺序对 UART、UDP、UDS 执行阻塞 `read`，程序可能卡在当前没有数据的通道，无法及时处理其他通道。`poll` 可以让一个线程同时等待多个 fd，返回后只处理实际发生事件的通道。

每个通道一个线程也能实现，但多个线程共享路由表、连接状态和发送资源时，需要 mutex、消息队列和生命周期管理，还会增加竞态、死锁、顺序不稳定和调试复杂度。当前项目的 fd 数量有限，单次处理以轻量级收发和转发为主，因此单线程事件循环更容易控制。

`poll` 的限制是处理函数不能长时间阻塞；CPU 密集任务或慢操作需要移出事件循环。fd 数量很大时，可以考虑 `epoll`。此外，`poll` 本身不等于非阻塞 IO，必要时仍要配合 `O_NONBLOCK`。

### 更简洁的面试版

> `poll` 让一个线程同时等待 UART、UDP、UDS 等多个 fd，避免阻塞在某一个 `read` 上。多线程也能实现，但会增加共享路由状态的加锁、竞态和调试复杂度。这个项目的 fd 数量有限、单次处理较轻，所以单线程 `poll` 事件循环更合适；但处理函数不能长时间阻塞，重任务需要交给工作线程。

### 可能追问

1. `poll` 和非阻塞 IO 是什么关系？
2. `poll`、`select`、`epoll` 有什么区别？
3. 如果某个 fd 的处理耗时 500ms，会发生什么？

---

## 第 44 轮：pollfd 的 fd、events 和 revents

### 面试官问题

**`poll()` 返回大于 0 后，如何判断哪个 fd 发生了事件？`fd`、`events`、`revents` 分别表示什么？如何处理 `POLLIN`、`POLLERR` 和 `POLLHUP`？**

### 本轮学习情况

- 本轮没有独立作答，先进入讲解。
- 已理解返回后需要遍历 `pollfd` 数组，并用位与检查 `revents`。

### 讲解后的回答框架

`fd` 是被监控的文件描述符；`events` 是程序希望内核监控的事件；`revents` 是内核返回的实际事件。`poll` 返回大于 0 后要遍历整个数组，跳过 `revents == 0` 的项。

- `POLLIN`：有数据可读，调用对应的 `read`、`recv` 或 `recvfrom`。
- `POLLERR`：通道出错，记录具体错误并决定是否重建。
- `POLLHUP`：对端关闭或设备断开，应处理可能存在的残留数据，再关闭并移除 fd。
- `POLLNVAL`：fd 无效，通常是生命周期管理错误。

`revents` 是位掩码，一个 fd 可能同时出现 `POLLIN | POLLHUP`，所以不能只用互斥的 `else if`。`poll` 的正返回值表示有非零 `revents` 的 fd 数量，不是事件位的总数。

### 更简洁的面试版

> `fd` 是监控对象，`events` 是请求监听的事件，`revents` 是实际发生的事件。`poll` 返回后我会遍历整个数组，用位与检查 `revents`：`POLLIN` 就读取数据，`POLLERR` 记录错误并判断是否重建通道，`POLLHUP` 处理残留数据后关闭并移除 fd。多个事件可能同时发生，所以要分别检查各事件位。

### 可能追问

1. `poll()` 返回 0 和返回 -1 分别表示什么？
2. `errno == EINTR` 时应该怎么处理？
3. 为什么 `POLLIN` 和 `POLLHUP` 可能同时出现？

---

## 第 45 轮：UDP 的 bind、recvfrom 和 sendto

### 面试官问题

**请说明 UDP socket 从创建到收发数据的基本流程。`bind()`、`recvfrom()`、`sendto()` 分别有什么作用？UDP 客户端发送数据前是否一定要调用 `bind()`？**

### 本轮学习情况

- 本轮没有独立作答，先进入讲解。
- 已理解接收方通常绑定固定端口，只主动发送的一方可以由内核自动分配临时源端口。

### 讲解后的回答框架

先使用 `socket(AF_INET, SOCK_DGRAM, 0)` 创建 UDP socket。`bind` 把 socket 绑定到本地 IP 和端口，便于对端向固定端口发送；`recvfrom` 接收一个 UDP datagram，同时返回发送方 IP 和端口；`sendto` 把一个 datagram 发往指定目标地址。

只主动发送的程序不一定显式 `bind`，第一次发送时内核可以选择合适的本地 IP 和临时端口。如果协议要求固定源端口、需要在固定端口接收、需要绑定特定本地 IP，才必须显式绑定。

UDP 保留报文边界，但接收 buffer 太小时，超出部分可能被截断并丢弃，不能像字节流一样在下一次 `recvfrom` 中继续读取。

### 更简洁的面试版

> UDP 先通过 `socket(AF_INET, SOCK_DGRAM, 0)` 创建 fd。`bind` 用于绑定本地 IP 和端口，`recvfrom` 接收一个 datagram 并取得发送方地址，`sendto` 向指定目标地址发送一个 datagram。只主动发送的客户端不一定显式绑定，内核可以自动分配临时源端口；需要固定接收端口、固定源端口或指定本地接口时才显式 `bind`。

### 可能追问

1. `INADDR_ANY` 表示什么？
2. UDP 接收 buffer 太小会怎样？
3. UDP 调用 `connect` 后是否就变成可靠连接？

---

## 第 46 轮：UDP 不可靠，为什么项目仍然使用它

### 面试官问题

**UDP 不保证到达、顺序和不重复，为什么 `CommRouter` 仍使用 UDP 和 APK、P401 通信？关键控制消息如何保证可靠性？**

### 本轮学习情况

- 本轮没有独立作答，先进入讲解。
- 已能区分“最新值优先”的实时数据和“必须确认执行”的关键命令。

### 讲解后的回答框架

遥测状态、摇杆和视频等数据更重视低延迟和最新值，旧包丢失后通常由下一次周期上报覆盖。UDP 无连接、保留消息边界，单个报文丢失不会等待重传后再交付后续新报文，因此适合这类实时数据。

关键控制命令不能只依赖 UDP，需要在应用层加入 `request_id`、ACK、timeout、retry 和接收端去重。发送端维护 pending 请求并在超时后有限重试；接收端发现重复 `request_id` 时不能重复执行非幂等操作，而应返回缓存的处理结果。ACK 还要区分“已收到”和“执行完成”。

具体到 MAVLink，可以使用 `COMMAND_LONG` 和 `COMMAND_ACK` 表达命令及结果。`CommRouter` 主要负责路由，可靠性语义应由业务协议和通信端点共同保证。

### 更简洁的面试版

> 状态、遥测和视频更看重低延迟和最新数据，偶尔丢帧可由下一次上报覆盖，所以适合 UDP。开关机、配置写入等关键命令则需要在应用层增加 `request_id`、ACK、超时重试和去重，避免 ACK 丢失后重复执行。MAVLink 命令可以使用 `COMMAND_LONG` 和 `COMMAND_ACK` 做结果确认，`CommRouter` 主要负责路由，不应把所有可靠性都归到路由层。

### 可能追问

1. ACK 丢失但命令已经执行时怎么办？
2. `seq` 和 `request_id` 的职责有什么区别？
3. 为什么实时状态通常不重传旧数据？

---

## 第 47 轮：Android init 服务启动失败的分层排查

### 面试官问题

**RK3562 开机后 `CommRouter` 没有正常运行，如何从进程状态、init 配置、日志、设备节点、普通权限和 SELinux 分层排查？**

### 本轮学习情况

- 本轮没有独立作答，先进入讲解。
- Android init 和 SELinux 仍是重点薄弱项，需要后续闭卷复述和命令练习。

### 讲解后的回答框架

先用 `getprop init.svc.comm_router` 和 `ps -A` 区分服务没启动、启动后退出、反复重启和进程存在但功能异常。然后通过 `logcat -b all`、`dmesg` 和 tombstone 查找退出码、动态库、段错误、设备和权限日志。

接着核对 `/vendor/etc/init/*.rc` 中的 service 名称、二进制路径、class、user、group、参数、`disabled/oneshot` 和启动顺序。再检查二进制、配置文件、`/dev/ttyS3`、`can1`、TUN 和网络接口是否存在并准备完成。

设备存在但打不开时，比较设备 owner/group/mode 与服务运行用户；普通权限正确仍报 `Permission denied` 时，检查 `avc: denied`、进程 domain 和设备 label。正式方案应修正标签或最小 SELinux policy，不能把关闭 SELinux 当作修复。

### 更简洁的面试版

> 我先用 `getprop init.svc.xxx` 和 `ps -A` 判断服务是未启动、反复重启还是进程存在但异常，再查 `logcat -b all`、`dmesg` 和 tombstone。随后核对 init `.rc` 的路径、class、user/group、参数和启动顺序，并检查 `/dev/ttyS3`、`can1`、TUN 和网络是否准备好。设备存在但打不开时先查普通权限，再查 `avc: denied`、进程 domain 和设备 label，最终修正权限或 SELinux policy。

### 可能追问

1. `running` 是否代表服务功能一定正常？
2. init 服务名和进程名为什么可能不同？
3. 普通权限正确但仍然 `Permission denied` 的原因是什么？

---

## 第 48 轮：进程运行但 APK 收不到 MAVLink 的分层排查

### 面试官问题

**`CommRouter` 进程处于 running，但 APK 收不到 MAVLink。如何判断问题在 UART 接收、协议解析、路由逻辑、UDP 发送还是 APK 接收端？**

### 本轮学习情况

- 本轮没有独立作答，先进入讲解。
- 已建立按数据流逐层取证的排查框架，后续需要独立复述。

### 讲解后的回答框架

先通过 `/proc/<pid>/fd` 和启动日志确认串口及 UDP socket 已成功打开。然后检查 UART 接收字节计数和最近接收时间；完全没有数据时检查设备节点、串口参数、pinmux 和物理信号。注意不要在服务运行时用另一个进程直接读取串口，以免抢走数据。

原始字节存在时，比较 MAVLink 成功帧、CRC 错误和解析丢弃计数，确认半帧、粘包和解析状态是否正确。解析成功后记录来源、sysid、compid、msgid、路由结果和具体丢弃原因，检查路由表、去重和防回环规则。

发送阶段检查 `sendto` 返回值、目标 IP/端口和路由，再使用 `tcpdump -ni any -X udp port <port>` 确认 UDP 包是否真正离开 RK。RK 出口没有包说明问题仍在服务或本机路由；RK 有包而 APK 设备没有，检查网络；APK 设备能抓到但应用没有处理，则检查 APK 的 socket、生命周期和消息过滤。

### 更简洁的面试版

> 我会沿 UART 字节、MAVLink 完整帧、路由命中、UDP 发包和 APK 接收五层排查。先确认 `/proc/pid/fd` 中串口和 socket 都已打开，再看 UART 字节计数和 MAVLink 成功帧计数。解析成功后检查来源、msgid 和路由丢弃原因，然后检查 `sendto` 返回值并用 `tcpdump` 确认包是否离开 RK。RK 能抓到而 APK 没收到就查网络；APK 设备能抓到但应用没处理，就查 APK socket、生命周期和过滤逻辑。

### 可能追问

1. 为什么不能在服务运行时直接 `cat /dev/ttyS3`？
2. 原始字节增长但完整帧计数为零说明什么？
3. `sendto` 成功是否能证明 APK 已收到？
4. 如何用计数器和时间戳减少大量日志对实时性的影响？

---

# 第二阶段：基于最新版两页简历的压力面试

本阶段从第 49 轮开始，直接按照最新版《机器人 / 无人机嵌入式软件工程师》简历逐条追问。和前 48 轮不同，本阶段不预先给出完整参考答案，避免把“看过讲解”误判成“能够独立回答”。

## 本阶段答题规则

- 每次只回答一轮，建议控制在 90 秒至 3 分钟。
- 不看简历、不搜索、不翻之前的标准答案，先用自己的语言回答。
- 不确定的项目参数要明确说“记不清，需要回查”，不能临场编造。
- 回答顺序尽量保持：项目背景 → 我的职责 → 方案选择 → 异常处理 → 验证结果。
- 每轮按 100 分评分：事实准确 30 分、技术原理 30 分、工程取舍 20 分、职责边界与表达 20 分。
- 低于 70 分需要重答；70～79 分属于基本能答；80～89 分能够应对常规追问；90 分以上才算能够扛连续深挖。

---

## 第 49 轮：最新版简历总述与个人职责边界

### 面试官问题

**请用两分钟介绍你在无人机和手持发射器项目中做过的工作。哪些模块是你独立完成的，哪些是参与联调，哪些方案由高级工程师给出了总体框架？**

### 本轮状态

- 待闭卷作答。
- 这是最新版简历的开场题，后续问题会完全根据本轮回答选择追问方向。

### 评分重点

- 能否把手持端、天地端、系统固件和Camera四条经历组织成一条清晰主线。
- 能否明确“主责STM32与升级”“参与RK用户态服务和系统联调”的边界。
- 是否避免把整个Android系统、APK、P401、Camera和产线工具都说成独立完成。
- 是否能给出两个最有代表性的量化结果，并说明测量起点和终点。

### 高危失分点

- 按简历顺序逐字背诵，没有产品架构和个人贡献主线。
- 使用“全部负责”“从零完成”等无法支撑的表述。
- 同时抛出过多名词，却讲不出一个具体故障闭环。

### 可能追问

1. 你做得最深的模块是哪一个？为什么？
2. 如果删掉高级工程师的框架和指导，你还能独立完成哪些部分？
3. 哪一项结果有完整日志或真机记录可以支撑？

---

## 第 50 轮：FreeRTOS任务级健康监控与IWDG喂狗

### 面试官问题

**简历写“由关键任务运行状态决定喂狗”。请具体说明关键任务如何上报健康、监督任务如何判断超时，以及为什么不能在定时器中断中无条件刷新IWDG。**

### 本轮状态

- 待闭卷作答。
- 旧模拟中相关问题曾约55分，是当前最需要补强的MCU知识点。

### 评分重点

- 能否说明心跳标志、计数器或时间戳方案，以及各自优缺点。
- 能否解释“中断仍运行但业务任务已死锁”时无条件喂狗会掩盖故障。
- 能否说明启动宽限期、最大允许心跳年龄和复位原因记录。
- 能否区分关键业务任务与非关键日志任务，避免任何小故障都触发整机复位。

### 可能追问

1. `HAL_GetTick()`溢出回绕后，超时判断还能否正确工作？
2. 如果监督任务自身得不到调度，IWDG会怎样？
3. 高优先级任务一直占用CPU时，低优先级监督任务如何保证运行？
4. 为什么选择IWDG而不是WWDG？

---

## 第 51 轮：手持端256 KB Flash的A/B分区

### 面试官问题

**简历写 STM32F103RCT6 的 256 KB Flash 被划分为 Bootloader、升级状态区和两个 APP 分区。请给出实际 Flash 布局、分区对齐方式和两个 APP 的链接地址。**

### 本轮状态

- 待补充真实项目参数后作答。
- 本人已确认公司项目为双 APP 分区；工作区中的旧单 APP 原型不是同一个实现，面试时必须主动说明工程阶段或来源差异。

### 评分重点

- 能否说出Bootloader、metadata、slot A、slot B的准确起止地址。
- 是否知道STM32F103RCT6属于高密度器件，Flash页大小与分区对齐必须一致。
- 能否解释两个APP是分别链接到不同地址，还是由Bootloader做地址映射。
- 能否用链接脚本或 map 文件说明每个区域的实际大小，并确认所有边界按 2 KB Flash 页对齐。

### 高危失分点

- 只会回答“两个 APP 分区”，不知道任何十六进制地址或镜像上限。
- 把旧工程的`0x08004000`单APP布局直接套到双分区。
- 不知道擦除页大小，给出无法对齐的分区边界。

### 可能追问

1. 两个APP槽位是否需要生成两份链接地址不同的bin？
2. 中断向量表和VTOR如何随当前槽位变化？
3. 如果 APP 增长超过槽位上限，构建阶段如何阻止越界？

---

## 第 52 轮：A/B metadata、试启动和断电一致性

### 面试官问题

**请设计A/B升级的metadata状态机，并分别说明升级写入中断电、写完未切换时断电、pending镜像首次启动失败、写confirmed时断电会发生什么。**

### 本轮状态

- 待闭卷作答。
- 旧模拟对metadata断电保护约65分，需要从“知道状态名”提升到“能处理原子性”。

### 评分重点

- 状态至少能区分active、pending、confirmed、invalid或等价含义。
- metadata不能只有单份；应考虑双副本、递增序号、CRC和选择最新有效记录。
- 新镜像写完且CRC正确后应先试启动，不能立即覆盖旧镜像的confirmed状态。
- confirmed条件不能只是进入`main()`，应包含调度器、关键外设或通信自检。

### 可能追问

1. 启动计数保存在哪里，Flash寿命如何处理？
2. 看门狗复位一次就回滚是否合理？
3. metadata双副本都无效时Bootloader如何选择？
4. 如何防止旧版本回滚攻击？

---

## 第 53 轮：UART升级约一分钟与120秒等待窗口

### 面试官问题

**UART 升级单轮端到端约 1 分钟，但 Bootloader 最长等待 120 秒。请拆解完整时序，并根据实际镜像大小和串口波特率估算理论最短传输时间。**

### 本轮状态

- 待闭卷作答。

### 评分重点

- 能区分“Bootloader等待RK启动的窗口”和“实际固件传输时间”。
- 能进行量级估算：115200 8N1通常每字节约10 bit，纯传输吞吐约11.5 KB/s。
- 能把MCU复位、RK重新上电、Android/Linux启动、服务启动、握手、擦除、发送、校验和再次复位分开。
- 能说明超时后旧APP有效与无有效APP时的不同处理策略。

### 可能追问

1. 为什么不把波特率提升到460800？
2. 擦除Flash期间串口数据继续到达怎么办？
3. ACK表示已收到升级命令，还是Bootloader已准备好接收固件？
4. 如果RK在第119秒才开始发送，Bootloader如何处理窗口边界？

---

## 第 54 轮：驱动板128 KB Flash的A/B布局

### 面试官问题

**驱动板使用 STM32F103CBT6，128 KB Flash 中有两个 52 KB APP 分区。请给出剩余 24 KB 如何分配，并说明 1 KB 擦除页下的准确地址规划。**

### 本轮状态

- 待补充真实地址后作答。

### 评分重点

- 能把128 KB换算成地址范围，并按1 KB页对齐。
- 能说明Bootloader代码、metadata、配置参数或保留区各占多少页。
- 能说明只擦写非活动槽位，Bootloader区域有写保护或边界检查。
- 能说明 52 KB 槽位是否包含镜像头，以及链接脚本如何限制尺寸。

### 可能追问

1. 为什么选择两个 52 KB 分区，剩余 24 KB 是否全部属于 Bootloader？
2. metadata放在Bootloader区尾部还是独立页？
3. 两个槽位链接地址不同，如何维护构建和版本发布？

---

## 第 55 轮：500 kbit/s CAN升级为何需要约20秒

### 面试官问题

**约 52 KB 固件通过 500 kbit/s 经典 CAN 升级约 20 秒。请说明每帧的数据格式、真实固件载荷、ACK 粒度和重传策略，并解释为什么不能直接用 52 KB 除以 500 kbit/s 得出升级时间。**

### 本轮状态

- 待闭卷作答。
- 这是简历量化数据最容易被计算追问的一题。

### 评分重点

- 知道经典CAN单帧最多8字节，固件有效payload还要扣除命令、序号或长度字段。
- 能说明CAN帧本身还有仲裁、控制、CRC、ACK、EOF、帧间隔和位填充开销。
- 能说明是逐帧ACK、分块ACK还是窗口发送，以及该设计如何影响吞吐。
- 能把Flash擦除/半字写入、任务调度、超时重传和脚本等待计算在内。

### 高危失分点

- 直接说500 kbit/s等于每秒500 KB。
- 不知道每帧实际有几个固件数据字节。
- 说实现了ACK/重传，却不知道ACK超时和重试次数。

### 可能追问

1. 如果每帧只有4字节固件数据，需要多少帧？
2. 为什么不使用CAN FD？
3. 如何处理ACK丢失但数据帧已经写入的情况？
4. 如何检测重复帧和乱序帧？

---

## 第 56 轮：CAN接收、Flash擦写与流控

### 面试官问题

**STM32擦除或写Flash时可能影响CPU取指和中断响应。升级过程中如何避免CAN FIFO溢出、丢帧或把同一数据写两次？**

### 本轮状态

- 待闭卷作答。

### 评分重点

- 能说明先擦除目标槽位还是按页擦除，以及擦除期间发送端必须等待明确状态。
- 能说明CAN ISR只做快速收包/入队，Flash写入由状态机处理，或采用严格的请求-响应流控。
- 能解释序号检查、重复ACK、幂等写入和写后校验。
- 能说明错误发生时不更新active slot，旧分区保持可启动。

### 可能追问

1. Flash写入最小粒度是什么？地址未对齐会怎样？
2. CAN硬件FIFO有几级，溢出后如何观察？
3. 是对每帧做CRC，还是只做整包CRC32？两者分别解决什么？

---

## 第 57 轮：约5秒自动对频的时序和成功判据

### 面试官问题

**你说天地端约5秒自动对频。请从硬件接触成功开始，按时间顺序说明CAN触发、MAC交换、ACK、网络配置、视频建立和地面端显示分别发生了什么。**

### 本轮状态

- 待闭卷作答。

### 评分重点

- 起点明确为物理链路/硬件接触成功，终点明确为飞机视频出现在地面终端屏幕。
- 能解释MAC report、应用层ACK、重复报文去重和配对命令之间的关系。
- 能说明每阶段的日志、抓包或界面判据，而不是只看最终“有画面”。
- 能说明数十轮测试的样机版本、轮数、失败数和异常恢复方式。

### 可能追问

1. 五秒里最耗时的是哪一阶段？
2. 如果MAC ACK成功但视频没出来，如何继续分层定位？
3. 接触抖动导致多次触发时，如何区分同一次配对过程？
4. 为什么不能只用CAN链路层ACK判断配对成功？

---

## 第 58 轮：旧dry-run记录与最终成功结果的解释

### 面试官问题

**你之前的记录显示地面端只完成dry-run，天空端外置CAN控制器接收还不稳定；现在简历写数十轮均成功。中间具体修复了什么，最终验证相比旧阶段增加了哪些步骤？**

### 本轮状态

- 待本人补充后续真实工作记录。
- 这是核对简历真实性的压力题，不允许使用推测性答案。

### 评分重点

- 能主动区分“旧阶段验证边界”和“后续完整闭环”，不回避历史问题。
- 能说出实际根因、修改内容、验证方法和最终数据。
- 如果真实pairing接口或天空端服务不是本人实现，要明确自己的负责范围。
- 无法回忆时应诚实说明需要回查，而不是编造MCP2515参数或修复结论。

### 可能追问

1. 最终根因是硬件、电气、驱动、采样点还是应用服务？
2. 如何证明不是偶然成功？
3. 断开后再次接触是否清理了旧状态？

---

## 第 59 轮：Rockchip系统启动链与镜像关系

### 面试官问题

**请说明RK3588从上电到进入Linux用户态的大致启动链。U-Boot、Kernel、DTB、boot.img、rootfs和userdata分别起什么作用？**

### 本轮状态

- 待闭卷作答。

### 评分重点

- 能从BootROM/引导介质讲到SPL或loader、U-Boot、Kernel、DTB和rootfs。
- 能说明DTB描述硬件，Kernel根据设备树进行驱动匹配和probe。
- 能区分boot分区、根文件系统分区和用户数据分区。
- 能结合实际`build.sh`产物说明自己构建、打包和烧录过什么。

### 可能追问

1. 修改设备树后为什么通常不需要重做rootfs？
2. Kernel找不到rootfs时可能出现什么日志？
3. U-Boot通过哪些参数把root设备告诉Kernel？

---

## 第 60 轮：RTL8188EUS外部内核模块适配

### 面试官问题

**请完整说明你如何为目标RK3588内核交叉编译RTL8188EUS驱动，并处理模块版本、依赖、安装路径和启动加载。**

### 本轮状态

- 待闭卷作答。

### 评分重点

- 能说明目标内核配置与构建产物必须准备好，外部模块通常通过内核`M=`机制构建。
- 能解释`ARCH`、`CROSS_COMPILE`和目标内核源码/输出目录。
- 能解释`vermagic`、`modinfo`、`insmod`、`modprobe`和`depmod`。
- 能说明模块加载成功但未出现网卡时，继续检查USB枚举、firmware、dmesg和接口状态。

### 可能追问

1. `Invalid module format`常见原因是什么？
2. `insmod`和`modprobe`有什么区别？
3. 把`.ko`复制进rootfs后为什么可能还需要`depmod`？
4. 驱动编译警告能否全部忽略？

---

## 第 61 轮：iperf验证TCP/UDP超过20 Mbit/s

### 面试官问题

**你如何使用iperf分别测试TCP和UDP？请给出服务端、客户端命令，说明测试方向、时长、UDP目标带宽，以及为什么UDP结果不能只看吞吐。**

### 本轮状态

- 待本人回忆实际使用的是iperf2还是iperf3，并补充真实命令。

### 评分重点

- 能说明server/client部署在哪一端，测试上行还是下行。
- TCP结果关注吞吐和重传；UDP还必须看丢包、乱序和jitter。
- 能说明测试时长、距离、信号环境和多轮重复方式。
- 能解释“超过20 Mbit/s”是最低值、平均值还是某次峰值。

### 高危失分点

- 不知道自己用的是iperf还是其他`ip`开头工具。
- 把UDP的`-b`目标发送带宽当成实际接收吞吐。
- 无法说明丢包率，仍声称UDP链路稳定。

### 可能追问

1. 为什么TCP吞吐可能低于链路物理速率？
2. UDP目标带宽逐步增加时如何找到稳定上限？
3. Wi-Fi测试如何排除CPU或存储瓶颈？

---

## 第 62 轮：rootfs 裁剪与统计口径

### 面试官问题

**你为什么在最新版简历中只写 rootfs 裁剪，而没有保留“缩减约 500 MB”？你原来比较的可能是解包目录、原始镜像、稀疏镜像还是压缩文件，应该如何重新测量？**

### 本轮状态

- 500 MB 已从简历删除；在统计口径确认前不得把它当成已验证事实。
- 当前应能讲清裁剪对象、依赖处理和回归验证，不需要强行给出体积数字。

### 评分重点

- 前后必须采用相同镜像格式、文件系统参数和统计命令。
- 能说明Qt组件、系统包、缓存/文档/locale等具体裁剪对象及依赖分析方法。
- 能说明同时新增ROS依赖、Wi-Fi和TUN后，比较的是净变化还是只统计删除量。
- 能给出启动、网络、ROS 依赖、Wi-Fi/TUN 模块和脚本等回归测试清单。

### 可能追问

1. ext4镜像文件变小是否等于实际文件内容减少？
2. 如何发现动态库被误删？
3. 为什么只运行一次程序不能证明rootfs裁剪安全？

---

## 第 63 轮：SD一键烧录与多设备产线升级

### 面试官问题

**请说明SD卡一键烧录如何组织各分区镜像；如果一台电脑同时升级多台硬件，如何识别设备、控制并发、执行自检并生成可追溯结果？**

### 本轮状态

- SD 整机烧录、多设备升级和脚本自检已通过产线考核；当前需要补齐考核日期、设备数量、自检项和原始结果。

### 评分重点

- 能说明升级卡制作、分区映射、烧录成功判据和断电风险。
- 多设备场景能通过USB拓扑、序列号、ADB/USB设备ID或网口地址建立唯一身份。
- 能说明并发限制、单设备状态机、超时、重试、失败隔离和日志目录。
- 自检至少覆盖版本、关键设备节点、网络、CAN/UART 和项目实际功能。

### 可能追问

1. 如何防止把镜像写到电脑系统盘？
2. 多台设备同时重启后，设备标识变化怎么办？
3. “脚本执行成功”与“产品功能通过”有什么区别？
4. “通过产线考核”的考核对象、成功判据和可追溯记录分别是什么？

---

## 第 64 轮：RK3588 CAN 设备树时钟修改

### 面试官问题

**你排查 RK3588 外置 CAN 控制器收帧异常时，为什么把设备树参考时钟修正为 25 MHz？请画出 `fixed-clock`、SPI 控制器、外置 CAN 控制器和 CAN 收发器之间的关系。**

### 本轮状态

- 待补齐修改前后日志后闭卷作答。

### 评分重点

- 能说明 25 MHz 必须来自板上实际晶振或原理图依据，不是凭经验选择。
- 能说明 `fixed-clock` 是时钟提供者，CAN 控制器节点通过 `clocks` 引用它。
- 能区分 SPI 控制器、CAN 控制器与物理层收发器的职责。
- 能说明参考时钟错误会使驱动计算出的 CAN 位时序与实际总线不一致。

### 可能追问

1. 修改的是 DTS、DTSI 还是 overlay，最终生成哪个 DTB 或镜像？
2. 两路 `xl2515` 如何区分片选、中断 GPIO 和接口名？
3. probe 成功为什么仍不能证明 CAN 链路正常？

---

## 第 65 轮：25 MHz 时钟与 500 kbit/s CAN 位时序

### 面试官问题

**外置 CAN 控制器参考时钟为 25 MHz、总线标称位速率为 500 kbit/s。请说明 BRP、TSEG1、TSEG2、SJW 和 sample point 的作用，以及时钟配置错误会出现什么现象。**

### 本轮状态

- 待闭卷作答，不要求死记某组寄存器值，但必须会解释计算关系。

### 评分重点

- 知道一个 CAN bit 由同步段、传播段、相位段和采样点组成。
- 能说明驱动依据输入时钟、BRP 和时间量子数量得到实际 bitrate。
- 能解释 bitrate 偏差可能导致错误帧、错误计数上升、ERROR-PASSIVE 或 BUS-OFF，而不只是“完全收不到”。
- 能坚持以驱动实际输出、控制器手册和对端配置核对，不虚构位时序参数。

### 可能追问

1. sample point 为什么常放在一个 bit 的较后位置？
2. 两端 bitrate 相同但 sample point 差异过大可能怎样？
3. CAN 控制器时钟和 SPI 时钟是不是一回事？

---

## 第 66 轮：用 dmesg、ip 和 candump 定位 CAN 收帧异常

### 面试官问题

**设备树修改并重新构建后，你如何用 `dmesg`、`ip -details -statistics link show can0` 和 `candump` 证明问题已经关闭？请给出排查顺序。**

### 本轮状态

- 待补齐真实接口名、修改前后输出和测试报文后作答。

### 评分重点

- `dmesg` 用于确认 SPI probe、时钟获取、IRQ 和 CAN netdev 注册。
- `ip -details -statistics` 用于确认 bitrate、sample point、控制器状态、错误计数和 RX/TX 统计。
- `candump` 只能证明用户态看到了报文，还应结合发送端日志和接口计数闭环。
- 无数据时能继续检查收发器供电、终端电阻、CAN_H/CAN_L、对端发送与 bit timing。

### 可能追问

1. `ERROR-ACTIVE` 为什么不等于链路一定正常？
2. RX 为 0、TX 增长分别可能说明什么？
3. BUS-OFF 后接口如何恢复，是否应该自动重启？

---

## 第 67 轮：深睡眠与安全关机时序的证据边界

### 面试官问题

**简历保留了“深睡眠”和“预留安全关机时间”。请说明到底是谁进入低功耗、唤醒源是什么，以及 MCU 在什么条件下才允许切断 RK3562 和外设电源。**

### 本轮状态

- 功能名称已确认，但具体低功耗层级、PWRON 有效电平、保持时间和关机完成判据仍待向指导工程师补证。

### 评分重点

- 能区分 RK suspend/息屏、STM32 Sleep/STOP/Standby 与整机断电，不混用“深睡眠”。
- 能说明硬掉电可能损坏文件系统或丢失配置，因此需要先触发软关机。
- 能给出 ACK、状态脚、心跳消失、power-good 或固定超时等候选判据，并明确哪一个才是项目实际实现。
- 能说明超时后的产品策略以及需要保存的串口日志或示波器时序证据。

### 可能追问

1. 心跳停止为什么不一定代表系统已经安全关机？
2. 固定延时和等待 ACK 各有什么风险？
3. 深睡眠时哪些电源轨和唤醒源仍保留？

---

## 第 68 轮：Linux多fd事件循环闭卷复盘

### 面试官问题

**请现场写出一个简化的`poll`事件循环，同时处理UART、UDP和UDS，并说明短读、`EINTR`、`POLLERR`、`POLLHUP`和某个处理函数耗时过长时如何处理。**

### 本轮状态

- 待闭卷作答。
- 第42～48轮主要是讲解，本轮用于确认是否已经真正掌握。

### 评分重点

- 正确初始化`pollfd`数组，返回后逐项检查`revents`位掩码。
- `poll`被信号中断且`errno == EINTR`时能够重试。
- UART按字节流追加到buffer解析，UDP按datagram处理。
- 错误、挂断、fd失效和重建逻辑清楚，耗时任务不阻塞整个事件循环。

### 可能追问

1. `poll`和非阻塞IO是什么关系？
2. 为什么`POLLIN`和`POLLHUP`可能同时出现？
3. fd数量很多时为什么考虑`epoll`？

---

## 第 69 轮：C语言、结构体通信与并发安全

### 面试官问题

**为什么RK-MCU协议不直接发送C结构体？请结合结构体对齐、大小端、编译器ABI、`volatile`和中断/任务并发说明风险。**

### 本轮状态

- 待闭卷作答。

### 评分重点

- 结构体可能有padding，不同编译器、选项和架构布局不一定一致。
- 多字节字段存在大小端问题，应明确逐字节编码或使用统一序列化。
- `packed`只能消除部分padding，还可能产生未对齐访问和性能/异常风险。
- `volatile`只限制部分编译优化，不保证复合操作原子性、内存顺序或线程安全。

### 可能追问

1. 如何计算一个结构体的大小和对齐？
2. MCU ISR与任务共享ring buffer时，head/tail如何保护？
3. `memcpy`和`memmove`有什么区别？
4. 协议版本升级时TLV如何保持兼容？

---

## 第 70 轮：终局压力题——证明简历中的量化结果

### 面试官问题

**你的简历同时写了约 1 分钟 UART 升级、约 20 秒 CAN 升级、约 5 秒自动配对、TCP/UDP 超过 20 Mbit/s、25 MHz CAN 参考时钟修正和“通过产线考核”。请逐项说明测量或判定口径、测试环境、样本数量和现有证据；哪些内容你愿意当场负责，哪些仍需回查？**

### 本轮状态

- 待完成前面各专项后进行。
- 这是投递前最终真实性验收题。

### 评分重点

- 每个数字都有明确起点、终点、工具、样本和成功判据。
- 能主动撤回或降低无法复核的数字，而不是为了维持简历表述继续编造。
- 能区分真实完成、阶段性验证和未来计划。
- 能说明哪些资料涉及公司保密，只提供脱敏流程和验证结论。

### 通过标准

- 6 项结果中至少 5 项能够闭卷讲清测量或判定方法。
- A/B 地址、CAN 有效载荷、iperf 命令、设备树修改依据和产线成功判据不能含糊。
- 对无法确认的数字给出明确回查动作，并同步修改简历。

---

## 第二阶段当前进度

- 已建立第49～70轮题目。
- 当前下一题：第49轮“最新版简历总述与个人职责边界”。
- 第49轮通过后，按顺序进入 FreeRTOS 看门狗、A/B 升级、CAN 吞吐、跨设备配对、RK3588 系统与设备树、Linux 用户态和 C 基础。
- 每轮完成后，应在对应题目下追加“你的原始回答要点、评分、问题和优化后的回答”，沿用前48轮记录方式。
