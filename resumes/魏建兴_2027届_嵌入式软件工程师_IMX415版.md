<!-- target-pages: 2 -->

# 魏建兴

男 | 20岁 | 2027届本科 | 157-1584-1972 | w15715841972@163.com<br>
求职方向：嵌入式软件工程师（MCU / RTOS / 嵌入式 Linux）

## 教育背景

**杭州电子科技大学 | 电子信息工程 | 本科**　2023.09 - 2027.06<br>
主修课程：嵌入式系统设计、通信原理、数字电路、模拟电路、C语言、FPGA应用技术

## 实习经历

**奇创未来科技公司 | 嵌入式软件开发实习生**　2025.12 - 至今

- 参与手持控制终端、RK3588 Camera 和驱动板升级功能开发，主要负责 STM32 控制固件、RK-MCU 串口协议和 Bootloader/IAP，也参与 RK3562/RK3588 用户态服务、CAN 设备树与 Camera 链路联调。
- 在高级工程师确定的整体架构下，独立完成分配模块的编码、烧录和板端验证；使用 J-Link、ADB、串口日志、`candump`、`media-ctl` 和 `v4l2-ctl` 排查软硬件交界问题，并整理升级与验证文档。

## 项目经历

### STM32F103 + RK3562 手持控制终端

<p class="project-meta">STM32F103RCT6 负责按键、电源、电量和外设控制，RK3562 Android 负责显示、网络和上层服务，两端通过 UART 通信，并接入 CAN、MAVLink 与图传链路。</p>

- 负责 STM32F103RCT6 固件开发，用 FreeRTOS 运行按键/电源、电量、UART/CAN 和状态监控逻辑。IWDG 由独立任务刷新，每 500 ms 检查主业务循环的最后活跃时间，超过 2 s 未更新便停止喂狗，同时在启动日志中记录复位原因。
- 为电源键加入 50 ms 去抖和短按、2 s 长按判断：短按显示电量或配合 RK 侧息屏/唤醒，关机态长按上电，开机态长按进入软关机。通过 PWRON 与 RK3562 协同深睡眠和软关机，并在断电前预留安全关机时间；联调中处理过连续短按后主控无法亮屏和控制脚状态异常的问题。
- 使用 PB1 ADC 采样估算电量并驱动四颗 LED 分档显示；接入 CH224 充电检测，充电时让下一档 LED 闪烁。GPS 和 QMA6100P 加速度计也在同一固件中完成接入和日志验证。
- 设计 RK-MCU 二进制串口协议，帧格式为 `0xA5 + type + seq + len + TLV + CRC16-CCITT`，支持 `HELLO`、状态摘要、命令和 `ACK/ERROR`。轮询接收时出现过短命令帧漏收，后改为 USART2 中断接收和环形队列解析。
- 在 RK3562 的 `/dev/ttyS3` 上抓取 16 帧连续状态数据，全部通过 CRC16 校验；随后发送 `request_id=0x1234` 的状态请求并收到成功 ACK。板端没有 Python 环境，因此另外写了纯 Shell 监视脚本，用于直接查看 MCU 电源、电量和看门狗状态。
- 实现 UART 双 APP 分区升级：256 KB Flash 划分为 16 KiB Bootloader、两个 118 KiB APP 槽和双页 metadata；RK3562 通过 `/dev/ttyS3` 触发 MCU 进入 Bootloader，升级仅写入非活动槽，不覆盖当前可运行固件。
- 升级使用 `IAP1` 镜像头、512 B 分段传输和整包 CRC32，按 `READY → RX → ERASED → OK` 阶段反馈结果；新镜像通过 pending、启动计数、看门狗和 APP 健康确认完成切换或回退，已完成 A/B 双槽镜像构建、下载及切换验证。
- 参与 RK3562 用户态链路联调，配合 `CommRouter`、`PeripheralManager` 打通 APK UDP、P401 TUN UDP、UART MAVLink、UDS 外设消息和 `can1` SocketCAN；个人工作集中在 MCU/UART、CAN 接入和板端日志验证。

<div class="page-break"></div>
<div class="page-continuation">魏建兴 | 嵌入式软件工程师 | 第 2 页</div>

### 跨设备 CAN 通信与驱动板安全升级

<p class="project-meta">RK3562 控制终端与 RK3588 设备端通过接触式 CAN 链路交换配对身份；RK3588 同时通过 DroneCAN 为 STM32F103CB 驱动板更新固件。</p>

- 参与图传快速配对链路开发，主要完成 RK3562 地面端逻辑：通过 SocketCAN 发送配对触发和身份报文，校验天空端 READY/ACK 并处理重复报文；将 CAN 身份交换、P401 持久化写入和 TUN 数据面检查分层，避免重复触发配对动作。
- 前期使用模拟天空端报文验证地面端上报、解析和应用层 ACK，基础协议已完成真实双板身份交换、peer 提交及 TUN 双向连通；当前自动化版本的连续换板次数和完整配对时延待进一步核对。
- 排查 RK3588 外置 CAN 控制器收帧异常时，对照板载 25 MHz 晶振修正设备树 `fixed-clock`，并结合 `dmesg`、`ip -d -s link` 和 `candump` 复查驱动、位速率与总线状态。
- 在 STM32F103CB 的 128 KiB Flash 中划分 16 KiB Bootloader、两个 52 KiB APP 槽、6 KiB reserved 和双 metadata 页；升级只擦写非活动槽，包头、目标槽、向量表或 CRC 校验失败时不提交 pending 状态。
- 采用 DroneCAN `BeginFirmwareUpdate/File.Read` 由 MCU 顺序拉取 AGUP 升级包，单次读取最大 256 B；RK 工具跟踪升级模式、读取次数、目标复位、10 秒运行探测和最终 CAN 状态。
- 完成 69 项主机测试和 10 轮 A/B 真机交替升级，包括 6 轮普通镜像和 4 轮填满 52 KiB 槽位的镜像；需要恢复的轮次均完成 150/150 请求响应并回到 `ERROR-ACTIVE`。
- 新固件先以 pending 状态试运行，通过健康窗口后确认；当前证据覆盖双槽下载、切换、连续升级和 IWDG 保持等正常路径，异常镜像回滚与逐点断电仍作为后续验证项。

### RK3588 IMX415 1080p60 Camera Bring-up

<p class="project-meta">基于 RK3588 Linux 平台适配 IMX415 摄像头 1080p60 RAW10 模式，验证从 sensor、MIPI、RKCIF、RKISP 到 NV12 输出的完整采集链路。</p>

- 基于现有 IMX415 V4L2 sensor driver 适配 `1920×1080@60fps` RAW10 模式，检查并修正 mode table、寄存器表、VTS/HTS、vblank、pixel rate、link frequency 和 MIPI lane 配置。
- 调试 Device Tree 和 media graph，核对 I2C、MCLK、reset/power GPIO、MIPI endpoint，以及 `IMX415 → DPHY → CSI2 → RKCIF → RKISP` 链路；使用 `media-ctl`、`v4l2-ctl` 和 dmesg 分层取证。
- 在真实 RK3588 板卡完成 RAW `/dev/video0` 与 ISP/NV12 `/dev/video11` 各 600 帧验证，实测达到 `60.00 fps`；稳定 timing 为 `VTS=2250`、`vertical_blanking=1170`。
- 定位 RAW 正常但 NV12 黑帧问题，通过对比 `rkaiq_3A` 启停前后的 controls，确认 AIQ 将 vblank 从 `1170` 改为 `46`；采用隔离不匹配 AIQ 和 systemd guard 固定 controls 的工程方案恢复输出。
- 更换摄像头方位后出现严重色差，使用测试源排除编码和接收端，再结合 RAW、media graph 与 bus format 判断 Bayer 相位不匹配，修正 `MEDIA_BUS_FMT_*` 后恢复颜色。

## 技术能力

- **MCU / RTOS：** 主要使用 C、STM32F103 和 FreeRTOS，做过按键/电源状态机、IWDG、Flash 擦写以及 GPIO、ADC、I2C、UART、CAN 外设开发。
- **通信与升级：** 做过 UART/CAN/DroneCAN 协议、TLV、CRC16/CRC32、ACK、超时重试和双分区状态机；完成手持 UART A/B 双槽验证及 AGS DroneCAN A/B 十轮真机正常路径验证。
- **Linux / Android / Camera：** 使用过 Rockchip SDK、Device Tree、V4L2、MIPI CSI-2、RKCIF、RKISP、Android init、systemd、ADB、SocketCAN 及 `poll` 多 fd 链路联调。
- **开发与调试：** Git、Makefile、Keil MDK、STM32CubeMX、GCC 工具链、Shell、`dmesg`、`logcat`、`tcpdump`、`candump`、`media-ctl`、`v4l2-ctl`、GStreamer、示波器和逻辑分析仪。

<!-- 待补口径：P401 自动化版本的连续换板轮数、完整配对时延与失败样本；手持 A/B 双槽验证的硬件版本、轮次和日志摘要。 -->
