# 嵌入式软件实习转正答辩文稿

> 建议时长：8—10 分钟
> 建议页数：9—10 页
> 答辩主线：围绕无人系统与手持终端，完成从 MCU 底层控制、板端通信、Camera bring-up 到系统镜像和固件升级的嵌入式工程闭环。

## 一、答辩定位

这段实习不适合被概括为“做了几个单独功能”，更准确的总结是：

> 我围绕嵌入式设备的控制、通信、感知和可维护性开展工作，主要负责 STM32 MCU 固件与升级能力，同时参与 RK3562 Android 用户态服务、RK3588 Linux Camera 和系统镜像工程，逐步形成了从硬件接口到系统服务、从功能实现到板端验证的问题闭环能力。

项目可以收束为三条能力主线：

1. **设备控制与通信链路**：手持发射器、RK-MCU UART 协议、P401 CAN 自动配对。
2. **Linux 平台与感知链路**：IMX415 1080p60、RK3588 Debian 系统镜像。
3. **可靠升级与工程交付**：手持 UART-IAP、AGS DroneCAN A/B 升级、测试与验收门。

## 二、项目情况总览

| 项目 | 应用场景 | 主要技术栈 | 我的工作与职责边界 | 当前结果 |
| --- | --- | --- | --- | --- |
| RK3562 + STM32F103 手持发射器 | 手持控制终端的电源、按键、电量、外设状态与通信控制 | C、FreeRTOS、GPIO、ADC、I2C、UART、CAN、IWDG、Bootloader、Android init、MAVLink | 主要负责 MCU 底层控制、RK-MCU 协议和 UART-IAP；参与 RK3562 用户态服务与链路联调 | MCU 控制与运行时协议形成完整链路；历史单槽 IAP 有板端传输证据，当前 A/B 版本仍需真机闭环 |
| P401 CAN 自动配对 | 地面端与天空端接触后自动交换设备身份，减少人工配置 | SocketCAN、CAN 扩展帧、Shell、Android init、systemd、状态机、厂商工具 | 参与协议演进、天地端脚本、部署与联调，不把整套产品链路表述为个人独立完成 | 基础六帧协议已完成双板交换、提交和 TUN 双向 ping；最新八帧自动流程仍待完整硬件验收 |
| RK3588 + IMX415 Camera | 无人系统计算节点的视频采集、编码和图传 | Linux、V4L2、Device Tree、MIPI CSI-2、RKCIF、RKISP、RAW10、NV12、GStreamer、H.265/RTP | 基于现有驱动完成板级模式适配、链路调试和问题定位，不表述为从零开发 sensor driver | 1080p60 RAW/NV12 各 600 帧验证通过；定位 AIQ 改写 vblank 导致黑帧的问题 |
| RK3588 Debian 系统镜像 | 面向嵌入式计算节点构建可刷写、可恢复、可验收的系统软件 | Debian、rootfs、ROS1、U-Boot、Loader、systemd、Wi-Fi 驱动、ext4、RKDevTool | 参与系统裁剪、组件集成、固件打包和板端验收 | no-Qt、ROS、Camera、MPP、RTP 已在指定镜像上通过；新 U-Boot 和部分候选镜像仍分别保留验证状态 |
| AGS MCU A/B 固件升级 | 整机装配后由 RK3588 通过 CAN 在线维护 STM32 固件 | STM32F103CB、Bootloader、A/B 分区、DroneCAN、File.Read、CRC32、IWDG、Python、Shell、SocketCAN | 参与实现升级状态机、主机工具、测试和台架闭环；当前定位为台架方案，不等同生产安全认证 | 69 项主机测试通过，完成 10 轮 A/B 真机交替升级；异常回滚、逐点断电、签名与高负载仍需补充 |

## 三、技术栈与应用场景

| 技术层 | 使用技术 | 解决的问题 | 典型应用 |
| --- | --- | --- | --- |
| MCU 与实时控制 | C、STM32、FreeRTOS、GPIO、ADC、I2C、IWDG | 处理强时序、贴近硬件的状态采集和控制 | 按键去抖、长短按、电源状态机、电量显示、传感器读取、看门狗 |
| 设备间通信 | UART、TLV、CRC16、ACK、CAN、DroneCAN、SocketCAN | 在 MCU、SoC 和外设之间可靠传递状态、命令和升级数据 | RK-MCU 状态上报、P401 身份交换、MCU 在线升级 |
| Linux/Android 板端服务 | C/C++、Shell、UDP、UDS、`poll`、MAVLink、Android init、systemd | 连接 APK、图传、串口、CAN 与本地外设，管理服务生命周期 | `CommRouter` 多路消息路由、按键映射、自动配对服务、自启动与恢复 |
| Camera 与音视频 | V4L2、Device Tree、MIPI CSI-2、RKCIF、RKISP、RAW10、NV12、GStreamer、H.265/RTP | 完成摄像头出图、ISP 处理、编码和网络传输 | IMX415 1080p60 适配、黑帧和色差定位、RTP 图传 |
| 系统构建与交付 | Debian rootfs、ROS1、U-Boot、Loader、ext4、RKDevTool、Wi-Fi 驱动 | 把功能集成为可重复构建、刷写、恢复和验收的完整镜像 | no-Qt 镜像、ROS 集成、无线联网、完整 `update.img` 打包 |
| 调试与验证 | ADB、logcat、dmesg、tcpdump、candump、cansend、media-ctl、v4l2-ctl、示波器、逻辑分析仪 | 将复杂故障按层切分，并为结论保留证据 | 串口收发、CAN 状态、服务启动、Camera media graph、网络和硬件波形定位 |

## 四、10 分钟逐字稿

### 第 1 页：个人介绍与答辩主题（约 40 秒）

各位领导、各位同事好，我是魏建兴，目前在公司担任嵌入式软件开发实习生。今天我汇报的主题是“从底层控制到系统交付的嵌入式工程实践”。

实习期间，我参与的工作主要覆盖 STM32 MCU 固件、RK3562 Android 板端服务、RK3588 Linux Camera 和系统镜像，以及 CAN、UART、UDP、MAVLink、DroneCAN 等通信链路。我的主要职责集中在 MCU 底层控制、通信协议和固件升级，同时也参与了 RK 平台的用户态服务、驱动适配和板端联调。

### 第 2 页：实习工作全景（约 50 秒）

如果把这些项目放到一张图里，可以归纳成三个层次。

第一层是设备控制，我使用 STM32 和 FreeRTOS 处理按键、电源、电量、传感器和看门狗等贴近硬件的逻辑。第二层是板端通信与感知，我在 RK3562 和 RK3588 上参与 UART、CAN、UDP、MAVLink、Camera 和图传链路。第三层是可靠性与交付，包括 Bootloader、A/B 升级、systemd 或 Android init 自启动、rootfs 构建、完整镜像打包以及硬件验收。

这段实习让我认识到，嵌入式工作不仅是把代码写出来，更重要的是让代码在真实硬件、真实启动流程和真实通信链路中稳定运行，并且能够证明它为什么可用。

### 第 3 页：核心项目一——手持发射器控制链路（约 2 分钟）

第一个核心项目是 RK3562 Android 主控加 STM32F103 MCU 的手持发射器。STM32 负责更贴近硬件的控制，RK3562 负责板端服务、APK 和网络链路。我的主责在 MCU 侧，包括电源和按键状态机、电量与充电显示、RK-MCU UART 协议以及 UART-IAP Bootloader。

电源控制并不是简单地检测一个按键。程序需要区分去抖、短按、长按、开机态、关机态和低电状态。短按用于显示电量；关机态长按触发上电；开机态长按需要先通过 `PWRON` 与 RK3562 协同，让上层系统获得关机处理时间，再进入后续断电流程。电量侧通过 ADC 采样和四档 LED 展示状态，并结合充电检测、显示平滑和低电提示，避免采样抖动造成频繁跳变。

为了让 RK 和 MCU 之间的接口可扩展、可校验，我参与设计了 `0xA5 + type + seq + len + TLV + CRC16` 的二进制协议。TLV 用于扩展电量、充电、看门狗、电源、接触和 GPS 等字段；`seq` 和 `request_id` 用于关联请求；CRC16 用于发现串口传输异常；未知 TLV 可以按长度跳过，降低协议升级对旧端的影响。

升级方面，Bootloader 与 APP 分区隔离，RK 通过串口触发 MCU 进入升级窗口。Bootloader 对包头、长度、地址和 CRC32 进行检查，启动 APP 前还会校验 MSP 和 Reset Handler。历史单槽方案中采用首 APP 页延后写入，降低传输中断后把不完整镜像误判为有效程序的风险。这里我的答辩口径会严格区分：历史单槽 IAP 有板端传输证据，当前扩展的 A/B 版本还需要补充完整真机闭环。

RK3562 侧我参与的是用户态通信链路和联调，不把整个 Android 系统说成独立完成。`CommRouter` 使用 `poll` 处理 APK UDP、P401 TUN UDP、UART MAVLink 和本地 UDS 消息；`PeripheralManager` 处理按键和 CAN 事件，再映射为 MAVLink 命令。我的工作重点是把 MCU、串口、CAN、服务、APK 和网络链路串起来定位问题。

### 第 4 页：核心项目二——P401 CAN 自动配对（约 1 分 20 秒）

P401 项目解决的是地面端和天空端设备需要人工读取身份、分别配置对端信息的问题。我们的思路是复用两端已有的物理 CAN 接触链路，在 CAN 上完成身份发现和双向确认，再调用厂商工具写入 P401 peer。CAN 只承载会话和身份控制信息，真正的视频和业务数据仍走 P401 TUN 链路。

协议经历了从基础六帧身份交换到当前八帧状态机的演进。新版本增加了专用的 `PAIRTRIG` 接触触发、READY/READY_ACK 会话门以及 DONE/DONE_ACK，目的是避免旧报文、跨会话 ACK 或单边结束造成错误写入。只有双方身份逐字节匹配并完成确认后，程序才允许修改持久化 peer。

这个项目让我更清楚地区分了“CAN 帧发出”“应用层交换成功”“P401 写入成功”和“数据面恢复”四个不同结论。基础六帧版本已经完成真实双板交换和提交，配对后 TUN 双向 ping 为零丢包；最新八帧自动流程完成了离线测试和安全部署，但还不能表述为已经稳定达到三秒以内，后续仍要补充健康接触条件下的多轮时延和换板压力测试。

### 第 5 页：核心项目三——IMX415 1080p60 Camera bring-up（约 1 分 40 秒）

第三个项目是 RK3588 Linux 平台上的 IMX415 Camera bring-up。应用场景是无人系统计算节点的视频采集、编码和图传。我的工作不是从零编写 sensor driver，而是基于现有 V4L2 驱动完成当前板卡的 1920×1080、60 帧 RAW10 模式适配，并进行设备树链路调试和板端验证。

整个数据链路是 IMX415 经 DPHY、CSI2、RKCIF 到 RKISP，前端可以从 RAW 节点验证传感器数据，经过 ISP 后从 NV12 节点进入编码和显示。适配时需要让寄存器表、VTS、HTS、vblank、pixel rate、link frequency 和 MIPI lane 配置保持一致，不能只修改一个帧率字段。当前稳定参数中，VTS 是 2250，有效高度是 1080，因此 vertical blanking 是 1170。

项目中最典型的问题是 RAW 正常但 NV12 黑帧。我没有直接反复修改驱动，而是按链路排查：先用测试源证明 H.265、RTP 和接收端正常，再确认 sensor RAW 有有效数据，最后将问题范围缩小到 ISP 和 AIQ。对比 AIQ 启停前后的 V4L2 controls 后发现，`rkaiq_3A` 会把 vblank 从 1170 改为 46，导致 ISP 输出异常。当前工程方案是避开不匹配的 AIQ 配置，并通过 systemd guard 固定已验证参数；长期方案是补齐与 1080p60 linear mode 匹配的 IQ 文件。

最终，RAW 和 NV12 路径都完成了各 600 帧验证。这个项目提升了我对设备树、media graph、sensor timing 和分层定位方法的理解。

### 第 6 页：RK3588 系统镜像与交付（约 1 分钟）

Camera 功能最终还需要进入可交付系统，因此我也参与了 RK3588 Debian 系统镜像工程。这里的目标不是单独修改 rootfs，而是形成从 SDK、U-Boot、kernel、DTB、rootfs 到完整 `update.img` 的构建和验收链路。

具体工作包括 no-Qt 与 headless 策略区分、ROS1 Base 和 Extra 集成、USB Wi-Fi 驱动的版本化安装与自动加载、systemd 网络恢复、SSH 策略以及完整固件的反向解包和一致性检查。指定镜像已经在真机上完成 no-Qt、ROS pub/sub、Camera、MPP H.265 和 RTP 冒烟验证。

这个项目中一个重要经验是：文件名相同不代表板端运行的就是同一版本。后续验收都需要结合 SHA-256、ext4 UUID、反向解包结果和串口启动日志确认产物身份。同时，离线验证通过也不等于新 U-Boot 或新 rootfs 已经完成硬件验证，两类状态必须分别记录。

### 第 7 页：可靠性项目——AGS MCU A/B 升级（约 1 分 30 秒）

AGS 项目解决的是整机装配后仍依赖 SWD 更新 MCU 的维护问题。方案由 RK3588 通过 DroneCAN 的 BeginFirmwareUpdate 和 File.Read 服务，把升级包传给 STM32F103CB。STM32 在运行中的 APP 内只擦写非活动槽，完整下载并通过流式 CRC、Flash 回读和向量表检查后，才提交 pending 元数据并复位进入试运行。

Bootloader 使用双元数据页保存确认槽、待试运行槽、版本和试运行次数。新镜像必须在健康窗口内确认，否则由 IWDG 触发复位并回滚到原确认槽。RK 主机侧不能只看到节点重新上线就判成功，还要检查进入升级模式、File.Read 数量、目标复位、10 秒运行探测以及最终 CAN 是否恢复到 `ERROR-ACTIVE`。

目前已有 69 项主机测试通过，并完成 10 轮 A/B 真机交替升级，包括普通镜像和恰好填满 52 KiB 槽位的镜像。连续升级中曾出现 IWDG 已经硬件启动、但软件状态被清除导致中途复位的问题；修复后保留不可逆硬件状态，并通过连续 A/B 测试验证。另一个问题是 MCU 复位窗口造成 RK 端 CAN 进入 `ERROR-WARNING`，最终把 150 次请求响应和恢复 `ERROR-ACTIVE` 纳入同一验收门。

这套方案当前是台架闭环，不等于生产或飞行安全认证。数字签名、反回滚、异常镜像回滚、逐点断电和多节点高负载测试仍是后续重点。

### 第 8 页：我的工作方法与能力沉淀（约 50 秒）

通过这些项目，我沉淀了三点工作方法。

第一，先建立系统视图，再写局部代码。遇到问题时先画清数据和控制链路，明确每个模块的输入、输出和所有者。第二，按层取证，不用最终现象直接猜根因。例如 Camera 黑帧会拆成 sensor、MIPI、RAW、ISP、编码、网络和接收端；通信故障会拆成物理层、驱动、SocketCAN、协议状态机、服务和业务数据面。第三，把“成功”写成可执行的验收条件，包括日志、帧数、CRC、错误计数、哈希、UUID 和真实硬件状态，而不是只看程序退出码。

我认为自己的核心优势，是能够在 MCU、Linux/Android 板端服务和真实硬件联调之间建立联系，并对结果的证据边界保持清楚。

### 第 9 页：不足与改进计划（约 50 秒）

我目前仍有三个需要加强的方向。

第一，继续深入 Linux 内核驱动和 Camera/ISP 体系，尤其是 V4L2 子设备、时序计算以及 AIQ/IQ 调校。第二，把可靠性测试从正常路径扩展到异常路径，包括断电、包损坏、服务中断、总线高负载和不健康镜像回滚。第三，提升工程自动化，把构建参数、版本、哈希、测试结果和硬件状态统一写入机器可读的 manifest，减少人工判断和版本混淆。

如果能够转正，我希望继续承担 MCU 与 RK 平台之间的板端软件工作，重点推进通信链路稳定性、升级安全性和系统交付自动化，并逐步具备独立负责嵌入式子系统的能力。

### 第 10 页：总结（约 20 秒）

最后总结一下：实习期间，我从 STM32 底层控制出发，逐步参与到 RK 板端服务、CAN/UART 通信、Camera bring-up、系统镜像和 A/B 升级中。相比刚入职时，我最大的成长是从“完成一个功能”转向“理解完整链路、定位真实问题并用证据完成验收”。以上是我的汇报，谢谢各位。

## 五、建议的 PPT 页面标题

1. 个人介绍：从底层控制到系统交付
2. 实习工作全景：三条主线、五个项目
3. 手持发射器：MCU 控制与 RK-MCU 通信
4. P401 自动配对：从身份交换到数据面恢复
5. IMX415 Camera：1080p60 适配与黑帧定位
6. RK3588 系统镜像：构建、打包、刷写、验收
7. AGS A/B 升级：可靠升级与台架闭环
8. 技术栈：技术如何服务具体场景
9. 成长、反思与转正后计划
10. 总结

## 六、高频追问与参考回答

### 1. 你在这些项目里最核心的个人贡献是什么？

我的主责是 STM32 MCU 侧的电源与按键状态机、状态采集、RK-MCU UART 协议和 Bootloader/IAP。RK3562 用户态服务、P401 配对、RK3588 Camera 和系统镜像中，我承担的是具体模块实现、适配与联调工作，不把整个系统表述为个人独立完成。

### 2. 哪个问题最能体现你的定位能力？

IMX415 的 RAW 正常、NV12 黑帧最典型。我先证明编码和网络链路正常，再证明 sensor RAW 有效，把问题缩小到 ISP/AIQ；最后通过 controls 对比确认 AIQ 将 vblank 从 1170 改为 46。这个过程体现的是分层排除和证据闭环，而不是试错式改参数。

### 3. 为什么 UART 协议使用 TLV，不直接发送 C 结构体？

直接发送结构体会受字节对齐、大小端和版本变化影响。TLV 可以让新字段增量扩展，旧端遇到未知字段时按长度跳过；配合帧长、序号、请求标识和 CRC，可以提高解析、兼容和问题定位能力。

### 4. Bootloader/IAP 如何降低升级失败变砖的风险？

单槽方案通过 Bootloader 与 APP 隔离、APP 向量合法性检查、整包 CRC 以及延后写首 APP 页降低风险；A/B 方案进一步只写非活动槽，通过双页元数据、pending/confirmed 状态、试运行和 IWDG 回滚保护已确认镜像。两种方案的验证状态需要分开表述。

### 5. 为什么 P401 需要应用层 ACK，CAN 不是自带 ACK 吗？

CAN 的 ACK 只说明总线上至少有节点正确接收了物理帧，不能说明目标应用解析了身份、确认了会话，更不能说明已经写入 P401。应用层 ACK 用于确认具体身份和状态机阶段。

### 6. AGS A/B 已经做了 10 轮，为什么还不能说可量产？

10 轮证明了正常下载、满槽写入、连续切换、试运行确认和部分 CAN 恢复路径，但还没有覆盖所有不健康镜像、任意断电点、文件服务中断、恶意包、多节点高负载、数字签名和反回滚。因此它是台架闭环，不是生产安全认证。

### 7. 你如何判断一个系统镜像真的刷对了？

不能只看文件名。需要核对产物 SHA-256、反向解包内容、ext4 UUID、板端实际挂载分区、串口 Boot 链和运行功能。离线检查与硬件启动验收也必须分开记录。

### 8. 你认为自己转正后的价值是什么？

我能同时理解 MCU 实时控制、Linux/Android 板端服务和 UART/CAN/网络链路，适合承担跨 MCU 与 SoC 的嵌入式子系统开发。转正后可以继续提高通信可靠性、升级安全性和交付自动化，减少联调周期和版本风险。

## 七、表达边界与保密提醒

- 手持 MCU 侧可以使用“主要负责”；RK3562 服务、APK、完整 Android 系统应使用“参与开发或联调”。
- IMX415 应表述为“基于现有驱动进行模式适配和板级调试”，不要说从零编写完整 sensor driver。
- P401 基础六帧协议已有硬件闭环；最新八帧和“三秒内自动配对”仍是待完整验收目标。
- 手持当前 A/B UART-IAP 只有构建和主机侧证据；AGS DroneCAN A/B 有 10 轮真机正常路径证据，两者不能混用。
- AGS 方案没有完成数字签名、反回滚、完整断电矩阵和飞行安全授权，不能描述为可直接量产。
- 对外展示前应隐藏公司源码路径、设备地址、账号、真实身份值、Wi-Fi/SSH 信息和内部项目资料。
