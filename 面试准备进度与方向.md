# 面试准备进度与方向

## 当前定位

当前简历更适合定位为：

> 嵌入式 Linux / Android 板端软件方向，具备 STM32 MCU 底层控制、通信协议、Bootloader/IAP 和多端联调经验。

面试时不建议把自己只讲成 MCU 开发。更好的表达是：

> 我有 MCU 底层经验，也参与了 RK3562 Android/Linux 用户态通信链路联调，能把底层硬件状态、串口/CAN/UDP/MAVLink 链路和上层 APK 业务串起来定位问题。

这样既贴近 Linux 岗位，又不丢失 MCU 项目优势。

## 已完成的面试模拟主线

当前 `面试模拟.md` 已覆盖 13 轮问答，主线集中在“手持发射器 RK3562 + STM32F103 控制链路开发”。

### 1. 项目表达

已练习内容：

- 2 分钟介绍手持地面控制终端项目。
- RK3562 与 STM32F103 的职责分工。
- 自己的职责边界：MCU 侧主要负责，RK 侧参与用户态通信链路开发和联调。
- 项目难点：多端、多协议、多服务、多硬件链路联调。

当前掌握程度：中等偏熟。

后续目标：

- 继续压缩成 60 秒版本和 30 秒版本。
- 避免把 RK 侧说成完整 Android BSP/APK 全部独立完成。

### 2. RK-MCU UART 协议

已练习内容：

- 为什么 UART 需要自定义协议。
- `0xA5 + type + seq + len + payload + CRC16` 帧格式。
- TLV 的作用。
- 小端、多字节字段、结构体对齐风险。
- Linux 用户态 `/dev/ttyS3` 的 `read/write/poll` 访问方式。

当前掌握程度：较熟。

后续目标：

- 能画出接收状态机。
- 能解释半帧、粘包、丢字节恢复同步。
- 能把协议和 Linux fd、buffer、poll 关联起来讲。

### 3. CRC、ACK、重试和可靠性

已练习内容：

- CRC 和 checksum 区别。
- CRC16 用于单帧检错，CRC32 用于整包固件校验。
- CRC 只能检错，不能纠错。
- 命令类消息通过 `request_id`、ACK/ERROR、timeout、retry 保证可靠性。
- MCU 通过去重缓存避免重复执行关键命令。

当前掌握程度：中等偏熟。

后续目标：

- 进一步理解 `seq` 和 `request_id` 的区别。
- 能解释 ACK 丢失导致重发的场景。
- 能回答“CRC 能不能防止恶意篡改”。

### 4. Bootloader / UART-IAP

已练习内容：

- APP 先 ACK，再复位进入 Bootloader。
- backup register、RAM、Flash 作为升级标志的优缺点。
- Bootloader IAP 等待窗口策略。
- APP 合法性检查：MSP 和 Reset_Handler。
- 跳转 APP 前关闭中断、设置 MSP、重定位 VTOR。
- APP 偏移到 `0x08004000` 后工程链接地址、烧录地址、向量表偏移的区别。
- HEX 和 BIN 的区别。
- IAP 防变砖：APP 首页延迟写入。

当前掌握程度：较熟，但仍需加强细节。

后续目标：

- 能手写 Bootloader 跳 APP 的伪代码。
- 能解释 `SCB->VTOR`、SysTick、NVIC pending。
- 能回答 A/B 分区、升级状态机、断电恢复。

## 当前暴露出的薄弱点

### 1. 概念容易混在一起

典型例子：

- TLV 和小端不是一回事。
- VTOR、向量表偏移、链接地址、烧录地址不是一回事。
- HEX 自带地址，BIN 不带地址。
- ACK 只代表 APP 收到命令，不代表 Bootloader 已经准备好接收固件。

建议：

- 面试时先分层，再回答。
- 多用“编译链接层、烧录层、运行时层”这种结构化表达。

### 2. Linux 方向表达还需要更突出

目前你对 MCU/IAP 已经能讲不少，但目标岗位偏 Linux，所以后续要把项目重心继续往 RK3562 用户态通信服务靠。

需要强化：

- fd、`open/read/write/ioctl/poll`
- 串口设备 `/dev/ttyS3`
- SocketCAN：`can1`、`candump`、`cansend`
- UDP socket：`bind`、`recvfrom`、`sendto`
- systemd / Android init 服务启动、日志排查
- 多 fd 事件循环和消息路由

### 3. 回答需要少一点“经验说法”，多一点“判断依据”

例如：

- “延迟 100ms”可以说，但更严谨的是“等待 UART TX complete”。
- “buffer 512B/1KB/4KB 都行”可以说，但更严谨的是“根据最大帧长、串口速率、调度延迟决定”。
- “等待 120 秒”可以说，但要说明“覆盖 RK 启动、服务启动、升级脚本启动时间”。

## 后续学习和面试模拟方向

### 第一阶段：补强 RK3562 Linux 用户态通信链路

建议下一阶段优先练这些问题：

1. `CommRouter` 为什么要用 `poll`？不用多线程行不行？
2. `/dev/ttyS3`、UDP socket、UDS、SocketCAN 在 Linux 用户态里为什么都可以看成 fd？
3. `poll` 返回后怎么判断哪个 fd 可读？
4. UDP 的 `bind`、`recvfrom`、`sendto` 分别干什么？
5. SocketCAN 的 `socket(PF_CAN, SOCK_RAW, CAN_RAW)`、`bind(can1)`、`read/write` 大概流程是什么？
6. RK 侧服务启动失败，你怎么查？从 `ps`、`logcat`、`dmesg`、权限、SELinux、设备节点怎么排？

目标：

- 把简历从“MCU 项目”讲成“Linux 用户态服务 + MCU 协议 + 多链路联调”。

### 第二阶段：补强 P401 CAN-MAC 配对

建议练习：

1. 为什么要通过 CAN 交换 `tun` MAC？
2. MAC report、ACK、去重分别解决什么问题？
3. DroneCAN 报文中的 DTID、node id、tail byte 大概是什么？
4. `candump` 看到帧以后怎么判断是天空端还是地面端？
5. MCP2515 收不到 CAN 帧时怎么分层排查？

目标：

- 能把 P401 讲成一个清晰的 Linux/CAN 联调案例，而不是只讲“脚本交换 MAC”。

### 第三阶段：补强 RK3588 IMX415 Camera 项目

建议练习：

1. V4L2 mmap 取帧流程。
2. media graph 怎么看。
3. RAW10、Bayer、NV12 分别是什么。
4. IMX415 -> DPHY -> CSI2 -> RKCIF -> RKISP -> video node 链路怎么讲。
5. 黑屏问题如何定位到 `rkaiq_3A` 修改 `vertical_blanking`。
6. 色差问题为什么可能和 Bayer phase / bus format 有关。

目标：

- 保留第二项目作为 Linux 驱动/BSP/Camera 方向的支撑。

### 第四阶段：通用 Linux 八股

建议练习：

1. 进程和线程区别。
2. 用户态和内核态区别。
3. 字符设备、设备节点、major/minor。
4. 阻塞 IO、非阻塞 IO、同步/异步。
5. `select/poll/epoll` 区别。
6. `ioctl` 为什么存在，和 `read/write` 有什么区别。
7. systemd service 和 shell 脚本区别。
8. 如何判断一个 `.service` 或 `.sh` 是否正常运行。

目标：

- 面试时能接住 Linux 基础追问，不只依赖项目经验。

## 建议的下一次模拟顺序

建议下一次从 RK3562 Linux 用户态开始：

1. 先问 `CommRouter` 架构。
2. 再问 fd 和 `poll`。
3. 再问 UDP socket。
4. 再问 SocketCAN。
5. 最后问服务启动和日志排查。

这条路线最符合你当前目标：偏 Linux 岗位，同时保留 MCU 协议和 Bootloader 的差异化优势。

