# 面试准备进度与方向

## 当前定位

当前简历更适合定位为：

> 通用嵌入式软件方向，主线为 STM32 MCU / RTOS、UART/CAN 通信与双分区升级，同时具备 RK3562/RK3588 Linux/Android 板端联调经验。

面试时不建议把自己只讲成 MCU 开发。更好的表达是：

> 我有 MCU 底层经验，也参与了 RK3562 Android/Linux 用户态通信链路联调，能把底层硬件状态、串口/CAN/UDP/MAVLink 链路和上层 APK 业务串起来定位问题。

这样既贴近 Linux 岗位，又不丢失 MCU 项目优势。

## 当前进度概览

更新时间：2026-08-03。

当前 `面试模拟.md` 已建立 **70 轮问答**。第 1～48 轮包含实际作答、点评和集中讲解；第 49～70 轮已按最新版简历重排，新增双分区升级、52 KB CAN-IAP、RK3588 CAN 设备树时钟、系统构建、产线考核和真实性验收题，仍需逐轮闭卷作答。

当前主线已经从“STM32 MCU + UART-IAP”扩展为：

> STM32 控制与可靠升级 + RK3562 Linux/Android 用户态链路 + 跨设备 CAN 配对 + RK3588 系统构建、设备树和量产交付。

## 已完成的面试模拟主线

### 1. 项目表达与 RK-MCU 通信（第 1～5 轮）

已覆盖：

- 手持地面控制终端项目总述和职责边界。
- RK3562 与 STM32F103 的分工。
- UART 自定义帧、TLV、大小端、半帧和粘包。
- CRC、ACK、超时重试、`request_id` 和命令去重。

当前状态：中等偏熟。能理解优化答案，但仍要练 30 秒、60 秒和 2 分钟三个版本，并避免把参与的 RK 用户态联调夸大为完整 Android BSP/APK 独立开发。

### 2. Bootloader、IAP 和看门狗（第 6～20 轮）

已覆盖：

- APP 进入 Bootloader 的 ACK 时机和升级标志。
- MSP、Reset_Handler、VTOR、链接地址和烧录地址。
- HEX/BIN、向量表跳转、A/B 分区、metadata 和试启动回滚。
- metadata 状态机、断电恢复、pending/confirmed。
- IWDG、WWDG、任务心跳和复位后的启动决策。

当前状态：较熟，但细节仍需复盘。手持端 UART-IAP 已确认为双 APP 分区；下一步要从真实链接脚本或 map 文件补齐地址，并能闭卷写出 Bootloader 跳 APP、metadata 状态转换和四类断电场景。

### 3. MCU 电源、按键和电量管理（第 21～31 轮）

已覆盖：

- 电源/按键状态机、去抖、短按/长按和防重复触发。
- RK3562 软关机、RK809 `PWRON` 协同和 MCU 断电时机。
- 息屏、睡眠、关机以及 STM32 Sleep/Stop/Standby。
- ADC 电量检测、分压换算、滤波、低电保护和百分比映射。

当前状态：中等。能够跟随项目场景理解，但需要把状态、事件、动作和超时条件整理成一张可口述的状态机。

### 4. P401 CAN-MAC 和 SocketCAN（第 34～41 轮）

已覆盖：

- P401 CAN-MAC 配对项目总述和职责边界。
- MAC report、应用层 ACK、DTID、node id 和 tail byte。
- CAN ACK 与应用层 ACK 的区别。
- `ERROR-ACTIVE`、`ERROR-PASSIVE`、`BUS-OFF` 和 `restart-ms`。
- SocketCAN 配置、`candump`、`cansend`、标准帧、扩展帧和 8 字节 payload。

当前状态：中等偏熟。本人已确认整机以硬件接触为起点、视频显示为终点，单轮约 5 秒并完成数十轮验证；个人主责仍集中在 RK3562 地面端 report/ACK、去重和板端验证，需补齐旧 `dry_run`/收帧问题关闭过程的脱敏证据。

### 5. RK3562 Linux 用户态通信链路（第 32～33、42～48 轮）

已覆盖：

- `CommRouter` 的 APK UDP、P401 TUN UDP、UART MAVLink 和 UDS 多入口架构。
- `poll` 与多线程的取舍，以及 `fd/events/revents`。
- UDP 的 `socket/bind/recvfrom/sendto` 和临时源端口。
- UDP 实时数据与关键命令的不同可靠性策略。
- Android init 服务启动失败的分层排查。
- 进程运行但 APK 收不到 MAVLink 时，从 UART、解析、路由、UDP 到 APK 的端到端取证。

当前状态：**已讲解、待复述**。第 42～48 轮主要由讲解完成，还不能按“已掌握”计算。下一次模拟应优先闭卷复述，而不是继续堆新知识。

### 6. RK3588 系统、CAN 设备树与产线交付（第 58～67 轮）

已覆盖：

- Rockchip SDK `build.sh`、镜像构建和 SD 卡一键烧录。
- RTL8188EUS 外部内核模块交叉编译、集成与 TCP/UDP `iperf` 验证。
- rootfs 裁剪、ROS 依赖预置、Wi-Fi 与 TUN 模块集成；500 MB 数字因口径不清已删除。
- 25 MHz `fixed-clock`、SPI0 下两路 `xl2515` 节点以及 `dmesg`、`ip -details -statistics`、`candump` 分层验证。
- 多设备升级、脚本自检和产线考核结果。

当前状态：项目事实已进入简历，但知识准备偏弱。必须补齐设备树修改前后日志、iperf 命令与 UDP 丢包/jitter、产线设备数量和自检项，才能扛住连续追问。

## 当前优势

- 能把 UART、CRC、ACK、Bootloader/IAP 和断电保护关联到真实项目。
- 能区分 MCU 主责、RK 用户态参与和未完成验证，职责边界比之前清晰。
- 已形成 P401 CAN 链路的分层排查意识，不再把 `ERROR-ACTIVE` 或 CAN 硬件 ACK 当作应用成功。
- 熟悉 ADB、logcat、dmesg、tcpdump、SocketCAN、串口日志和硬件测量工具的基本定位作用。

## 当前主要薄弱点

### 1. Linux 新知识以听懂为主，独立输出不足

当前能够理解 `CommRouter`、`poll`、UDP 和 Android init 的讲解，但第 42～48 轮没有独立作答。下一步需要不看笔记完成：

1. 90 秒介绍 `CommRouter` 架构。
2. 解释 `poll` 与多线程取舍。
3. 手写最小 `pollfd` 遍历框架。
4. 口述 UDP socket 收发流程。
5. 按层排查“服务没启动”和“服务运行但 APK 没数据”。

### 2. Android init 和 SELinux 仍是重点薄弱项

需要熟悉并能解释：

- `getprop init.svc.xxx`、`ps -A`、`setprop ctl.restart xxx`。
- `/vendor/etc/init/*.rc` 中的 service、class、user、group 和启动顺序。
- 设备节点 owner/group/mode 与服务运行身份。
- `avc: denied`、进程 domain、文件 label 和最小策略修正。

### 3. RK3588 设备树、系统构建和量产证据仍不完整

仍需覆盖：

- 设备树的节点继承、`compatible` 匹配、`clocks`、SPI 片选和中断 GPIO。
- 25 MHz 时钟如何参与 CAN bit timing，以及修改前后 `dmesg`/接口统计差异。
- Rockchip 启动链、外部内核模块 `vermagic`、`depmod`、`insmod/modprobe`。
- iperf2/iperf3 的真实命令、测试方向、时长、UDP 丢包率和 jitter。
- 产线考核日期、并行设备数量、自检项、成功判据和结果日志。

### 4. 通用基础和手撕代码覆盖不足

- C：内存分区、指针、对齐、`volatile`、原子性、编译链接。
- FreeRTOS：任务状态、队列、信号量、互斥量、FromISR 和栈检查。
- Linux：进程/线程、阻塞/非阻塞、`select/poll/epoll`、字符设备和 `ioctl`。
- 手撕：环形缓冲区、CRC16、TLV 状态机、`memmove`、链表和字符串。

## 建议的下一次模拟顺序

下一次先做第 42～48 轮闭卷复述：

1. `CommRouter` 架构和消息流。
2. `poll` 与多线程、`pollfd` 事件处理。
3. UDP socket 流程和应用层可靠性。
4. Android init、权限和 SELinux 排查。
5. “进程运行但 APK 没数据”的端到端定位。

复述通过后，再继续：

1. 阻塞/非阻塞 IO、`EAGAIN` 和边缘情况。
2. `select/poll/epoll` 对比。
3. RK3588 CAN 设备树、25 MHz 时钟与 SocketCAN 分层排查。
4. UART/CAN A/B 地址、metadata、断电回滚和升级耗时估算。
5. Rockchip 系统构建、iperf、产线考核与通用 C/FreeRTOS 手撕代码。

近期目标不是继续增加问答数量，而是把“已经听懂”推进到“能独立回答、能接住追问”。
