## 个人定位

杭州电子科技大学电子信息工程本科在读，实习方向为嵌入式软件开发。工作覆盖 STM32 MCU 固件、RK3562 Android 板端服务、RK3588 Linux Camera 与系统镜像，具备从硬件接口、通信协议到板端服务和真机验证的完整链路意识。

**核心方向：** MCU/RTOS、嵌入式 Linux/Android、Bootloader/IAP、UART/CAN/DroneCAN、Camera bring-up、系统构建与板级联调。

## 实习概述

**奇创未来科技公司｜嵌入式软件开发实习生｜2025.12—至今**

参与无人系统手持终端和板载计算节点的软件开发，主要负责 STM32 MCU 底层控制、RK-MCU 通信协议和固件升级；参与 RK3562/RK3588 板端服务、P401 图传配对、Camera 适配、Debian 系统镜像构建及硬件联调。能够使用日志、抓包、总线工具和硬件仪器完成跨 MCU、SoC、外设与网络的问题定位。

## 核心项目

### 1. 手持发射器 RK3562 + STM32F103 控制链路

**应用场景：** 手持控制终端的开关机、电量显示、状态采集、板端通信和图传控制。<br>
**技术栈：** STM32F103、C、FreeRTOS、GPIO、ADC、I2C、UART、CAN、IWDG、Bootloader、Android init、UDP/UDS、MAVLink、SocketCAN。

- 主要负责 STM32F103 MCU 固件，完成按键去抖、长短按识别、电源状态机、电量/充电显示、IWDG 看门狗及 RK3562 `PWRON` 协同控制。
- 设计 RK-MCU 二进制通信协议，采用 `0xA5 + type + seq + len + TLV + CRC16` 帧格式，支持状态上报、命令下发、ACK/ERROR 和 Bootloader 触发。
- 实现 UART-IAP Bootloader，完成 APP 分区、包头检查、Flash 擦写、CRC32、向量表合法性检查及 APP 跳转；使用延后写首 APP 页降低异常中断风险。
- 参与 RK3562 用户态服务联调，通过 `poll`、UDP、UDS、UART 和 SocketCAN 打通 APK、P401、MAVLink 与本地外设链路。
- 使用 ADB、logcat、tcpdump、串口日志、candump、示波器和逻辑分析仪定位服务启动、串口收发、CAN 通信和 APK 数据显示问题。

### 2. P401 CAN 自动配对

**应用场景：** 地面端与天空端接触后自动交换 P401 身份，替代人工登录设备和配置对端。<br>
**技术栈：** SocketCAN、CAN 扩展帧、Shell、Android init、systemd、状态机、厂商 `wire_pair` 工具。

- 参与天地端身份交换协议和自动配对状态机，实现专用 `PAIRTRIG`、READY/ACK、身份确认及 DONE 握手。
- 将 CAN 会话、P401 持久化写入和 TUN 数据面检查分层，避免把物理帧 ACK 误判为应用配对成功。
- 基础六帧版本已完成真实双板身份交换、持久化提交和 TUN 双向 ping 零丢包；最新八帧版本已完成离线测试，硬件多轮时延仍待验证。

### 3. RK3588 IMX415 1080p60 Camera bring-up

**应用场景：** 无人系统计算节点的视频采集、ISP 处理、H.265 编码与 RTP 图传。<br>
**技术栈：** RK3588 Linux、IMX415、V4L2、Device Tree、MIPI CSI-2、RKCIF、RKISP、RAW10、NV12、GStreamer、H.265/RTP、systemd。

- 基于现有 V4L2 sensor driver 适配 `1920×1080@60fps` RAW10 模式，修正 mode table、VTS/HTS、vblank、pixel rate、link frequency 和 MIPI lane 配置。
- 调试 `IMX415 → DPHY → CSI2 → RKCIF → RKISP` media graph，使用 `media-ctl`、`v4l2-ctl` 和 dmesg 分层验证 RAW 与 ISP/NV12 路径。
- 定位 RAW 正常但 NV12 黑帧问题，确认 `rkaiq_3A` 将 vblank 从稳定值 `1170` 改为 `46`；通过 systemd guard 固定已验证 controls。
- 完成 RAW `/dev/video0` 与 NV12 `/dev/video11` 各 600 帧真机验证，并打通 MPP H.265 与 RTP/UDP 图传冒烟链路。

### 4. RK3588 Debian 系统镜像工程

**应用场景：** 为嵌入式计算节点提供可构建、可刷写、可恢复和可验收的完整系统镜像。<br>
**技术栈：** Debian rootfs、ROS1 Noetic、U-Boot、Loader、Linux 5.10、systemd、ext4、Wi-Fi 驱动、RKDevTool。

- 参与 no-Qt/rootfs 裁剪、ROS1 Base/Extra、RTL8188FU Wi-Fi 驱动、自启动服务和 SSH 策略集成。
- 建立 `U-Boot/boot/rootfs → update.img → 反向解包 → 哈希/UUID/FIT/e2fsck → 真机刷写` 验收链路。
- 指定镜像已完成 no-Qt、ROS pub/sub、Camera、MPP H.265 和 RTP 真机冒烟；不同候选产物分别记录离线和硬件状态。

### 5. AGS STM32F103CB DroneCAN A/B 固件升级

**应用场景：** 整机装配后由 RK3588 通过 CAN 在线维护控制 MCU，减少外部 SWD 操作。<br>
**技术栈：** STM32F103CB、A/B Bootloader、DroneCAN、BeginFirmwareUpdate、File.Read、CRC32、IWDG、Python、Shell、SocketCAN。

- 参与设计 16 KiB Bootloader、双 52 KiB APP 槽及双元数据页，只允许运行镜像写入非活动槽。
- 实现 AGUP 包检查、DroneCAN 顺序拉取、流式 CRC、Flash 回读、pending 提交、试运行确认和 IWDG 回滚链路。
- 完善 RK 主机打包、文件服务和一键升级工具，将下载数量、目标复位、10 秒探测及最终 `ERROR-ACTIVE` 作为联合成功门。
- 69 项主机测试通过，完成 10 轮 A/B 真机交替升级；正常路径已形成台架闭环，签名、反回滚、逐点断电和高负载测试仍需补齐。

## 技术能力

- **MCU/RTOS：** STM32F103、C、FreeRTOS、IWDG、GPIO/ADC/I2C/UART/CAN、电源状态机、Bootloader/IAP。
- **Linux/Android：** 用户态服务、`poll`、UDP/UDS、Android init、systemd、ADB、进程与设备权限排查。
- **通信协议：** UART TLV、CRC/ACK、CAN、SocketCAN、DroneCAN/libcanard、MAVLink、设备身份交换和升级协议。
- **Camera/多媒体：** V4L2、Device Tree、MIPI CSI-2、RKCIF、RKISP、RAW/NV12、GStreamer、MPP、H.265/RTP。
- **系统工程：** Debian rootfs、ROS1、U-Boot、Loader、完整镜像打包、SHA-256/UUID、刷写与恢复。
- **调试验证：** logcat、dmesg、tcpdump、candump/cansend、media-ctl、v4l2-ctl、串口、示波器、逻辑分析仪。

## 核心成果

- 建立手持设备 MCU 控制、RK-MCU 协议和 UART-IAP 的完整技术链路。
- 完成 IMX415 1080p60 RAW/NV12 各 600 帧硬件验证并定位 AIQ 黑帧根因。
- 完成 AGS MCU A/B 升级 69 项主机测试和 10 轮真机交替升级。
- 参与构建 RK3588 可交付镜像，完成 ROS、Camera、MPP、RTP 等指定范围真机验收。
- 从“实现单点功能”成长为能够跨 MCU、SoC、驱动、服务和网络分层定位问题。

## 30 秒总结

实习期间，我主要负责 STM32 MCU 控制、RK-MCU 通信协议和固件升级，并参与 RK3562/RK3588 板端服务、Camera 和系统镜像工作。我的优势是能够把 MCU、Linux/Android、UART/CAN 和真实硬件链路串起来，通过日志、抓包和硬件验证完成问题闭环。转正后希望继续提升通信可靠性、升级安全性和系统交付自动化，逐步独立负责嵌入式子系统。
